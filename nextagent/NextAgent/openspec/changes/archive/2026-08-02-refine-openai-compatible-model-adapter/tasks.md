## 0. Frozen contract 与架构门禁

- [x] 0.1 完成 frozen `agent-contracts` 目标契约的补充群内确认，并在 `references/model-provider-options-contract-confirmation.md` 记录参与者、日期、结论、异议条目和 follow-up。
  来源：design“群内确认”；`references/model-provider-options-contract-confirmation.md`。
  验证：确认记录状态为“已确认”；除既有范围外，明确记录 2026-07-30 需求方确认的开放 `providerOptions`/reserved collision、通用 optional `FetchGateway` 与 app composition、LOCAL 缺省/仓库外 REMOTE 边界、当前 change 只把该能力交给模型 adapter 而不迁移其他 REST client、删除 header policy、raw `agentId` 不拒绝、hook readonly low-copy view/replacement-only detach，以及 2026-07-31 确认的 frozen `systemConfig.modelProfiles` 直接校验、删除 configured ids/membership、保留既有 catalog query 名称和两个 app-facing model ports、推荐 operation identity 由 session service 自有 UUID generator 建立且不由 app composition 注入，并明确 summary generation 承接 context assembly trusted string-only `flowVariables`、排除原始用户问题 `input_question`、不 hard-code 空对象或 deep clone runtime context。

- [x] 0.2 建立跨 Function architecture/contract gate，固定 `agent-app` 以 frozen `systemConfig.modelProfiles` 校验 Agent assembly 模型引用，`agent-model` 对 lazy catalog resolution、provider SDK、correlation-header composer 和 same-model retry 的 ownership，Gateway 对运行环境通用 fetch contract 的 ownership，Context Engine 的 selection ownership，Core 的 cross-model fallback orchestration ownership，以及 Gateway bindings 完成后由 app 把 optional fetch 装配给当前模型 consumer 的边界；不得保留 configured ids/membership、header policy 或平行 model catalog 命名。
  来源：`FN-4.1 调用模型`、`FN-4.2 模型失败降级`、`FN-4.3 装配上下文`；design“唯一责任边界和依赖方向”。
  验证：`npm run lint:architecture`；相关 app/model architecture tests；assertions 覆盖 SDK 隔离、通用 `FetchGateway` 只位于 gateway contract、binding 名为 `fetch`、LOCAL 不实现该 port、app 先完成 Gateway binding 再注册模型、request/hook/provider options 不携带 fetch/header、header policy 零结果、single retry owner 和 owner-package narrow dependencies。

## 1. `FN-4.1 调用模型`

- [x] 1.1 更新 model 与 Agent App configuration public contracts/runtime schemas：实现 app-owned `ModelProviderProfile` 父层、canonical `ModelProfile` 子层、`ModelCatalogEntry`、`ResolvedModelConfiguration`、app-private `ModelCatalogQueryService(list/get)`、provider-private `ModelGatewayModelInformationService`、独立 `ModelInvocationService(complete/stream)`、既有 `ModelInvocationScope` 的单一 closed target shape、closed `ModelInvocationRequest`、只表达 provider-neutral 终态事实的 closed `ModelFinalResult`，以及只声明流式终态 shape 的 `ModelStreamTerminalResultSchema`；`agent-contracts/model` 不导出终态判断 helper 或消费行为。app-facing model ports 只保留 catalog query 与 invocation，不建立 configured ids/membership、selection catalog 组合接口或 mega-interface；available catalog entry 不在 `configuration.modelId` 外重复顶层 `modelId`，selection 原样复用其 frozen `ResolvedModelConfiguration`。在 `agent-contracts/model` 建立唯一 canonical `ModelInferenceOptions` type/runtime schema，恰好包含 `temperature/maxOutputTokens/topP/topK/presencePenalty/frequencyPenalty/thinking/providerOptions`，供 Capability patch、Prompt Template 和 Skill metadata 复用结构规则但不授予平行来源 authority。`RawDefaultSystemConfig/DefaultSystemConfig` 只提供两层 `modelProfiles[]`，父层包含 `providerId`、optional `baseUrl/credentialRef` 和 required `models[]`，子层包含唯一 `modelId`、模型画像、required `fallbackEligible`，以及 optional `temperature/maxOutputTokens/topP/topK/presencePenalty/frequencyPenalty/thinking`、`providerOptions`、`timeoutMs` 和 `maxRetries`。产品配置 `providerId` 清单恰好为 `openai-compatible | model-gateway`；配置项通过校验即进入目录；Host 模型配置 public projection 只保留含 frozen `modelProfiles/modelProfileValidationEvidence` 的 `systemConfig`，不建立重复 registry/index。现有 `profileId/modelProfileId/providerKind/modelName/enabled/modelOptions` 原子迁移到目标 closed contracts，不保留 alias 或 dual read/write；model composition input/public type 同时删除 `RuntimeModelProviderKind/modelProviderKind/productModelProviderKind`，每个父项只以 `providerId` exact lookup 已装配 registration。迁移 bundled `default-system.yaml` 为同一嵌套 shape，把现有 `profileId/modelName` 收敛为单一 `modelId`，把 `modelOptions` 展开为目标扁平字段，并原样保留现有显式 `temperature=0.2`、`maxOutputTokens=2048`、`topP=1` 与 `timeoutMs=120000`；只有原配置省略的字段才使用 catalog 固定默认值，空 `providerOptions` 与 `enabled=true` 分别由字段缺失和“配置存在即启用”无损表达；同步 env projection。bundled Agent manifest 省略部署相关模型引用，由通用 assembly compile 规则从 frozen system config 解析，不保留 builtin normalization、`default-openai` alias 或 manifest 环境变量解析；test composition 与 fixture 仍使用调用方显式提供的 exact `modelId`。
  来源：`model-invocation-contract` 的“全局模型目录提供安全模型配置”“Agent App system config 使用 canonical model/provider 配置”“Invocation scope represents real lifecycle coordinates”“Target-state request fields are stable invocation inputs”。
  验证：`npx vitest run --config vitest.config.contract.ts tests/contract/model-provider-configuration-contracts.test.ts tests/contract/model-invocation-contracts.test.ts tests/contract/core-contracts.test.ts`；`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/system-config.test.ts packages/agent-app/tests/configuration-composition.test.ts packages/agent-app/tests/composition.test.ts`；`rg -n "RuntimeModelProviderKind|modelProviderKind|productModelProviderKind|ConfiguredModelLookup|ConfiguredModelMembership|ModelSelectionCatalog" packages tests` 返回零结果；覆盖两层 closed config、产品 `providerId` exact 清单及大小写/unknown negative、配置存在即进入模型目录、全部八个 inference fields 的 profile 配置/冻结/解析、bundled profile 显式 `temperature=0.2/maxOutputTokens=2048/topP=1/timeoutMs=120000` 的转换保真、duplicate provider registration、重复 model identity、provider-owned access validation、credential omission=no credential、fallback-only invalid credential degraded-ready、primary/shared-provider invalid credential fail-closed、其他 config error 不降级、config/env projection、Host 只通过 frozen system config 读取模型配置/evidence 且无重复 registry、scope `operationId`、optional run triple all-or-none、nested scope rejection、request/scope/result unknown-field rejection、恰好两个 app-facing model ports、available entry 不重复顶层 `modelId`、selection/catalog configuration exact shape reuse、lazy Gateway result、safe catalog projection，以及配置省略时 `temperature=0.55`、`maxOutputTokens=32,000`、`topP=1`、其他 inference options 保持缺失、`timeoutMs=30,000`、`maxRetries=2`、display/default-model/selection-model/locale/context-patch/hook-mutation 缺省语义。

- [x] 1.2 保持按唯一 `providerId` exact lookup 的可信 provider runtime registry、catalog lazy resolution 与 health 行为；删除 registration 的 header-policy ownership，并让 compatible registration 只接收 app 在 Gateway bindings 完成后从通用 optional `FetchGateway` 适配出的 private fetch function，不直接依赖 gateway contract subpath。
  来源：`model-invocation-contract` 的“全局模型目录提供安全模型配置”“Invocation preconditions are validated before provider execution”；stable `system-health-check` 的“The primary health endpoint is a bounded live check”“The deep health endpoint runs real checks in-band”。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/health-check.test.ts packages/agent-model/tests/health-probe.test.ts packages/agent-app/tests/model-preload-composition.test.ts packages/agent-app/tests/configuration-composition.test.ts packages/agent-platform-gateway-remote/tests/remote-gateway-provider.test.ts`；覆盖 duplicate/unknown model、configured-profile inclusion、provider binding、ready/assembly publication/primary health 前零 Gateway metadata call、`get` 单模型解析、`list` 全目录解析且单模型失败不丢失其他 entry、unknown get 零 provider call、同模型并发 single-flight、取消后可重试、重复查询复用 frozen entry、Gateway `FOUND/NOT_FOUND/UNAVAILABLE`、model-scoped degradation、app ready、Agent activation，以及显式 deep health 使用同一 query/signal 解析 default/first activated model 并安全投影结果。

- [x] 1.3 保持模型调用前置校验与固定 Agent/Session/Request/Run correlation-header composer；删除额外 header-policy 分支，确认可信 raw `agentId` 原值作为固定 Agent header 发送，Model Gateway 透传完整 canonical request 但不把 scope 转为模型可见/provider-native body。
  来源：`model-invocation-contract` 的“Invocation scope represents real lifecycle coordinates”“模型 transport 通过可选 Gateway fetch 装配”。
  验证：`npx vitest run packages/agent-model/tests/openai-compatible-provider.test.ts packages/agent-model/tests/lifecycle-hook-wrapper.test.ts`；覆盖 full-run/无-run exact headers、raw agent-id positive case、operation non-influence、无 policy marker、Gateway envelope、background hook execution 和 request-run timeline absence。

- [x] 1.4 使用固定 AI SDK 6 compatible adapter 保持 Chat Completions 非流式/流式、flat optional inference fields、logical-invocation total timeout、single-owner same-model retry、完整 tool call、安全终态和 best-effort usage；把 inner `providerOptions` 包装到 AI SDK `openaiCompatible` namespace，未知 JSON fields 原样透传，只拒绝 canonical 顶层与 identity/access/transport reserved collision；顶层 `thinking` 映射为唯一 provider-native reasoning authority；custom fetch 只适配 app 交付的 optional fetch function，缺失时省略 SDK fetch，删除 header policy。
  来源：`model-invocation-contract` 的“OpenAI-compatible 调用遵循统一 Chat Completions 语义”“可恢复错误按受控次数重试”“成功调用尽量保留 provider usage”“Failure exits are explicit and safe”。
  验证：`npx vitest run packages/agent-model/tests/openai-compatible-provider.test.ts packages/agent-model/tests/lifecycle-hook-wrapper.test.ts packages/agent-core/tests/run-bound-model-invocation.test.ts packages/agent-core/tests/model-fallback-orchestration.test.ts packages/agent-workflow/tests/workflow-llm-enhancement.test.ts`；`npx vitest run --config vitest.config.release.ts packages/agent-platform-gateway-remote/tests/remote-gateway-provider.test.ts`；`npx vitest run --config vitest.config.contract.ts tests/contract/model-invocation-contracts.test.ts`；`npm run lint:architecture`；覆盖 unknown option pass-through、known AI SDK option、reserved camelCase/provider-native aliases rejection、optional Gateway fetch、default platform fetch、signal、固定 headers、`/chat/completions`、stream/abort/retry/timeout/tool/usage/safe error、只由 finish reason/safe error 证明流式终态、`stop + toolCalls`、content-filter/error/unknown 安全收敛，以及 `retryable=false` 不触发 fallback；既有 `length` 恢复测试保持不变。

- [x] 1.5 原子迁移 run-bound `MODEL_INVOCATION_STARTED | MODEL_INVOCATION_COMPLETED | MODEL_INVOCATION_FAILED` producer、runtime/persisted schema、历史读取与 `agent-observability` timeline mapper/trace/log projector，使 timeline 安全 identity 只使用 `stepId/modelId`；timeline `stepId` 是 run-bound scope `operationId` 的原值投影，同模型 retry 只有一个事件对，cross-model fallback 复用 operation value 并切换 `modelId`。Observability mapper 必须以 `stepId/modelId` 校验 event，只把 `runId + stepId` 用于进程内 duration/first-visible pairing，删除 `stepId/modelId/providerId/providerKind/modelProfileId/modelName` diagnostic projection，同时保留 duration、usage、first-visible、failure 与 stable run/timeline refs。
  来源：`model-invocation-contract` 的“模型调用时间线使用 canonical identity”。
  验证：`npx vitest run packages/agent-core/tests/run-bound-model-invocation.test.ts packages/agent-core/tests/model-fallback-orchestration.test.ts`；`npx vitest run packages/agent-observability/tests/timeline-observation-mapper.test.ts packages/agent-observability/tests/trace-projector.test.ts packages/agent-observability/tests/trace-projector-negative.test.ts packages/agent-observability/tests/structured-log-projector.test.ts`；断言 producer、persisted schema、历史读取使用 exact `stepId/modelId`，mapper 保留 duration/usage/first-visible/failure，trace/log 不出现 step/model/provider identity，旧字段不存在且 background recommendation 不产生 run-bound timeline/observation。

- [x] 1.6 将 bundled 与 local runtime package 的 `default-system.yaml` 序列化为真正 YAML，保留全部配置值与 env projection；打包资源路径解析和测试消费者统一按 YAML-compatible parser 读取，不建立 JSON-only 第二套实现。
  来源：design“bundled `default-system.yaml` 的目标模型段固定为”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/system-config.test.ts packages/agent-app/tests/runtime-logging-config.test.ts packages/agent-app/tests/task-callback-config.test.ts tests/local-runtime-package.test.ts tests/fullstack-packaging-boundary.test.ts`；断言 bundled 与 packaged 文件拒绝 `JSON.parse`、YAML parse 后配置值保真、package validation/startup 与自定义资源目录仍可用。

- [x] 1.7 将 default Agent assembly 测试 fixture 从产品 assembly 目录归位到 `agent-app` testing 实现目录，名称显式表达 test fixture，删除无效 `workspaceDir` 和隐式 `deterministic-test-model` 默认参数；全部调用点显式传入 fixture `modelId`，产品 composition 不得引用该 testing helper。
  来源：design“配置 schema、目录 identity 与 bundled Agent 迁移”。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/main-path.test.ts tests/agent-kernel/cancellation-boundary.test.ts tests/agent-kernel/terminal-consistency.test.ts`；`rg -n "createDefaultAssemblyRegistry|src/assembly/default-agent-assembly|modelId = \\"deterministic-test-model\\"|_workspaceDir" packages tests` 返回零结果；`npm run lint:architecture`。

- [x] 1.8 收敛 composition 命名与模型预加载输入：model 子层使用 `PreparedModelComposition`、`prepareModelComposition`、`composeModelRuntime` 和 `resolveConfiguredModelGatewayProvider`；`PreparedCompositionInputs` 只携带单一 `model: PreparedModelComposition`，不拆散 provider preparation，也不携带最终 invocation service 作为 runtime input；configured runtime 接收 frozen assembly registry 与 lifecycle-hook invocation port，在 `agent-model` 内部完成 authorization、model hook、catalog availability 和 provider invocation，只向 app 返回 `modelCatalog` 与唯一 `modelInvocationService`；app 不导入或分发 authorization/hook wrapper，不建立 configured/raw/authorized/run-bound invocation alias；顶层产品装配只保留 `ProductCompositionOutcome`、`ProductHostCompositionInput`、`runProductComposition*` 和 frontend finalizer 命名；只选择 channel auth/frontend hosting 变体的 contract/helper 使用 `HostCompositionSelection`/`defaultHostCompositionSelection`，删除误导性旧名、caller-specific model alias 和单字段 explicit-model wrapper。
  来源：design“模型 transport 通过可选 Gateway fetch 装配”的 composition 分层命名决策。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/model-preload-composition.test.ts packages/agent-app/tests/composition-input-characterization.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts`；`rg -n "PreparedProductModelComposition|preloadProductModelCompositionSync|resolveProductModelGatewayProvider|ExplicitProductModelInput|ProductCompositionSelection|defaultProductSelection" packages tests` 返回零结果；`rg -n "preloadModelCompositionSync|configuredModelInvocationService|runBoundModelInvocationService|authorizedModel|lifecycleModel|observedModel|backgroundModel|workflowModelInvocation|modelRuntime\\.|createAssemblyAuthorizedModelInvocationService|createLifecycleHookModelInvocationService" packages/agent-app/src` 返回零结果；`rg -n "assembly-authorization|lifecycle-hook-wrapper" packages/agent-model/src/index.ts` 返回零结果。

- [x] 1.9 OpenAI-compatible 唯一 production registration 依赖启动校验静态建立 catalog entry，不实现 Gateway-only lazy model-information resolver，也不得合成不可达的零窗口；测试脚本化模型通过测试 `ModelGatewayProvider` 的 model-information service 返回原测试 profile 窗口，不建立 injected compatible registration。
  来源：design“FN-4.1 调用模型”“FN-10.5 集成外部系统”的 app-to-model composition。
  验证：`npm run build`；`npx vitest run --config vitest.config.release.ts packages/agent-model/tests/openai-compatible-provider.test.ts packages/agent-model/tests/model-catalog.test.ts packages/agent-app/tests/model-preload-composition.test.ts`；`rg -n "contextWindowTokens: modelProfile\\.contextWindowTokens" packages` 返回零结果。

- [x] 1.10 删除零调用且与 `env:`/`file:` frozen SecretReference grammar 冲突的 legacy `assertEnvCredentialReference`。
  来源：design“FN-6.9 引用密钥”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/system-config.test.ts`；`rg -n "assertEnvCredentialReference" packages tests` 返回零结果。

- [x] 1.11 将 Context budget contract 的 stale `ModelOptions.maxOutputTokens` 注释更新为 canonical `ModelInferenceOptions.maxOutputTokens`。
  来源：design“FN-4.3 组装模型上下文”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-context-engine/tests/assemble-context.test.ts`；`rg -n "ModelOptions\\.maxOutputTokens" packages tests` 返回零结果。

- [x] 1.12 将 provider registration composition 完整归还 `agent-model`：model-owned 无副作用 preparation 对 validated profiles 与 trusted Gateway provider inputs 执行 required-singleton 校验并返回 opaque input，app model 子层只把该 input 收敛为单一 `PreparedModelComposition`，预加载时只追加 credential resolver 与从 `FetchGateway` 适配的 private fetch function；configured catalog factory 独占 registration 构造和 provider exact matching，app 不保留 registration helper、低层 factory 调用或可供其他 package 绕过 configured factory 的根导出。
  来源：design“配置 schema、目录 identity 与 bundled Agent 迁移”的 catalog bootstrap owner；“FN-10.5 集成外部系统”的 app-to-model composition。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-model/tests/model-catalog.test.ts packages/agent-app/tests/model-preload-composition.test.ts tests/agent-kernel/main-path.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts`；`rg -n "createModelCatalogRuntime|createOpenAICompatibleModelProviderRegistration|createModelGatewayProviderRegistration|ModelProviderRuntimeRegistration" packages/agent-app/src packages/agent-model/src/index.ts` 返回零结果。

- [x] 1.13 将 scripted-model app factory 完整收敛到 `agent-app/testing`：生产 `create-app.ts` 只保留真实 provider 的 `createNextAgentApp`/async 入口，`create-local-configured-app.ts` 只保留 production local-configured entrypoint；删除两处 production `createComposedApp` wrapper/re-export 与 `createLocalConfiguredComposedApp`，测试调用统一经 testing surface；`CreateNextAgentAppOptions` 和 production host options 不暴露 injected `ModelInvocationService`，testing positional model 在 testing surface 内包装为测试 `ModelGatewayProvider` 并把测试 config 投影为一致的 `model-gateway` profiles，`runProductComposition*`、internal composition options、`PreparedModelComposition` 与 configured runtime factory 均不接受最终 invocation service。
  来源：design“模型 transport 通过可选 Gateway fetch 装配”的 production/testing composition surface。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/composition.test.ts packages/agent-app/tests/composition-runner.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts tests/architecture/module-structure.test.ts`；`rg -n "export (async )?function createComposedApp|createLocalConfiguredComposedApp|injectedModel|createInjectedOpenAICompatibleModelProviderRegistration" packages/agent-app/src/composition packages/agent-model/src` 返回零结果；source assertion 固定 testing import 必须来自 `@nextagent/agent-app/testing`，且 testing config/provider binding 一致。

- [x] 1.14 删除残留的 service-input/service-output 环：移除 configured model runtime 与 app internal composition 中全部 `injectedModel` 字段、compatible injected registration 和条件分支；在 `agent-model/testing` 提供只供测试使用的 `ModelGatewayProvider` adapter，`agent-app/testing` 统一投影测试 profiles、装配该 provider 并保留 canonical request 捕获；增加负向 architecture assertion，禁止 production model/app composition 接受 `ModelInvocationService`。
  来源：design“模型 transport 通过可选 Gateway fetch 装配”的 production/testing composition boundary。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-model/tests/model-catalog.test.ts packages/agent-app/tests/model-preload-composition.test.ts packages/agent-app/tests/composition-runner.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts`；`rg -n "injectedModel|createInjectedOpenAICompatibleModelProviderRegistration" packages/agent-app/src packages/agent-model/src` 返回零结果。

- [x] 1.15 分离 model config authoring 类型与不可信运行时校验：`RawModelProfileConfig` 复用 canonical `ModelProfile` 的强字段类型，为开发者保留 string/number/boolean/`ThinkingOptions`/`JsonObject` 提示；validation 入口改为 `readonly unknown[]`，provider/profile parser 只在 `Record<string, unknown>` 上逐字段收窄后构造 validated profile，不以 authoring type 代替 runtime validation。
  来源：design“配置 schema、目录 identity 与 bundled Agent 迁移”的 authoring/runtime boundary。
  验证：`npm run build`；`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/system-config.test.ts tests/agent-kernel/config-assembly.test.ts`；architecture source assertion 固定 `RawModelProfileConfig = ModelProfile`、unknown parser inputs 与运行时非法字段/类型拒绝。

- [x] 1.16 删除没有独立生命周期或消费者的 `ModelProfileRegistry`：production `NextAgentApp` 只通过 frozen `systemConfig.modelProfiles/modelProfileValidationEvidence` 向可信 Host 投影模型配置；诊断直接消费同一 validation evidence；删除 registry 实现、testing export、composition layer 和所有重复 lookup/index，不建立替代 helper 或第二套权威。
  来源：`model-invocation-contract` 的“Agent App system config 使用 canonical model/provider 配置”“可信 App Host 可读取配置快照但运行期模型功能不依赖它”；design“全局模型目录的启动索引与按需解析”。
  验证：相关 contract/release/architecture tests；`rg -n "ModelProfileRegistry|modelProfileRegistry|createModelProfileRegistry|reportModelProfileValidationEvidence|composeModelProfileLayer" packages tests` 返回零结果；断言模型配置及 nested options、validation evidence array/items 保持冻结，诊断仍产生相同安全 code，production App public keys 不包含重复 registry。
  验证结果（2026-07-31）：release 定向测试 3 个文件 92 项、contract 定向测试 1 个文件 2 项、architecture 定向测试 1 个文件 45 项全部通过；上述 `rg` 返回零结果。

## 2. `FN-10.1 注册和执行钩子`

- [x] 2.1 保持 `BEFORE_MODEL_INVOKE` closed mutation schema 与 replacement-only runtime reducer；把 model hook wrapper 的 nested boundary 改为运行期 readonly low-copy view，禁止原地别名污染，同时只对实际 replacement fields detach/canonicalize，未替换字段不做 full-request deep clone；selected model、scope、Owner/Agent Scope、provider access、transport 和 execution budget 保持受保护。
  来源：`lifecycle-hook-execution` 的“Stage-specific boundaries and mutations are minimal runtime contracts”。
  验证：`npx vitest run --config vitest.config.contract.ts tests/contract/core-contracts.test.ts`；`npx vitest run packages/agent-model/tests/lifecycle-hook-wrapper.test.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-coverage-gaps.test.ts`；覆盖 messages/tool/providerOptions/thinking nested in-place mutation、replacement-after-return mutation、unchanged-field reference reuse、合法 mutation、protected authority 和 safe mutation summary。

## 3. `FN-4.3 装配上下文`

- [x] 3.1 实现 `ModelSelectionService.select(request, signal)`、复用既有 `identityContext` 并显式携带 accepted `agentId/agentVersion/agentAssemblyRef` 的 closed request、closed result、exact failure reasons，以及接收 trusted `ContextAssemblyOptions` 的 `ContextEnginePort.assemble`。accepted Assembly 已在 publication 阶段完成模型引用校验，selection 不重复 unknown activation 校验；`mode` 只允许 `INITIAL | FALLBACK` 并强制 attempted-id 组合约束。选择只读取 accepted Agent 激活且 catalog 中 `AVAILABLE` 的模型，`SELECTED.configuration` 原样复用 catalog available entry 的 frozen configuration。把 `ModelInputRenderRequest.providerOptions` 和 `RenderedModelInput.providerOptions` 收敛为 optional carry。Context Engine 对前七个 provider-neutral inference fields 固定执行 selected safe profile configuration → selected Prompt Template → governed Capability patch → trusted request 的逐字段覆盖；call-level `providerOptions` 的 Capability layer 只接受受治理 Skill patch，并按 Prompt Template → Skill → trusted request 顶层浅合并且同名嵌套对象整体替换，不读取 private profile defaults，全部 call-level 来源缺失时保持字段缺失且不合成空对象。模型调用边界再把 private profile `providerOptions` 置于 call-level composite 之前，并应用 governed hook 作为最后一层。
  来源：`context-engine` 的“Model selection uses Agent-activated model configurations”“Context Engine separates assembly from rendering”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-context-engine/tests/model-selection-service.test.ts packages/agent-context-engine/tests/assemble-context.test.ts`；覆盖 `identityContext + explicit Agent fields` closed schema、选择首次触发 activated Gateway model 的 safe query、available configuration exact reuse、`INITIAL/FALLBACK` allowed values 与非法组合、默认顺序、unknown/unavailable、cancellation、compatible model filter、attempted ids、七个 provider-neutral fields precedence、call-level `providerOptions` shallow merge/nested replacement/no-empty-object、duplicate reasoning control rejection、private profile non-exposure 和来源治理。

- [x] 3.2 实现 fallback reassembly，并使 summary generator 与 workflow model node 使用统一 selection result；切换模型时重新计算 prompt compatibility、window budget、compaction、flat options 和 render，summary 不再包裹 caller-level same-model retry，也不得在 call-level `providerOptions` 缺失时合成空对象。
  来源：`context-engine` 的“Model selection uses Agent-activated model configurations”“Fallback selection recomputes model-specific context”“Context Engine separates assembly from rendering”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-context-engine/tests/model-selection-service.test.ts packages/agent-context-engine/tests/assemble-context.test.ts packages/agent-context-engine/tests/traceable-summary-generation.test.ts packages/agent-workflow/tests/workflow-llm-router.test.ts`；覆盖未声明 `maxOutputTokens` 时按 `32,000` 预留、覆盖值重算、call-level provider options 缺失保持缺失、无正 input budget 时 fail closed。

- [x] 3.3 让 `agent-model` configured runtime 复用同一 frozen definition/binding state，但只向 app composition 暴露既有 `ModelCatalogQueryService(list/get)` 与独立 `ModelInvocationService(complete/stream)`；app 仅按 composition-root 职责分发这两个端口，Context Engine selection 直接接收命名的 `ModelCatalogQueryService`，不得建立 configured ids/membership、平行 catalog 名称、匿名交叉类型或 mega-interface。
  来源：design“配置 schema、目录 identity 与 bundled Agent 迁移”的明确 model ports；`context-engine` 的“Model selection uses Agent-activated model configurations”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-model/tests/model-catalog.test.ts packages/agent-context-engine/tests/model-selection-service.test.ts packages/agent-app/tests/model-preload-composition.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts`；`rg -n "interface ModelCatalogRuntime|ModelCatalogQueryService\\s*&|modelRuntime\\.(?:assertActivated|has|get|list|complete|stream)" packages tests` 返回零结果。

- [x] 3.4 修复 summary generation 的 flow-variable handoff：Core 只把 runtime `RequestContext.flowVariables` 的 trusted string entries 浅投影到 `ContextAssemblyRequest`，排除非 string state 和原始用户问题 `input_question`；compression 通过 required `TraceableSummaryGenerationRequest.flowVariables` 传递同一 trusted map；summary model selection 与最终 prompt assembly 使用该 map，不再 hard-code 空对象或建立第二套 projection。
  来源：`context-engine` 的“Model selection uses Agent-activated model configurations”；`prompt-template-assembly` 的“Prompt assembly has one decision boundary”“Prompt template selection is deterministic”；design“直接模型调用方的迁移规则”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-core/tests/agent-routing-core.test.ts packages/agent-context-engine/tests/context-compression-orchestrator.test.ts packages/agent-context-engine/tests/traceable-summary-generation.test.ts`；`npx vitest run --config vitest.config.contract.ts tests/contract/context-assembly-contracts.test.ts`；覆盖 runtime trusted string boundary projection、非 string runtime state 与 `input_question` 排除、compression request handoff、selection/assembly 同值复用和无 flow variables 时空映射语义。
  验证结果（2026-07-31）：release 定向测试（含 request-scoped summary 回归）4 个文件 93 项、contract 定向测试 1 个文件 10 项全部通过；`npm run build`、`npm test`（119 文件 1118 项）、`npm run test:contract`（40 文件 337 项）、`npm run lint:architecture`（41 文件 256 项）与 `openspec validate --all --strict`（260 项）全部通过；覆盖 `input_question` 和非 string runtime state 的负向排除。

- [x] 3.5 收敛 summary generator production 入口：`DefaultTraceableSummaryGenerator.generate()` 直接拥有 request Agent scope 下的 assembly lookup、model selection、prompt assembly 和 invocation request construction；`agent-app` 直接装配该实现，删除已经没有独立生命周期或行为的 `createRequestScopedSummaryGenerator` wrapper、重复 options type、public export 和平行测试文件，不保留 alias 或第二套入口。
  来源：design“直接模型调用方的迁移规则”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-context-engine/tests/traceable-summary-generation.test.ts packages/agent-app/tests/composition.test.ts`；`rg -n "RequestScopedSummaryGenerator|createRequestScopedSummaryGenerator|request-scoped-summary-generator" packages tests` 返回零结果；`npm run build`、`npm test`、`npm run test:contract`、`npm run lint:architecture` 与 `openspec validate --all --strict` 全部通过。
  验证结果（2026-07-31）：release 定向测试 2 个文件 58 项通过，目标旧名称在 `packages`/`tests` 中零结果；`npm run build`、`npm test`（119 文件 1118 项）、`npm run test:contract`（40 文件 337 项）、`npm run lint:architecture`（41 文件 256 项）与 `openspec validate --all --strict`（260 项）全部通过。

## 4. `FN-10.4 自定义工具和提示词`

- [x] 4.1 将 prompt authoring 迁移为只允许 canonical `modelId` string 直接赋值给 `match.model`，拒绝 nested object、array、null 和非法 scalar；让 template `modelOptions` compiler/type/handoff 复用 canonical `ModelInferenceOptions`，支持与模型配置一致的八个 optional inference fields：`temperature/maxOutputTokens/topP/topK/presencePenalty/frequencyPenalty/thinking/providerOptions`，省略字段不合成默认值；compiler 校验 closed shape、null 和保留字段，最终模型确定后由 selected adapter 校验 `providerOptions`，并拒绝与顶层 `thinking` 重复的 reasoning controls；拒绝 provider access、`timeoutMs` 和 `maxRetries`；compatibility evaluator 只产生 canonical compatible `modelId` 集合，final prompt assembly 只消费统一 selection result 中的 selected `modelId`。
  来源：`prompt-template-assembly` 的“Prompt assembly has one decision boundary”“Prompt template selection is deterministic”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-context-engine/tests/prompt-template-assembly.test.ts packages/agent-context-engine/tests/model-selection-service.test.ts`；覆盖 exact canonical `modelId` matching、closed authoring、八字段 option 正负边界、`providerOptions` compatible/incompatible selected-provider 校验与 raw-value non-leakage、optional no-override、source priority、specificity、fallback、provider access/timeout/retry 和 unknown-field rejection。

## 5. `FN-1.20 查看推荐问题`

- [x] 5.1 保持 `postTerminalCallback(command, run, status)` 与 `SuggestedQuestionRequest` 既有 closed contract；terminal 预计算与 Web cache miss/on-demand 生成汇入同一推荐模型调用边界；推荐服务只在每次实际启动模型调用前通过 service-owned cryptographically secure UUID generator 建立 fresh `operationId`，App composition 不注入或感知 generator；服务通过统一 selection result 构造 background `complete()` request，并保留 completed run 的真实 causal correlation；推荐与其他调用方使用同一个 `ModelInvocationService` 并执行 model hook，但不进入 request-run hook/model timeline，background `PEND` 不创建 pending truth。缓存命中、非 `COMPLETED` terminal status、attachment cleanup 和其他 callback consumer 不生成虚假推荐 identity，也不改变既有语义。
  来源：`question-recommendation` 的“Model Invocation for Recommendations”。
  验证：`rg -n "postTerminalCallback|SuggestedQuestionRequest" packages tests`；`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/suggested-question-service.test.ts packages/agent-app/tests/composition.test.ts packages/agent-channel-web/tests/suggested-questions-routes.test.ts`；覆盖三参数 callback characterization、全部 caller/consumer、terminal 与 Web 两条实际生成路径的 service-owned fresh UUID、App composition generator absence、缓存命中不生成 identity、四个既有 headers、`tools=[]`、非流式调用和既有 attachment cleanup/清洗/cache/API 行为。

- [x] 5.2 将推荐 operation identity 的生成 owner 完整归还 `agent-session`：删除 `session-services-composition.ts` 的 `randomUUID`/`createOperationId` 装配和 service dependency 字段，由 suggested-question service 在确定实际调用后内部生成 UUID，并保持生成失败时 provider 不启动和空结果语义；补充负向 architecture assertion 防止业务 identity generator 回流 app composition。
  来源：`question-recommendation` 的“Model Invocation for Recommendations”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/suggested-question-service.test.ts packages/agent-app/tests/composition.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts`；`rg -n "randomUUID|createOperationId" packages/agent-app/src/composition/session-services-composition.ts` 返回零结果。

## 6. `FN-8.3 记忆提取和老化`

- [x] 6.1 使 memory scheduler/cycle owner 为每个 accepted extraction cycle 冻结 `cycleId`；LLM strategy 使用统一 selection result 构造 prompt/background invocation，把 `cycleId` 同值映射为 scope 的唯一 `operationId`，省略不存在的 session/request/run coordinates，并通过同一个 `ModelInvocationService` 执行 model hook而不创建 request-run pending/timeline truth。
  来源：`memory-extraction` 的“Extraction strategy and configuration”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-memory/tests/memory-extraction-llm-strategy.test.ts packages/agent-app/tests/composition.test.ts`；覆盖 cron/controlled trigger identity、Agent-only header、selection failure、default locale 和既有 RULE_FIRST/prompt/candidate/degradation 行为。

## 7. `FN-4.2 模型失败降级`

- [x] 7.1 更新 Core fallback orchestrator，使其只在 model-bound same-model retry exhausted 后评估 lifecycle gate、维护 attempted model ids、调用 Context Engine trusted reassembly 并记录 evidence；Core 不选择 provider model，也不复用前一模型的渲染结果。
  来源：`model-fallback-semantics` 的“Agent Core orchestrates model fallback explicitly”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-core/tests/model-fallback-orchestration.test.ts packages/agent-context-engine/tests/model-selection-service.test.ts`；覆盖 allowed/denied/exhausted、deadline/budget、cancellation、attempted ids、visible output 和 evidence。

## 8. `FN-10.2 装配插件`

- [x] 8.1 更新 RESERVED model policy metadata：`modelSelectionPolicy` owner 为 Context Engine，`modelFallbackPolicy` owner 表达 Core lifecycle gate 与 Context Engine selection；两者保持 `RESERVED` 且不可激活。
  来源：`agent-scoped-plugin-composition` 的“Policy plugins use an explicit open policy inventory”。
  验证：`npx vitest run packages/agent-plugin-sdk/tests/plugin-sdk.test.ts packages/agent-app/tests/plugin-policy-inventory.test.ts tests/architecture/plugin-composition-boundary.test.ts`。

## 9. `FN-3.2 编译智能体装配`

- [x] 9.1 保持单一 frozen config、readiness、安全诊断、`ConfigValidationEvidence`、非模型 public projection、startup assembly compile 和 request path 不 reparse；Agent definition 省略 `modelIds` 时由通用 compiler 按 frozen `systemConfig.modelProfiles` 顺序继承全部已校验 canonical ids，显式配置必须 non-empty ordered unique 且 unknown 失败，singular Agent `modelId` 仍拒绝；`AgentAssembly` 始终携带解析后的 non-empty `modelIds` 与 optional 且必须属于该集合的 `defaultModelId`，省略 default 时由 initial selection 使用第一个 eligible id。startup compile、Capability graph validation 与受控 hot reload publication 必须直接使用同一个 frozen `systemConfig.modelProfiles`，不得建立 configured ids/membership、builtin normalization 或第二套存在性权威，且不触发 Gateway metadata、不复制模型目录项、provider access、profile defaults 或 transport facts。
  来源：`agent-package-assembly` 的“Agent Package Assembly Compiles Runtime-Ready Assembly At Startup”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/agent-plugin-definition-parser.test.ts packages/agent-app/tests/configuration-composition.test.ts packages/agent-app/tests/composition.test.ts packages/agent-app/tests/model-reference-composition.test.ts tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/invoked-agent-discovery-config.test.ts tests/agent-kernel/main-path.test.ts`；覆盖省略 `modelIds` 时 builtin/local/parent Agent 按 frozen system config 全量有序继承、显式 non-empty/order/unique 范围保持、显式空数组与 singular `modelId` rejection、optional/reference-valid `defaultModelId`、default omission 的 first-eligible selection、unknown model、known Gateway model 在 metadata 未解析时可发布、assembly freeze、ready 前零 Gateway metadata call、受控 hot reload unknown rejection/accepted publication、request path no-reparse 与 provider access/defaults absence；`git diff -G 'ConfigValidationEvidence|readinessState|deepFreeze|agentAssemblyRef|modelIds|defaultModelId' -- packages/agent-app/src/config packages/agent-app/src/composition packages/agent-app/src/assembly`。

## 10. `FN-5.1 管理能力目录`

- [x] 10.1 保持 custom Capability provider 只有在可信 adapter support 解析成功后才能贡献 executable descriptor；模型目录改造不改变 Capability provider registry、冻结顺序或 descriptor 产出。
  来源：`capability-source-configuration` 的“Custom providers require explicit adapter registration”。
  验证：`npx vitest run tests/architecture/capability-source-configuration.test.ts`；`npx vitest run --config vitest.config.release.ts tests/agent-kernel/plugin-provider-capability.test.ts`；`git diff -G 'CapabilityProvider|isProviderAdapterRegistered|assembleCapabilityProviders|provider registry|adapter' -- packages/agent-app/src/config/capability-providers.ts packages/agent-app/src/composition/capability-composition.ts packages/agent-capability/src/extension-registration.ts packages/agent-capability/src/provider-config.ts`。

## 11. `FN-5.2 调用能力`

- [x] 11.1 将 `CapabilityInvocationResult.contextPatch.modelName` 原子迁移为 optional canonical `modelId`，让 `modelOptions` 复用 canonical `ModelInferenceOptions` 八字段 closed schema，并更新 runtime schema、producer、fixture、Core consumer 与 Context Assembly handoff；`providerOptions` 只接受已接受 `SkillMetadata.modelOptions` 经受治理 Skill Tool mapper 产生的值，Core 以内部 capability target/Skill resolution 事实校验来源且不增加 public provenance 字段；非 Skill Capability result、Capability 参数、Skill input/body、模型输出和其他 metadata 的同形注入安全失败；`modelId` 缺失不覆盖模型选择，`modelOptions` 及其单个字段缺失不建立 Capability-specific 默认值；拒绝 provider access、timeout、retry、unknown、null、non-object provider options 和未激活 model id。
  来源：`capability-catalog` 的“Executors Return Results Without Owning Runtime Side Effects”。
  验证：`npx vitest run --config vitest.config.contract.ts tests/contract/core-contracts.test.ts`；`npx vitest run packages/agent-core/tests/capability-model-patch-resolver.test.ts packages/agent-context-engine/tests/assemble-context.test.ts`。

## 12. `FN-5.9 调用技能`

- [x] 12.1 使 Skill Tool 将 `SkillMetadata.model` 原样投影为 canonical `modelId`，不按 provider model/display name 反查；把 Skill manifest parser、`SkillMetadataSchema`、typed metadata、mapper 和 fixtures 的 `modelOptions` 从开放 `JsonObject` 原子迁移为 canonical `ModelInferenceOptions` 八字段 closed inference contract：`temperature/maxOutputTokens/topP/topK/presencePenalty/frequencyPenalty/thinking/providerOptions`；`providerOptions` 只能来自 source-admitted、manifest-validated、已接受的 Skill metadata，最终模型确定后由 selected adapter 执行 reserved-field validation 且 inner unknown JSON fields 保持开放；省略字段不合成默认值，Skill 不接受 provider access、`timeoutMs` 或 `maxRetries`。
  来源：`skill-tool` 的“Skill tool is the model-facing Skill execution entry”。
  验证：`npx vitest run packages/agent-capability/tests/skill-manifest.test.ts packages/agent-capability/tests/skill-tool.test.ts packages/agent-core/tests/capability-model-patch-resolver.test.ts`；覆盖八字段映射、`providerOptions` governed Skill 正路径、非 Skill 同形注入拒绝、selected-provider compatible/incompatible 校验与 raw-value non-leakage、unknown/provider-access/timeout/retry/null/non-object rejection 和 optional no-override。

## 13. `FN-6.9 引用密钥`

- [x] 13.1 把 optional `credentialRef` 从 legacy flat model profile 原子移到两层 `modelProfiles[]` 的父层 provider access config，同时保持 `env:`/`file:` SecretReference grammar、active validation、最底层 resolver/adapter handoff、encrypted envelope、独立 key source 和 raw-secret non-leakage；字段缺失表示 no credential，不增加环境自动发现、ENC 支持或 Secret resolver/adapter production path 修改。
  来源：`secret-configuration-boundary` 的“Product credentials use the frozen SecretReference grammar”。
  验证：`npx vitest run --config vitest.config.contract.ts tests/contract/core-contracts.test.ts tests/contract/model-invocation-contracts.test.ts`；`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/system-config.test.ts packages/agent-app/tests/configuration-composition.test.ts`；覆盖 provider-entry valid/invalid/missing credential reference、profile credential rejection、safe diagnostic 和 resolver handoff；`git diff -G 'resolveSecret|ENC|decrypt' -- packages/agent-common packages/agent-app` 证明 Secret resolver/adapter path 无行为改动。

## 14. `FN-10.5 集成外部系统`

- [x] 14.1 保持通用 Gateway selection、freeze、defaults、readiness、fallback 和安全 unavailable/degradation 语义；在 `agent-contracts/gateway` 新增环境中立且 Fetch-compatible 的 `FetchGateway`，由 optional `GatewayBindings.fetch` 承载，不新增 adapter kind、selection entry、LOCAL default/readiness requirement 或仓库内 REMOTE 实现；该 port 可供 app composition 下的 outbound HTTP consumer 复用，但本 change 只把它装配给 OpenAI-compatible adapter，不迁移其他 REST client；merge 对多个非空 binding fail closed，缺失不影响 LOCAL startup。
  来源：`gateway-configuration` 的“Gateway configuration is loaded and stabilized during startup”。
  验证：`npx vitest run --config vitest.config.contract.ts tests/contract/gateway-configuration-contracts.test.ts`；相关 gateway/app composition tests；覆盖 contract shape、optional absence、single binding merge、duplicate binding conflict、LOCAL no implementation/default、model-information non-regression；`git diff -G 'gatewaySelection|selectedGatewayProviderEntries|readiness|fallback|defaults|GatewayProvider' -- packages/agent-app/src/config/gateway-selection.ts packages/agent-app/src/composition/gateway-composition.ts packages/agent-contracts/src/gateway`。

## 15. `FN-10.11 开发工作台`

- [x] 15.1 在 `add-ts-dev-agent-workbench` 已归档的 stable baseline 上，原子迁移工作台 SQLite read port、process graph、server/browser projector、事件标签、详情和 effective view 到 timeline `stepId/modelId` 与 assembly `modelIds/defaultModelId`；删除 model invocation payload/read path 的 `modelProfileId/providerKind/modelName` 读取、fixtures 和显示，不保留 alias/dual read；Capability action 的既有 provider/source classification 不在该删除范围。工作台只从正式 run-bound `MODEL_INVOCATION_*` timeline event 构建 model action，background recommendation 不进入投影。
  来源：`dev-agent-workbench` 的“Run-bound model invocations use one runtime timeline boundary”“Workbench exposes a reconstructed run effective view”。
  验证：`npx vitest run packages/agent-dev-workbench/tests/sqlite-read-port.test.ts packages/agent-dev-workbench/tests/process-graph-layout.test.ts packages/agent-dev-workbench/tests/browser-smoke.test.ts`；`npm run build -w @nextagent/agent-dev-workbench`；断言 SQLite/server/browser/effective-view 使用相同 exact canonical model identity、model action 不含旧模型字段、Capability action 的既有分类保持、background recommendation 不投影。

## 16. `FN-7.5 采集指标`

- [x] 16.1 原子更新 model metric inventory、descriptors、projector、开发者指标文档和 tests：`model_invocation_total`/`model_invocation_duration_seconds` 只保留 `outcome`，`model_token_usage_total` 只保留 `token_type/outcome`，`model_ttft_seconds`/`model_total_latency_seconds` 只保留 `outcome`，`model_chunk_latency_seconds` 不含 labels；删除 `provider_kind` vocabulary/lookup，且不以 `OTHER`、`modelId`、`providerId` 或其他 provider category 替代。metric name、value、dedup、registry validation、local/remote sink 和其他 inventory 保持。
  来源：`agent-runtime-metrics` 的“Metric inventory 必须声明来源、标签和增强需求”“Metric labels 必须低基数且固定”。
  验证：`npx vitest run tests/agent-kernel/runtime-metrics.test.ts packages/agent-observability/tests/metrics-registry.test.ts packages/agent-observability/tests/local-metric-history-exporter.test.ts`；覆盖 6 个 model metrics exact labels、identity label negative cases、missing usage、duration/stream timing、dedup 与 sink non-regression；`rg -n "provider_kind|providerKindFor" packages/agent-observability tests/agent-kernel/runtime-metrics.test.ts docs/developer/22-observability-metrics.md` 返回零结果。

## 17. 跨 Function 集成

- [x] 17.1 调整唯一 app composition 路径：profile validation 后直接使用 frozen `systemConfig.modelProfiles` 完成 startup/hot-reload assembly publication；随后完成 Gateway bindings，把 optional `fetch` 交给当前模型 provider runtime registrations，建立 lazy `ModelCatalogQueryService`、独立 invocation port、compatible provider、Gateway invocation/information provider、correlation-header composer和 Context Engine selection；删除 outbound header policy。保持 model-provider health、Capability startup model-reference validation、readiness/primary health 与 `NextAgentApp` public projection 既有约束。
  来源：design“唯一责任边界和依赖方向”“最小代码落点”；stable `system-health-check` 的“The primary health endpoint is a bounded live check”“The deep health endpoint runs real checks in-band”。
  验证：`npx vitest run --config vitest.config.release.ts packages/agent-app/tests/model-preload-composition.test.ts packages/agent-app/tests/composition.test.ts tests/agent-kernel/main-path.test.ts`。

- [x] 17.2 完成 model contract/boundary、selection/consumers、composition/non-regression 三条 seam 的跨 Function验证；追加 startup/hot reload 直接通过 frozen `systemConfig.modelProfiles` 拒绝 unknown model、恰好两个 app-facing model port 的明确分发、configured runtime 内部 authorization-before-hook、authorization/hook helper root-export absence、所有 consumer 共用唯一 `ModelInvocationService`、configured ids/membership 零结果、通用 optional Gateway fetch 到当前模型 consumer 的 end-to-end、LOCAL default fetch、open provider-options unknown pass-through/reserved rejection、raw agent-id header、header-policy absence、hook nested alias isolation与 replacement-only detach、post-terminal recommendation hook execution、background `PEND` provider/pending/timeline absence、finish reason/safe error 唯一流式终态 schema、contracts 终态判断 helper absence、`stop + toolCalls` 兼容、content-filter/error/unknown 安全收敛和 non-retryable fallback denial 黑盒证据，并保持其余 design A-H target fixtures、既有 `length` 恢复和其他 non-regression 证据。
  来源：16 个受影响 Functions 与 25 个 delta specs。
  验证：`npm test`；`npm run test:contract`；相关 architecture、contract、release 和 e2e tests 均通过。

- [x] 17.3 按需求方后续确认收敛 stream ownership，并取代 1.1/17.2 中的 terminal marker/schema 子项：`ModelInvocationService.stream(request, signal, onDelta)` 统一 await delta callback、Promise 返回唯一 final；删除 `ModelStreamTerminalResultSchema` 与 Core/Workflow/Gateway terminal 判别实现，统一 model lifecycle service 校验 inner delta/final；OpenAI-compatible adapter 使用 AI SDK `timeout.totalMs` 作为 initial request、retry、backoff 的唯一总时限，删除自建 deadline controller/timer/race，并保留 caller abort、timeout、unexpected stream end 的安全分类。
  来源：本 change `model-invocation-contract` 的“Non-streaming and streaming invocation share one terminal result contract”“Profile timeout constrains provider execution”及 2026-07-31 stream service ownership 补充确认。
  验证：model contract、lifecycle wrapper、OpenAI-compatible adapter、Core、Workflow、remote Gateway 的 targeted tests；全量 build/test/contract/architecture/OpenSpec 门禁。

- [x] 17.4 按 SOLID 分离 `agent-model` 内部 runtime 职责：provider runtime abstraction、唯一 binding registry、lazy model catalog、catalog-backed invocation service 与 configured runtime composition 分属独立模块；catalog 不再创建或实现 invocation service，不按具体 `providerId` 决定 resolution path；上述内部组件中只有 configured runtime factory 通过 package root 暴露，app-facing port 仍恰好为 `modelCatalog` 与 `modelInvocationService`，不新增 public contract、membership authority、alias 或第二套实现。
  来源：design“唯一责任边界和依赖方向”中 SOLID 内部分层设计；行为保持型白盒重构。
  验证：`npx vitest run packages/agent-model/tests/model-catalog.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/app-composition-root-source.test.ts tests/architecture/workspace.test.ts`；`npm run typecheck`；`rg -n "providerId ===|providerId !==" packages/agent-model/src/catalog/model-catalog.ts` 返回零结果；全量后端与 OpenSpec 门禁通过。

- [x] 17.5 把 compatible stream 的协议聚合完整交给 AI SDK：`streamText.onChunk` 只投影实时 provider-neutral delta，终态只读取 SDK result promises；使用 AI SDK reasoning middleware 处理 `<think>` text fallback；流式与非流式经同一个 terminal mapper 消费 SDK normalized finish reason，并共用 prompt bridge，把混合请求中的 `SYSTEM` content 交给 AI SDK `system` option、禁止其回流 `messages`，同时无 warning 地保留 system-only 合法请求；删除 raw chunk 请求/解析、自建 content/reasoning/tool/usage/finish 累计状态和自定义 think-tag state machine。Agent Core、Workflow 和其他调用方只保留 timeline、可见输出限制、Tool/Capability 执行、业务投影与 cross-model fallback，不解释 provider stream、不建立第二套终态，也不重复完成 run-bound invocation 已拥有的失败终态。
  来源：design“AI SDK 6 标准调用路径”“Thinking、tool 和 usage 映射”；保持现有 `ModelInvocationService` 黑盒契约与 Core/Workflow owner 边界。
  验证：`npx vitest run packages/agent-model/tests/openai-compatible-provider.test.ts packages/agent-model/tests/lifecycle-hook-wrapper.test.ts packages/agent-core/tests/run-bound-model-invocation.test.ts`；`npx vitest run --config vitest.config.architecture.ts tests/architecture/boundaries.test.ts`；`rg -n "includeRawChunks|readOpenAICompatibleRawDelta|createThinkMarkupState|rawFinishReason|finish-step" packages/agent-model/src` 返回零结果；全量后端与 OpenSpec 门禁通过。

## 18. Change 整体验证

- [x] 18.1 完成后端、OpenSpec 和语义审查门禁。
  验证：`npm run build`；`npm test`；`npm run test:contract`；`npm run lint:architecture`；`openspec validate --all --strict`；`git diff --check`；`nextagent-skill-review` 与 `nextagent-code-review` 无阻塞 finding。
  验证结果（2026-08-01，合并 `origin/main` 后）：`npm run build`、1421 项常规测试、353 项 contract 测试、274 项 architecture 测试、受影响 change（本 change 与 `recover-ts-model-output-token-limit`）的 strict validation、`git diff --check` 均通过；`nextagent-skill-review` 和 `nextagent-code-review` 结论均为 PASS，无 P0/P1/P2 finding。全仓 `openspec validate --all --strict` 还报告 5 个随 `origin/main` 合入、且不属于本 change 的既有活跃 change 格式错误：`add-ts-skill-driven-api-call`、`add-ts-workflow-sub-recipe-node-record-info`、`enhance-ts-workflow-api-choice-node`、`enhance-ts-workflow-knowledge-qa`、`enhance-ts-workflow-output-parser-display-control`；本 change 未修改这些 artifact。

## 19. Breaking change developer source asset 离线升级

- [x] 19.1 在 `migration/model-authoring-v2/test_migrate.py` 先建立 Python 标准库行为与故障恢复测试，覆盖默认与显式路径发现、JSON/YAML system config、Agent/Prompt/Skill 同一模型映射、单模型动态 identity 的 Agent 继承与不可表达引用阻塞、dry-run 不写入、write/backup/journal、幂等、BOM/newline 与 Skill body 保真，以及 duplicate/access conflict/disabled/unknown/source-target mixed/非法目标字段/非有限 JSON/unsupported YAML/path escape/concurrent change/write failure 的零部分写入和安全报告。
  来源：proposal 目标“Agent 开发者可以在升级框架版本前离线检查并转换自己维护的资产”；design“Developer source asset 离线升级”。
  验证：实现前运行 `python migration/model-authoring-v2/test_migrate.py` 并确认目标行为因工具缺失而失败；实现后同一命令全部通过，且测试只依赖 Python 标准库。
  验证结果（2026-08-02）：首次运行因 `migrate.py` 尚不存在按预期失败；实现后 `python migration/model-authoring-v2/test_migrate.py` 的 14 项测试全部通过，覆盖额外的进程中断 journal 恢复、缺失目标文件恢复、恢复前目标变更的零部分写入拒绝、自动回滚保留并发修改、literal block 语义保真、动态模型引用无损/阻塞分支、target closed-shape/provider identity/provider-options 校验、目标 identity/credential/safe-integer 校验、严格 JSON 数值、discovery/write-time symlink/junction 拒绝和 NextAgent framework source root 拒绝。

- [x] 19.2 实现 `migration/model-authoring-v2/migrate.py` 单文件 Python 3.11+ 标准库工具：默认 `--root .` dry-run，支持分类路径 override、受限 YAML/JSON/SKILL frontmatter、单一跨文件 model mapping、目标字段转换、fail-closed validation、安全报告、root-contained backup/journal、hash-before-replace、rollback、`--recover` 和幂等；不得进入 `packages/`、跟随 symlink、读取 runtime config path 扩张 scope、输出敏感值或给 runtime 增加 alias/dual read。
  来源：proposal 目标“Agent 开发者可以在升级框架版本前离线检查并转换自己维护的资产”；design“Developer source asset 离线升级”。
  验证：`python migration/model-authoring-v2/test_migrate.py`；`python migration/model-authoring-v2/migrate.py --help`；`rg -n "model-authoring-v2|migrate\.py" packages` 返回零结果。
  验证结果（2026-08-02）：14 项 Python 测试通过；`--help` 正常列出 dry-run/write/recover 与全部路径 override；package source scan 输出 `NO_PACKAGE_MIGRATION_REFERENCES`；runtime authoring alias scan 输出 `NO_RUNTIME_LEGACY_MODEL_AUTHORING_ALIASES`，observability defensive redaction vocabulary 不属于兼容 alias。

- [x] 19.3 刷新开发者文档和工具 README，使 quickstart、system config、Agent definition、Prompt Template、Skill、deployment/troubleshooting 示例只使用 target `providerId/modelId/modelIds/defaultModelId` 和 flat canonical inference fields，并记录从目标 release/tag 源码复制独立单文件工具且不进入 runtime 运行包的获取方式、任意位置执行与 `--root` 目标边界、dry-run、write、backup/recovery、supported YAML subset、路径 override、manual-blocking cases 与 runtime 不兼容旧格式的边界；不得保留误导性的旧模型字段示例。
  来源：proposal 目标“Agent 开发者可以在升级框架版本前离线检查并转换自己维护的资产”；design“Developer source asset 离线升级”。
  验证：`rg -n "modelProfileIds|defaultModelProfileId|profileId|modelName" README.md docs/developer docs/用户配置和使用指导.md` 对当前模型 authoring 说明返回零结果；Capability provider 分类等非模型同名术语不在删除范围；文档中的迁移命令由 `python migration/model-authoring-v2/migrate.py --help` 支持。
  验证结果（2026-08-02）：扩展 stale-field scan 同时包含 `modelProfileId` 并输出 `NO_STALE_MODEL_AUTHORING_FIELDS`；残留 `providerKind` 均为 Capability provider 分类/trace 语义，不是模型 authoring 字段；迁移 CLI help 验证通过，`git diff --check` 通过。

## 20. 新增范围整体验证

- [x] 20.1 完成 Python migration、受影响 developer docs、OpenSpec strict validation 和语义审查门禁；确认 runtime config/prompt/skill parsers 没有旧字段 alias、startup auto-migration 或对 migration 目录的依赖。
  来源：proposal 目标“Agent 开发者可以在升级框架版本前离线检查并转换自己维护的资产”；design“Developer source asset 离线升级”“验证策略”。
  验证：`python migration/model-authoring-v2/test_migrate.py`；`npm run build`；`npm test`；`npm run test:contract`；`npm run lint:architecture`；`openspec validate --all --strict`；`git diff --check`；`nextagent-skill-review` 与 `nextagent-code-review` 无阻塞 finding。
  验证结果（2026-08-02）：14 项 Python migration 测试、`npm run build`、138 个文件 1429 项常规测试、43 个文件 353 项 contract 测试、44 个文件 274 项 architecture 测试、当前 change strict validation、迁移 CLI help、文档/运行时 legacy-field 与 package dependency scans、`git diff --check` 均通过；常规测试包含 compatible complete/stream/system-only prompt bridge 的 AI SDK warning 回归用例；`nextagent-skill-review` 结论 PASS，`nextagent-code-review` 对本次范围无 P0/P1/P2 finding。全仓 `openspec validate --all --strict` 为 250 项通过、5 项失败，失败项仍是合并前已记录且不属于本 change 的 `add-ts-skill-driven-api-call`、`add-ts-workflow-sub-recipe-node-record-info`、`enhance-ts-workflow-api-choice-node`、`enhance-ts-workflow-knowledge-qa`、`enhance-ts-workflow-output-parser-display-control`；本 change strict validation 通过且未修改这些 artifacts。

## 归档前更新基线检查（非实施任务）

归档本 change 前，`add-ts-dev-agent-workbench` 必须已经归档到 stable。归档流程在最新 stable baseline 上原子应用来源 `REMOVED` 与目标 `ADDED/MODIFIED`，确认 `model-provider-configuration`、`model-profile-contracts` 和 `model-info-contracts` 已清空且全部行为有 canonical 承载，再从既有 Function/Feature/spec-to-design-map/roadmap 中删除这些 legacy spec 引用并退役 stable specs，不得退役其所属 Function 或 Feature；同时同步其他受影响 stable specs、Functions、Features、architecture 和 module designs，使架构与模块文档中的模型配置和 public projection 只引用归档后的 canonical model contract。不得使用 dual read/write 或 compatibility alias 绕过迁移顺序。
