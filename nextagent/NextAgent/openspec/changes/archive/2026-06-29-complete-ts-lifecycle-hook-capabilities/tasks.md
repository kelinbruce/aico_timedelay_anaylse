## 1. Runtime Contract

> 说明：`tests/agent-kernel` 由 `vitest.config.release.ts` 纳入 release 验证；单独运行这些目标测试时使用 `npx vitest run --config vitest.config.release.ts ...`，不要使用默认 `npm test -- tests/agent-kernel/...` 入口。

- [x] 0.0 前置归档确认：`refine-ts-pending-input-contracts` 必须已归档，且 pending input question/answer、`AgentExecutionOutcome.status=PENDING_INPUT`、gateway active/due query、resolve idempotency 和 answer/cancel/timeout/recovery 所需共享 contract 已进入基线；本 change 只消费该 pending input contract，不新增 hook-private pending DTO、Record、status 或 control signal。
  验证：`openspec validate --all --strict`、code review 检查点：`openspec/changes/refine-ts-pending-input-contracts` 不再作为 active change；`rg "PendingInputQuestion|AgentExecutionOutcome|resolvePendingInput|PENDING_INPUT" openspec/specs packages/agent-contracts/src`
  来源：proposal 依赖；spec requirement `Hook control outcome and pending input remain separate`
- [x] 1.0 明确 developer-facing `LifecycleHook` interface、`LifecycleHookExecutable`、`defineLifecycleHook(...)` helper、`configure` / `execute` 函数、runtime-internal `LifecycleHookDefinition`、`agent.yaml.hooks` 和 `AgentAssembly.hooks` 的 contract 形态，定义 `CUSTOM` hook 只通过当前 accepted AgentAssembly 显式 entry 生效、`SYSTEM` hook 默认对所有 Agent 生效且可按 Agent 显式关闭；`LifecycleHook` / `defineLifecycleHook(...)` 必须使用 literal-preserving typing 保留 `supportedStages`，使 `execute` 获得 stage-indexed `HookInput<S>` / `HookResult<S>`。
  验证：`npm run build`、`npm run test:contract`
  来源：spec requirement `Hook definitions and Agent assembly activation remain separate and bounded`；design D0
- [x] 1.0a 清理旧 hook contract：移除或替换 `HookExecutionMode`、`HookDecision`、独立 `AgentHookBinding`、`HookInput.config`、`HookResult.decision`、`HookInvocationEvent.decision`、`LifecycleHookDefinition.name/source/defaultOrder/defaultTimeoutMs/defaultConfig/executionMode`，确保 public runtime contract 只暴露 effects/outcome/AgentAssembly.hooks/configure 后的目标形态。
  验证：`npm run build`、`npm run test:contract`、`rg "HookExecutionMode|HookDecision|AgentHookBinding|decision\\?|executionMode|defaultConfig|defaultOrder|defaultTimeoutMs" packages/agent-contracts/src/runtime packages/agent-runtime/src packages/agent-app/src`
  来源：design D-1；spec requirement `Developer-facing hook contracts use canonical fields`
- [x] 1.0b 实施 `ts-core-contracts` frozen baseline refinement：将 lifecycle-changing hook evidence 收敛到 timeline-only `HOOK_INVOKED`，把 hook baseline 从 executionMode/decision/independent binding 更新为 effects/outcome/AgentAssembly.hooks，允许当前 Agent 显式关闭 system hook，并确保单独 `HOOK_OUTCOME_APPLIED`、`HookDecision`、`HookExecutionMode` 不再出现在 public core contract vocabulary。
  验证：`npm run test:contract`、`rg "HOOK_OUTCOME_APPLIED|HookDecision|HookExecutionMode|AgentHookBinding|SYSTEM hooks MUST NOT be disabled|HookResult\\{ decision|decision=PEND|HookInput\\.config" openspec/specs packages tests`（仅允许历史 archive 或负向“不得 emit separate HOOK_OUTCOME_APPLIED”引用命中）
  来源：spec requirement `Hook And Pending Boundary Baseline`；spec requirement `Canonical Timeline And Stream Projection`；design D-1
- [x] 1.1 扩展 `agent-contracts/runtime` hook contract，新增 `HookEffect = OBSERVE | TRANSFORM | CONTROL`，并把非空去重的 `effects` 集合加入 `LifecycleHookDefinition` 和 `HOOK_INVOKED`；`OBSERVE` contract 必须覆盖有界、幂等、不影响当前流程的外部观察/通知类副作用，并在 HookInput / `HOOK_INVOKED` 中提供同一 stage occurrence 恢复重试稳定、不同 occurrence 区分的 idempotency key；contract 必须定义 key 为 `stageOccurrenceKey + ":" + hookId`，`stageOccurrenceKey` 不得暴露 path/prompt/model/tool/attachment/credential/side-effect payload。
  验证：`npm run build`、`npm run test:contract`
  来源：spec requirement `Hook effects define side effects and execution strategy`；design D1
- [x] 1.2 新增 `HookOutcome`，使用 `PASS`、`SKIP`、`DENY`、`BLOCK`、`PEND` 替换旧 decision vocabulary，并补充 safe diagnostic vocabulary；新增 `agent-contracts/runtime` 共享 lifecycle hook control-interruption signal、`LifecycleHookInvocationPort`、`LifecycleHookInvocationRequest` 和 `LifecycleHookInvocationResult`，port shape 为 `invoke(request, signal?)`，result 使用 `CONTINUE` / `INTERRUPT` discriminated union，`CONTINUE` 携带 effective boundary，`INTERRUPT` 携带 safe `DENY` / `BLOCK` / `PEND` control metadata；request 必须携带 stage、accepted run coordinates、trusted owner scope reference、stage occurrence coordinate 和 immutable-by-contract stage boundary，且不得携带 `AgentRunStatePort`、runtime state writers、provider raw error、路径或 credential，也不得在当前 stage boundary contract 明确允许的字段之外携带 raw prompt、model/tool/attachment 内容；identity 字段保留在 `HookInput` 顶层和 executor-only coordinates，stage boundary 只携带 stage-specific 字段；contract 类型必须把 boundary / nested fields 暴露为 readonly 形态，运行期实现必须避免 owner-owned mutable reference 暴露，但允许 immutable projection、refs/summaries、structural sharing 或 copy-on-write，不要求 full-boundary deep clone；contract 文档必须明确 hook code 对 boundary 字段有进程内读取权限，`HOOK_INVOKED` / safe diagnostics / control signal redaction 只限制观测输出，不限制 enabled hook 读取当前 stage boundary；`HookInput<S>` 必须以 `input.stage` 作为 TypeScript discriminant，并把 `boundary` narrowed 到该 stage 的 boundary type。
  验证：`npm run build`、`npm run test:contract`、`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts`
  来源：spec requirement `Runtime executor is the only authority that interprets outcomes and reduces mutations`；design D5
- [x] 1.3 增加 contract-level validation helper，统一校验 `SYSTEM` hook 必须 `failureMode=FAIL`、binding 不得改写 kind/effects/execution strategy，并将 `disabled=true` 解释为 disabled。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-failure.test.ts tests/agent-kernel/config-assembly.test.ts`
  来源：spec requirement `Hook definitions and Agent assembly activation remain separate and bounded`
- [x] 1.3a 从 `AgentRunStatePort` 的跨 owner public surface 移除或替换 `invokeLifecycleHook?`；若 runtime implementation 内部仍需要 adapter，必须保持在 `agent-runtime` 私有实现内，不得被 `agent-core`、`agent-model`、`agent-context-engine` 作为注入依赖使用；更新 contract tests / grep assertions，证明 stage owner 只依赖 `LifecycleHookInvocationPort`。
  验证：`npm run build`、`npm run test:contract`、`rg "invokeLifecycleHook\\?" packages/agent-contracts/src packages/agent-core/src packages/agent-model/src packages/agent-context-engine/src`
  来源：spec requirement `Lifecycle hooks execute only at runtime-owned lifecycle stages`；design D0/D4
- [x] 1.4 补齐 stage-specific boundary / mutation contract，确保 `PlanningBoundary` / `PlanningMutation`、`ModelInvokeBoundary` / `ModelInvokeMutation`、`ModelResultBoundary` / `ModelResultMutation`、`CapabilityInvokeBoundary` / `CapabilityInvokeMutation`、`CapabilityResultBoundary` / `CapabilityResultMutation`、带 `before` / `after` 受限变体的 `ContextCompactBoundary` / `ContextCompactMutation`、`AgentTerminalBoundary` / `AgentTerminalMutation` 都位于 `agent-contracts/runtime`；导出或等价提供 `HookBoundaryByStage`、`HookMutationByStage`、`HookInput<S>`、`HookResult<S>` stage-indexed typing；每个 mutation 必须是 closed object 并只暴露 spec 中列出的字段，未知字段、JSON Patch、expression DSL、owner/agent override、runtime state mutation 字段必须 fail closed。
  验证：`npm run build`、`npm run test:contract`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`；design D4
- [x] 1.5 增加 developer-facing canonical contract validation，要求产品路径 hook 定义通过 `defineLifecycleHook(...)` 生成满足 `LifecycleHook` interface 的 hook implementation object，并使用 `execute`、可选 startup-only `configure(config)`、`effects`、`outcome` 和 stage-specific mutation；运行期 `HookInput` 不得包含装配期 `config`，并对 unsupported input forms 提供 fail-closed validation；增加 TypeScript contract tests / type fixtures，证明单 stage hook 的 `execute` 只看到该 stage boundary 和 mutation，多 stage hook 可通过 `switch (input.stage)` narrowing 到对应 boundary/mutation，未声明 stage 的 boundary 字段和 mutation 在编译期报错，`BEFORE_REQUEST_ACCEPT` mutation 类型为 `never` 或等价 impossible type。
  验证：`npm run build`、`npm run test:contract`、type-level contract tests / `tsc --noEmit` fixture
  来源：spec requirement `Developer-facing hook contracts use canonical fields`；design D8
- [x] 1.6 扩展 `agent-contracts/agent-assembly`、`agent-app` AgentDefinition parser 和 AgentAssembly compiler，支持 `agent.yaml.hooks` 编译为 `AgentAssembly.hooks`；hook entry 不携带独立 `bindingId`、`agentId`、`agentVersion` 或 `agentAssemblyRef`，只包含 `hookId`、`enabled?`、`disabled?`、`stages?`、`order?`、`timeoutMs?`、`config?`；`CUSTOM` hook entry 的 `order` 仅接受 `priority?` / `before?` / `after?` 对象，省略时保持启用声明顺序；`SYSTEM` hook entry 不得覆盖 order；拒绝裸数字 order、枚举 order slot、未知目标、跨 kind 目标、目标不在同一 effective stage、循环依赖、超过 `maxHooksPerStage` 等 unsupported override field / effective hook set。
  验证：`npm run build`、`npm run test:contract`、`npm test -- tests/agent-kernel/config-assembly.test.ts`
  来源：spec requirement `Hook definitions and Agent assembly activation remain separate and bounded`；design D6a

## 2. App Startup Hook Registry

- [x] 2.0 移除旧 hook directory product path：删除 `packages/agent-app/src/composition/hook-directory-loader.ts` 或等价目录加载实现；删除 `create-app.ts` 中 `loadHookDirectoryForSystemConfig` import/use；删除 `testing.ts` 对 `loadHookDirectoryForSystemConfig` / `HookDirectoryLoadResult` 的 export；删除 `paths.ts`、system config schema/defaults 和相关 tests 中的 `hooksRoot` 作为 hook source root 派生；更新 `configuration-boundary.md`，不得再把 `hooksRoot` 描述为 trusted product hook source root。
  验证：`npm run build`、`npm run test:contract`、`npm run lint:architecture`、`rg "hook-directory-loader|loadHookDirectoryForSystemConfig|HookDirectoryLoadResult|hooksRoot|configRoot/hooks|hook\\.json" packages/agent-app packages/agent-contracts tests openspec/designs/architecture/configuration-boundary.md`
  来源：design D-1/D6；spec requirement `Startup composition uses hook object metadata as the semantic authority`
- [x] 2.1 更新 `agent-app` startup hook registry，使 app composition 接收显式 `LifecycleHook` implementation objects；system hooks 和后续 plugin composition 已装配 hooks 都必须以该 object 形态进入 registry；本 change 不新增 hook 目录配置、manifest 或目录扫描加载路径；`agent-app` 必须把 `agent-runtime` hook executor adapter 作为 `LifecycleHookInvocationPort` 注入给 `agent-core`、`agent-model` 和 `agent-context-engine`，其中 `createDefaultContextEngine(...)` / `DefaultContextEngineDependencies` 或等价 context-engine factory dependency 必须有显式 hook invocation port 入参；stage owner 不得自行构造 executor、不得从 global runtime singleton 获取 executor。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/config-assembly.test.ts`
  来源：spec requirement `Startup composition uses hook object metadata as the semantic authority`
- [x] 2.1a 注册推荐首个内置 `SYSTEM` hook `system.output-redaction-guard`，使用 `defineLifecycleHook(...)` 或等价 canonical hook object 声明 `kind=SYSTEM`、`supportedStages=["BEFORE_AGENT_TERMINAL"]`、`effects=["TRANSFORM","CONTROL"]`、`failureMode=FAIL` 和显式 system order；提供 `configSchema` 和 `configure(config)` 以支持 per-Agent 自定义额外 pattern、分类阈值、redaction token 或 block policy，并证明 config 只在 startup / AgentAssembly materialization 阶段校验和闭包为当前 AgentAssembly 隔离的 configured executable，运行期 `HookInput` 不携带 config；证明该 hook 无 Agent entry 时默认进入 effective hook set，当前 Agent 可按 system hook disable 规则显式关闭，且 Agent entry 不得覆盖其 kind、effects、failure mode 或 system order。
  验证：`npm test -- tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts`
  来源：spec scenario `System output redaction guard protects the final client-visible content`；design D6b
- [x] 2.2 补齐 startup hook registry negative tests：hook object 未知 stage、缺失 effects、空 effects、重复 effects、非法 effects、重复 hookId、`SYSTEM + CONTINUE`、`SYSTEM` 缺失 `order.priority`、目录/manifest/config-driven hook semantics、非 canonical hook object 都必须启动失败且不得部分加载。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-failure.test.ts tests/agent-kernel/config-assembly.test.ts`
  来源：spec requirement `Startup composition uses hook object metadata as the semantic authority`；design D6
- [x] 2.3 更新 startup composition characterization，证明 `BEFORE_CONTEXT_COMPACT` 和 `AFTER_CONTEXT_COMPACT` 可由 startup registry 中的 `LifecycleHook` object 声明；`BEFORE_CONTEXT_COMPACT` 只在 agent-context-engine 准备执行真实 compaction 时触发，`AFTER_CONTEXT_COMPACT` 只在 summary draft 生成并通过基础校验后、`commitCompaction` 持久化之前触发，skipped/no-op context path 不触发 after hook。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Lifecycle hooks execute only at runtime-owned lifecycle stages`
- [x] 2.4 更新 startup composition validation，拒绝非 canonical execution strategy 字段，证明执行策略只由 `effects` 派生：observe-only 进入 observe group，包含 `TRANSFORM` 或 `CONTROL` 进入 serial impact group。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Developer-facing hook contracts use canonical fields`；design D8
- [x] 2.5 更新 startup hook registry / Agent assembly materialization，证明开发者可用 `defineLifecycleHook(...)` 生成的单个 `LifecycleHook` implementation object 提供 identity/effects/可选 `configure`/`execute`，registry 从该对象生成 runtime definition、configured executable 和 code registration；`agent.yaml.hooks` 编译进 `AgentAssembly.hooks`，表达启用/关闭/收窄/relative order/timeout/config；`config` 只在启动期经 `configSchema` 校验并传给 `configure(config)`，不得进入运行期 `HookInput`；相同 `hookId` 在 Agent A/B 使用不同 config 时必须按 AgentAssembly 生成隔离的 configured executable，accepted run 只能执行自身 frozen `agentAssemblyRef` 对应 executable；缺失 hook definition、缺失 code registration 或 config schema 校验失败必须在发布 `AgentAssembly` 前 fail closed；`CUSTOM` hook 只有当前 accepted AgentAssembly 显式 enabled entry 时生效；`SYSTEM` hook 无 Agent entry 也默认生效，且 `enabled=false` 或 `disabled=true` 可按当前 Agent 关闭该 system hook；`enabled` 与 `disabled` 冲突必须 assembly compile 失败；任一 stage 的 effective hook 总数超过 `maxHooksPerStage` 必须 assembly compile 失败，runtime 不截断。
  验证：`npm test -- tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Hook definitions and Agent assembly activation remain separate and bounded`；design D0

## 3. Runtime Hook Executor Semantics

- [x] 3.0 更新 effective hook resolver，使其只按 accepted run 固化的 `agentId`、`agentVersion`、`agentAssemblyRef` 和 frozen `AgentAssembly.hooks` 解析 hook activation，并合并默认生效的 system hook definitions；当前 Agent 显式 disable 的 system hook 不得进入 effective hook set。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Hook definitions and Agent assembly activation remain separate and bounded`；design D0
- [x] 3.1 重构 runtime hook executor，按 effects 分为 observe-only observation group 和 serial impact group，并使用 startup-materialized `AgentHookSnapshot` 查找 effective hooks；observe-only invocation 必须获得可用于外部观察/通知副作用幂等写入的 stable idempotency key；executor 必须从 stage owner 提供的 replay-stable `stageOccurrenceKey` 和 `hookId` 生成 `stageOccurrenceKey + ":" + hookId`，并区分每次 hook execution attempt 的 `hookInvocationId` 与可跨 recovery retry 稳定的 idempotency key。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts`
  来源：spec requirement `Hook effects define side effects and execution strategy`；design D2
- [x] 3.2 实现 observe-only hook 并行有界执行：stage entry boundary、per-hook timeout、observe group timeout、all-settled evidence、stage 返回前 settle-or-timeout、失败不改变 request truth；补充 observe side-effect characterization，证明外部审计/通知/trace/诊断采样/分析索引/缓存类副作用只能在 hook timeout 内执行，失败或 unsafe error 只产生观测降级，不能改变 protected operation、effective boundary、pending input、terminal commit、checkpoint、canonical timeline 或 runtime-owned truth；证明同一 stage occurrence 恢复重试 idempotency key 稳定，合法新 occurrence idempotency key 区分；测试必须覆盖 `BEFORE_PLANNING` round N recovery retry key 相同、round N+1 key 不同、同一 occurrence 的不同 configured hook key 不同、`BEFORE_REQUEST_ACCEPT` 在无 `requestRunId` 时使用 pre-acceptance submit / acceptance coordinate，以及 stage owner 缺失 replay-stable occurrence / operation coordinate 时 fail closed before executing observe side-effect-capable hooks。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Observe hooks execute in bounded parallel without owning request truth`；design D3
- [x] 3.3 保持包含 `TRANSFORM` 或 `CONTROL` effect 的 hook 串行稳定顺序：`SYSTEM` group 永远先于 `CUSTOM` group；system group 按框架内置 hook definition 的显式 order 执行且 system hook 缺失 order 必须 fail closed；custom group 构建同 stage impact hook graph，把 `order.before` / `order.after` 作为约束，并用 `(priority if present else declarationOrdinal, declarationOrdinal, hookId)` 作为 stable topological sort comparator；不得接受裸数字 order、枚举 slot、跨 kind 目标、未知目标、非同 stage 目标、循环依赖或矛盾约束。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts`
  来源：spec requirement `Impact hooks use a stable synchronous execution order`
- [x] 3.4 实现 `PASS` / `SKIP` / `DENY` / `BLOCK` / `PEND` outcome 解释，并把 `DENY` / `BLOCK` / `PEND` 记录到 timeline-only `HOOK_INVOKED` evidence；runtime hook executor 在 `DENY` / `BLOCK` / `PEND` 时必须通过 `LifecycleHookInvocationPort` 返回共享 lifecycle hook control-interruption signal，stage owner 只能传播该 signal，不能把它转换成 owner-local business result。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Runtime executor is the only authority that interprets outcomes and reduces mutations`；design D5
- [x] 3.5 增加 direct composition negative tests，证明 `SYSTEM` hook 使用非 `FAIL` failure mode 会 fail closed。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Hook failure handling is explicit and bounded by failure mode`
- [x] 3.6 保持 `PEND` 只允许 `BEFORE_MODEL_INVOKE`、`BEFORE_CAPABILITY_INVOKE` 和 `BEFORE_AGENT_TERMINAL`，并拒绝 `BEFORE_REQUEST_ACCEPT`、`BEFORE_PLANNING`、`AFTER_MODEL_RESULT`、`AFTER_CAPABILITY_RESULT`、`BEFORE_CONTEXT_COMPACT` 和 `AFTER_CONTEXT_COMPACT` 的 `PEND`；证明 runtime executor 在返回 `PEND` interruption 前已经创建 runtime-owned pending input 和 durable resume coordinates，pending 创建失败时 fail closed 且不得返回无 pending truth 的 `PEND` signal；证明 answered pending 继续使用 frozen hook snapshot 恢复同一 run，并证明 `BEFORE_PLANNING` pending 在 planning pause/resume 语义被后续 change 定义前保持 fail-closed。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-pending.test.ts tests/agent-kernel/local-runtime-recovery.test.ts`
  来源：spec requirement `Runtime executor is the only authority that interprets outcomes and reduces mutations`
- [x] 3.7 实现 result validation：hook result 必须使用 canonical `outcome`，unsupported input forms fail closed 且产生 safe diagnostic。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Developer-facing hook contracts use canonical fields`；design D8
- [x] 3.8 明确并实现 recovery 时 impact hook 重执行语义：runtime 必须从保存的 recoverable lifecycle coordinate 重新接入，恢复坐标之前已经完成的 stage 通过 checkpoint / lifecycle truth 保留效果但不回放；恢复落点的 protected operation 尚未完成时，该 stage 的 enabled hooks 使用 frozen hook snapshot 重新执行；`TRANSFORM` / `CONTROL` hook result 从恢复后的 stage boundary 重新计算，runtime 不缓存或重放之前返回的 mutation/control output，也不为 impact hook result 提供 observe side-effect idempotency key；测试必须覆盖 planning round 2 recovery 不重放 round 1 `BEFORE_PLANNING`、恢复落在 round 1 `BEFORE_MODEL_INVOKE` 时重新执行该 stage hook、以及依赖外部读取的 impact hook 由 hook 作者负责确定性/幂等语义。
  验证：`npm test -- tests/agent-kernel/local-runtime-recovery.test.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts`
  来源：spec requirement `Lifecycle hooks execute only at runtime-owned lifecycle stages`；design D3

## 4. Stage Mutation Application

- [x] 4.1 实现 stage/effects/mutation kind 和字段级校验，非法 stage mutation、未知 mutation 字段、越界字段值、observe-only mutation、无 `TRANSFORM` effect mutation、无 `CONTROL` effect 控制结果、`CONTROL` outcome + mutation 同时返回都必须按规格处理；runtime executor 提供单一通用 `reduceBoundaryMutation`：mutation 中出现的同名字段完整替换 effective boundary 字段，未出现字段保持不变；stage-specific 代码负责 schema、allowed fields 和字段安全 invariant validation；`HOOK_INVOKED` mutation summary 必须记录 mutation kind、replaced field names 和每个替换字段的 safe size/count/digest metadata，且不得记录字段值；runtime / stage owner 必须在 hook invocation 前执行 boundary finalization，按字段选择 immutable projection、stable ref/digest/count/summary、已不可变值 structural sharing、copy-on-write / lazy projection 等明确运行期策略，TypeScript `readonly` 不得作为唯一保护；boundary 不得暴露 owner-owned mutable reference，并且只对 accepted mutation 中出现的 replacement fields 做 detach/canonical clone、校验和应用；未修改字段允许 structural sharing，不要求因为 hook 运行而全量复制。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-failure.test.ts tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`
- [x] 4.1a 在 `agent-core` agent loop 内接入 `BEFORE_PLANNING` effective boundary mutation，触发位置必须在每个 planning turn 调用模型前，且在请求/技能路由和 routing constraints 已解析、当前 planning-turn 输入已确定之后、context assembly / model request construction 之前；替换当前 `default-agent` 只在 initial round 触发的临时实现，证明多轮 planning 每一轮都会触发；boundary 必须包含 round index、step id、effective `maxRounds` / `maxCalls`，以及进入该 planning turn 前已经累积的 request-local `capabilityGeneratedMessages` / `capabilityContextPatch`，并遵循通用 boundary immutability 规则；boundary 不得包含尚未执行的本轮模型输出、tool calls、capability result 或 context patch；证明 `flowVariables`、`capabilityGeneratedMessages`、`capabilityContextPatch`、`maxRounds`、`maxCalls` 只按同名字段完整替换当前 effective planning-turn boundary，且不得覆盖 request truth、owner scope、agent scope、routing constraints、routing authority、tool result truth 或 provider-level model invocation fields；不得使用 runtime 外层 pre-`agent.execute()` hook 或 `agent-model` 的 `BEFORE_MODEL_INVOKE` provider boundary 作为替代。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`；design D4
- [x] 4.1b 增加跨 stage mutation interaction characterization，证明 runtime 不维护跨 stage effective-boundary cache，也不把前一 stage boundary 复制到后一 stage；`BEFORE_PLANNING` mutation 必须通过 `agent-core` 正常消费后影响 context assembly / model request construction，随后 `BEFORE_MODEL_INVOKE` boundary 从新构造的 model request 生成；`BEFORE_MODEL_INVOKE` mutation 必须通过 `agent-model` 正常 provider invocation 影响 provider output，随后 `AFTER_MODEL_RESULT` boundary 从 normalized provider result 生成；测试必须证明实现不会复用 mutation 前的旧 boundary 快照构造后续 stage。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`；design D4
- [x] 4.1c 增加 boundary immutability / detached mutation negative tests，覆盖至少 model messages、tool descriptors、capability result payload、context patch 或 summary content 中的嵌套数组/对象：hook 对 received boundary 做原地修改不得影响 stage owner 内部对象、effective boundary 或 protected operation input；stage owner 在 hook invocation 后修改自身 mutable source object 不得改变 hook-visible boundary snapshot；hook 返回 mutation replacement object 后继续修改同一对象引用不得影响已归约 effective boundary 或 owner-owned business object；测试应证明 reducer 只需要 detach/canonicalize mutation 中出现的 replacement fields，未修改字段可 structural sharing，不要求 full-boundary deep clone；测试或 dev helper 应覆盖至少一种运行期 finalization 技术（如 freeze / immutable projection / schema projection / structured clone / copy-on-write guard）实际阻断引用副作用。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`；design D4
- [x] 4.2 在 `agent-model` provider invocation boundary 接入 `BEFORE_MODEL_INVOKE` effective boundary mutation，触发位置必须在 concrete `ModelInvocationRequest` 构造完成后、provider SDK 调用前；删除 `agent-runtime` 外层 pre-`agent.execute()` 的旧 `BEFORE_MODEL_INVOKE` 触发点（当前 `packages/agent-runtime/src/lifecycle/submit.ts` 中的 runtime wrapper 调用），并移除/迁移当前 `default-agent` 内围绕模型调用的 core-level 临时触发点，用 negative characterization 证明 runtime 不再在 agent loop 启动前触发该 stage、agent-core 不再拥有 provider boundary 触发；`agent-model` 可通过 `agent-contracts/runtime` 的 hook invocation contract 消费 effective model request / safe provider options 和共享 lifecycle hook control-interruption signal，但不得导入 `agent-runtime` implementation，也不得使用 runtime run state、checkpoint、timeline、terminal、runtime command/session 或 RequestRun state mutation contract；证明 `messages`、`tools`、`commonOptions`、`providerOptions`、`timeoutMs` 只按同名字段完整替换当前 effective `ModelInvocationRequest`，不能覆盖 owner scope、agent scope、provider/model identity、baseUrl 或 credentialRef，且 `tools` 只能由当前 request 已有 descriptors 组成；证明 `messages` boundary 可以被 enabled hook code 在内存中读取并完整替换，但 `HOOK_INVOKED`、mutation summary、control signal、logs、metrics、audit 和 safe diagnostics 不输出 raw messages / raw prompt / full boundary / full mutation；大 messages 边界必须使用 readonly projection / structural sharing 或等价 copy-on-write，未修改 messages 不要求 full clone，accepted replacement message list 必须 detach/canonicalize 后再应用；证明 `BEFORE_MODEL_INVOKE` 返回 `DENY` / `BLOCK` / `PEND` interruption 时 provider SDK 不会被调用，`ModelInvocationService.complete(...)` / `stream(...)` 不新增 `PEND` result variant、不返回 `ModelFinalResult`，而是原样抛出共享 control-interruption signal；测试必须覆盖 agent loop、fallback 或其他非 agent-loop model consumer 中至少两类模型调用路径，确认单个 runtime pre-`agent.execute()` hook 不能替代 per-invocation pre-model hook，且 model caller 不把该 signal 包装成 provider error、model safe error、fallback miss 或普通 exception。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`；design D4
- [x] 4.3 在 `agent-model` provider result boundary 接入 `AFTER_MODEL_RESULT` effective boundary mutation，触发位置必须在 provider result normalization 后、返回 caller 前；移除/迁移当前 `default-agent` 内 provider result 后的 core-level 临时触发点；`agent-model` 可通过 `agent-contracts/runtime` 的 hook invocation contract 消费 effective model result projection，但不得导入 `agent-runtime` implementation，也不得使用 runtime run state、checkpoint、timeline、terminal、runtime command/session 或 RequestRun state mutation contract；证明 `content`、`reasoning`、`toolCalls` 只按同名字段完整替换 downstream `ModelFinalResult` projection，不能新增/改写 tool call、finishReason、usage、provider ids、safeError 或 raw model result evidence，且 agent loop 之外带 accepted run context 的模型调用路径也经过该边界。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`
- [x] 4.4 在 `agent-core` tool loop 接入 `BEFORE_CAPABILITY_INVOKE` effective boundary mutation，不下沉到 `agent-capability`；触发位置必须在 tool call 已解析为 capability id、descriptor / routing constraints / subagent guard 已通过、具体 `CapabilityInvocationRequest` 已构造之后，且在 `CAPABILITY_BEFORE_CALL` checkpoint、`CAPABILITY_STARTED` event 和 `capabilityInvocation.invoke(...)` 之前；证明 `arguments`、`timeoutMs` 只修改当前 effective capability invocation，`arguments` 必须通过 capability input schema，capability id、toolCallId、invocationId、idempotencyKey、trusted coordinates、owner scope、agent scope 和 authorization facts 不可被 hook 覆盖。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`
- [x] 4.5 在 `agent-core` tool loop 接入 `AFTER_CAPABILITY_RESULT` effective boundary mutation，不下沉到 `agent-capability`；触发位置必须在 `capabilityInvocation.invoke(...)` 返回 raw result 且 basic `CapabilityInvocationResult` envelope validation 通过之后，且在 effective result validation、status-specific handling、request-local result effects、`buildModelVisibleCapabilityPayload(...)`、capability result message append 和 capability completion event 之前；证明 `structuredPayload`、`generatedMessages`、`contextPatch` 只按同名字段完整替换 effective `CapabilityInvocationResult` 上的可变字段，tool loop 再基于 effective result 生成 downstream/model-visible capability result message 和 current-run context projection；raw capability result evidence、status、safeError、resultRef、artifactRefs、durable artifacts 和 capability completion truth 保留；证明 invocation throw / invalid result envelope 路径走 safe error path 且不暴露 transformable result boundary。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Stage-specific boundaries and mutations are minimal runtime contracts`
- [x] 4.6 在 `agent-context-engine` 真实 compaction 边界接入 `BEFORE_CONTEXT_COMPACT` / `AFTER_CONTEXT_COMPACT` mutation，并移除/迁移当前 `default-agent.render` 内的 core-level compact hook 临时触发；`agent-context-engine` 可通过 `agent-contracts/runtime` 的 hook invocation contract 消费 effective context compaction boundary，但不得导入 `agent-runtime` implementation，也不得使用 runtime run state、checkpoint、timeline、terminal、runtime command/session 或 RequestRun state mutation contract；`DefaultContextEngineDependencies`（或等价 factory deps）必须新增显式 `LifecycleHookInvocationPort` 入参，`assemble-context` 必须把该 port 通过 explicit options/deps 传给 `runSummaryCompression(...)` / `summary-compression-orchestrator`，orchestrator 不得从 `default-agent`、global singleton 或 `AgentRunStatePort` 获取 hook executor；证明 `ContextCompactMutation.before.targetBudgetUnits` 只能按同名字段完整替换当前 compaction operation 的目标预算，且 summary generation 使用 effective target budget；证明 `ContextCompactMutation.after.content` 只能在 summary draft 生成且通过基础校验后、构造最终 summary `SessionMessage` / `SessionMessageRecord` 并调用 `commitCompaction` 之前按同名字段完整替换 effective `TraceableSummaryDraft.content` / summary message `content`，并且 `commitCompaction` 接收由 mutated draft 构造的 summary message；补齐预算证据、raw sources、source/covered/retained refs、active context version、summary ids、idempotency key、compression evidence、checkpoint/terminal truth 不可被覆盖，以及 skipped/no-op path 不触发 `AFTER_CONTEXT_COMPACT`、post-commit path 不允许再应用 after mutation、当前 checkpoint 后触发位置不能满足目标语义的 negative test。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/main-path.test.ts`
  来源：spec requirement `Lifecycle hooks execute only at runtime-owned lifecycle stages`；design risk skipped/no-op context path
- [x] 4.7 在 `agent-core` agent loop 判断当前 accepted run 可以正常退出、没有来自模型的待执行 tool calls、最终 agent output content 已形成之后接入 `BEFORE_AGENT_TERMINAL` mutation，触发位置必须在当前直接发送 final-content event 的 normal-exit 分支之前，也必须在 agent loop 完成返回之前；证明 `AgentTerminalMutation` 只允许 `finalContent` 和 `toolCalls` 两个字段，`finalContent` 只按同名字段完整替换 effective final content，并重新通过 output limit 与 terminal completeness validation；证明 agent-core 发送的 final-content event 使用 hook-mutated final content，runtime-owned run output 从该 final event 收集并成为 terminal commit input；证明 terminal decision boundary 的初始 `toolCalls` 为空，hook 返回非空 `toolCalls` 时 agent-core 不发送本次 final-content event，而是通过现有 `executeToolCallsInOrder(...)` 执行这些 tool calls 并继续下一轮 planning/model；证明 hook-provided tool calls 必须通过当前 Agent binding、capability descriptor、routing constraints、subagent guard、capability input schema、`maxCalls` 和剩余 round budget 校验，并且继续触发 `BEFORE_CAPABILITY_INVOKE` / `AFTER_CAPABILITY_RESULT`；证明非空 `toolCalls` 与 `finalContent` replacement 同时出现时 fail closed；证明 hook 不得修改 final event type、final-event flags、`stepId`、terminal status、terminal message id、terminal commit metadata、run status、checkpoint truth、raw model/capability evidence、owner scope 或 agent scope；不得为了该 hook 修改 `Agent.execute(...)` 返回契约或让 agent-core 额外返回 effective handoff object；移除 `agent-runtime` terminal commit 路径中的旧 `BEFORE_AGENT_TERMINAL` 调用（当前 `packages/agent-runtime/src/terminal/terminal-commit.ts`）和旧 stage vocabulary；证明 `DENY` / `BLOCK` / `PEND` 会阻止本次 final-content event 发送且 mutation 被忽略；runtime terminal commit、cancel、supersede 或 runtime failure terminal path 不拥有该 hook。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts`
  来源：spec requirement `Runtime executor is the only authority that interprets outcomes and reduces mutations`
- [x] 4.7a 实现并验证 `system.output-redaction-guard` 的 terminal transform/control 行为：在 `BEFORE_AGENT_TERMINAL` 读取 `finalContent`，扫描 credential-like pattern、内部 IP 段、客户标识、手机号和本地/内部路径等敏感信息；内置默认规则和 per-Agent config 规则必须通过 startup `configSchema` 校验并由 `configure(config)` 生成本地只读策略快照，config 只能表达 pattern/policy data，不得表达脚本、表达式 DSL、远程策略 URL、owner/agent scope override、hook outcome 或 mutation payload；可确定安全脱敏时返回完整替换后的 `AgentTerminalMutation.finalContent`，agent-core 最终发送的 final-content event 必须使用脱敏后的 effective content；无法安全脱敏、高风险泄漏或脱敏会破坏输出完整性时返回 `BLOCK`，agent-core 不得发送本次 final-content event；hook 及其 event/log/metric/audit/control signal/diagnostic 不得输出 raw `finalContent`、raw finding、路径、credential、手机号或客户标识，只能输出类别、数量、safe digest 和安全 reason code；实现必须 bounded、deterministic，不在 request path 进行远程网络调用；证明该保护不通过 `AFTER_MODEL_RESULT` 处理中间模型输出实现，也不修改 `Agent.execute(...)` 返回契约。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec scenario `System output redaction guard protects the final client-visible content`；design D6b
- [x] 4.8 建立并满足 stage-effect coverage matrix，覆盖 9 个 lifecycle stage 在真实 stage owner 位置的 `OBSERVE` / `TRANSFORM` / `CONTROL` 行为：每个 stage 至少有一个 observe-only hook 证明触发位置、boundary 可见性和 observe failure / timeout / side-effect failure 不改变主流程；每个有 mutation contract 的 stage 至少有一个 transform 正例证明 mutation 被 owner 消费并影响 protected operation 或 downstream projection，`BEFORE_REQUEST_ACCEPT` 必须有 transform mutation fail-closed 负例；每个 stage 至少有一个 control 正例证明 `DENY` 或 `BLOCK` 阻止该 stage 后续影响型 hook 和 protected operation / downstream consumption；`PEND` 只在 `BEFORE_MODEL_INVOKE`、`BEFORE_CAPABILITY_INVOKE`、`BEFORE_AGENT_TERMINAL` 做正例，其它 stage 做 fail-closed 负例；该矩阵必须显式列出每个 stage 对应的测试文件和 case 名称，作为 task 勾选前的验收证据。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-pending.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts tests/agent-kernel/main-path.test.ts tests/agent-kernel/local-runtime-recovery.test.ts`、code review 检查点：stage-effect coverage matrix 中 9 个 stage 均有 observe / transform / control 覆盖，`PEND` 正负例与 stage 许可一致。
  来源：design verification map；spec requirement `Lifecycle hooks execute only at runtime-owned lifecycle stages`；spec requirement `Hook effects define side effects and execution strategy`

## 5. Observability And Safe Evidence

- [x] 5.1 更新 `HOOK_INVOKED` timeline projection，输出 kind、effects、execution strategy、outcome、ignored control diagnostic、safe mutation summary、status、safe idempotency key，以及 observe side-effect safe diagnostic。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`
  来源：spec requirement `Every hook invocation produces a structured observability fact`
- [x] 5.2 增加 redaction negative tests，证明 `HOOK_INVOKED` / observation payload 不包含 raw prompt、model messages、model output、finalContent、tool args/result、attachment content、credential、token、手机号、客户标识、路径、完整 boundary、完整 mutation、observe side-effect payload、external response body 或 external raw error；其中 `BEFORE_MODEL_INVOKE` 测试必须包含可识别的 message content，断言 hook code 可读取 boundary messages，但 event、mutation summary、control signal 和 diagnostics 只输出 safe size/count/digest metadata；`system.output-redaction-guard` 测试必须包含可识别的 final output sensitive content，断言 emitted final-content 使用脱敏内容或被 `BLOCK` 阻断，所有 hook evidence 只输出类别、数量、safe digest 和安全 reason code。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-*.test.ts`
  来源：spec requirement `Every hook invocation produces a structured observability fact`
- [x] 5.3 增加 timeline negative tests，证明 ignored observe control output、observe failure 和 observe side-effect failure 不写 lifecycle-changing evidence；`DENY` / `BLOCK` / `PEND` 等 lifecycle-changing impact outcome 只记录在 `HOOK_INVOKED.outcome`。
  验证：`npm test -- tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts`
  来源：spec requirement `Lifecycle-changing hook outcomes are recorded in HOOK_INVOKED without default client projection`

## 6. Architecture Boundaries And Full Validation

- [x] 6.1 增加或更新 architecture/source-level assertions，证明 `agent-runtime` / `agent-core` / `agent-model` / `agent-context-engine` / `agent-channel-web` 不扫描 hook 目录，不动态 import hook module，不引入 `agent-contracts/hook`，stage mutation contract 只从 `agent-contracts/runtime` 导出；证明 `BEFORE_PLANNING` 不在 runtime 外层 pre-`agent.execute()` 触发，也不由 `agent-model` provider boundary 替代，而是在 agent-core 每个 planning turn 的 context assembly / model request construction 之前触发，当前 `default-agent` initial-round-only 临时触发不作为目标保留；`BEFORE_MODEL_INVOKE` / `AFTER_MODEL_RESULT` 只能在 `agent-model` provider boundary 触发并覆盖所有带 accepted run context 的模型调用路径，当前 `default-agent` core-level model/result 临时触发不作为目标保留；`BEFORE_CONTEXT_COMPACT` / `AFTER_CONTEXT_COMPACT` 由 `agent-context-engine` compaction boundary 触发，after 必须位于 summary draft 生成后、`commitCompaction` 持久化前且不覆盖 skipped/no-op path，当前 `default-agent.render` compact hook 临时触发不作为目标保留；证明 `agent-context-engine` 的 hook invocation port 只能通过 context engine factory deps / summary compression orchestrator deps 注入，不能由 `summary-compression-orchestrator` 导入 `agent-runtime`、访问 global runtime singleton、调用 `AgentRunStatePort` 或依赖 `default-agent` 触发；`BEFORE_AGENT_TERMINAL` 由 `agent-core` 在 agent loop 判断可正常退出、没有来自模型的待执行 tool calls 且最终 content 已形成之后、final-content user-visible event 发送之前触发，且不由 runtime terminal commit 触发；证明该 stage 返回非空 `toolCalls` 时复用 `agent-core` 现有 tool loop 并继续后续 planning/model round，而不是由 runtime terminal commit 或 `agent-capability` 下沉 owner 直接执行；`BEFORE_CAPABILITY_INVOKE` / `AFTER_CAPABILITY_RESULT` 仍由 `agent-core` tool loop 触发且不下沉 `agent-capability`，其中 before capability 必须位于 invoke/checkpoint/start event 之前，after capability 必须位于 raw result basic envelope validation 之后和 effective result downstream consumption 之前；证明 `agent-runtime` 不导入 `agent-model` / `agent-context-engine` / `agent-capability` implementation，`agent-model` 和 `agent-context-engine` 只允许导入 `agent-contracts/runtime` 中的 lifecycle hook stage invocation symbols、`LifecycleHookInvocationPort` / request / result types 和共享 lifecycle hook control-interruption signal，不得导入 `agent-runtime`，不得引用 `AgentRunStatePort`、checkpoint、timeline、terminal、runtime command/session、RequestRun store/state mutation contract；证明 `AgentRunStatePort.invokeLifecycleHook?` 不再是 stage owner 注入面，`ModelFinalResult` / model stream result 不新增 `PEND` variant，`agent-model` 不把 lifecycle hook interruption 映射为 provider/model safe error；更新 dependency-cruiser contract subpath allowlist 和 architecture fixtures，使 `agent-model` / `agent-context-engine` 对 runtime subpath 的访问仅按上述受限 hook contract 成立；本 change 不保留 hook 目录配置或 manifest 产品路径。
  验证：`npm run lint:architecture`、code review 检查点：hook contribution 只来自 startup hook registry input，开发者 hook 发现/装载由后续 plugin composition change 承载
  来源：spec requirement `Hook code execution is app-composed and bounded`；design D4/D6
- [x] 6.2 运行目标测试集，并核对 4.8 stage-effect coverage matrix，覆盖 hook core、pending、failure、terminal、startup registry / config assembly、main path、recovery，以及每个 lifecycle stage 的 observe / transform / control stage-owner integration 行为。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-pending.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts tests/agent-kernel/lifecycle-hook-execution-terminal.test.ts tests/agent-kernel/config-assembly.test.ts tests/agent-kernel/main-path.test.ts tests/agent-kernel/local-runtime-recovery.test.ts tests/agent-kernel/lifecycle-hook-stage-owner-integration.test.ts`
  来源：proposal validation scope；design verification map；task 4.8
- [x] 6.3 运行全局门禁，确认完整 hook 能力不破坏 build、contract、architecture 和 OpenSpec。
  验证：`npm run build`、`npm run test:contract`、`npm run lint:architecture`、`openspec validate --all --strict`
  来源：proposal validation scope
- [x] 6.4 增加 lifecycle hook 开发者文档，创建 `docs/developer/17-lifecycle-hooks.md` 并更新 `docs/developer/README.md` / `docs/index.md` 导航；文档必须面向 hook 作者和 Agent package 作者，覆盖 `defineLifecycleHook(...)`、`effects`、`outcome`、9 个 stage 的 owner / 触发位置、各 stage mutation 字段、`agent.yaml.hooks` 启用 / 禁用 / stage 收窄 / order / timeout / config、`configSchema` + `configure(config)`、`SYSTEM` vs `CUSTOM`、`PEND` stage 限制、boundary 只读与同名字段完整替换规则、`HOOK_INVOKED` / redaction 安全约束、`system.output-redaction-guard` 示例，以及不支持 hook 目录 / manifest / 运行期动态加载的边界；文档不得成为独立 contract 权威，API 精确定义仍以 `agent-contracts/runtime` 和本 change 的 OpenSpec contract 为准。
  验证：`openspec validate complete-ts-lifecycle-hook-capabilities --strict`、code review 检查点：开发者可仅通过该文档写出一个合法 custom hook、配置到 Agent、理解每个 stage 可修改字段和失败语义。
  来源：developer-facing lifecycle hook API；design D0/D4/D6a/D6b/D8

## 7. Cross-Effect-Group Order 约束与 Observe 隔离明确性（补齐）

- [x] 7.0 在 change spec delta 和长期基线 `openspec/specs/lifecycle-hook-execution/spec.md` 中补全归档时丢失的约束：observe-only "MUST NOT participate in mutation or control ordering"、observe "MUST NOT see boundary mutations produced by serial impact group"、order fail-closed 列表（unknown/cross-kind/cross-stage/cycle）、observe order "MAY be recorded for diagnostics"、cross-effect-group order target fail-closed、mutationSummary 只用于 applied mutation。
  验证：`openspec validate --all --strict`（132 passed, 0 failed）
  来源：design D2 cross-effect-group 约束；spec `Impact hooks use a stable synchronous execution order`、`Observe hooks execute in bounded parallel`、`Every hook invocation produces a timeline-only observability fact`
- [x] 7.1 在 assembly compiler `validateHookOrderTargets` 中增加 cross-effect-group order target 校验，并统一 order target 错误为 `AgentError`；在 runtime `materializeLifecycleHookStageExecutions` 中增加 `assertNoCrossEffectGroupOrderTargets` defense-in-depth 校验；把 observe-only custom hook 从 `sortCustomLifecycleHookExecutions` 排除，避免虚假拓扑边污染 impact 排序；把 `addEdge` 的静默忽略改为 fail closed。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`（13 passed）
  来源：spec `Impact hooks use a stable synchronous execution order`；design D2
- [x] 7.2 修复 observe-only hook 返回 mutation 时 `HOOK_INVOKED` 多记 mutationSummary 的问题：`invokeSingle` 成功分支对 observe-only hook 不传 mutation 给 `emitHookInvoked`，只保留 `OBSERVE_CONTROL_IGNORED` diagnostic code。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts -t "does not record mutationSummary"`（1 passed）
  来源：spec `Every hook invocation produces a timeline-only observability fact`；spec scenario `Ignored observe control output is observable`
- [x] 7.3 补测试：cross-effect-group order target fail-closed 负例（observe→impact、impact→observe、unknown target）、observe-only 之间 order 允许正例、observe-only mutation 不记 mutationSummary 正例。
  验证：`npx vitest run --config vitest.config.release.ts tests/agent-kernel/lifecycle-hook-execution-core.test.ts tests/agent-kernel/lifecycle-hook-execution-failure.test.ts`（21 passed）
  来源：spec scenario `Cross-effect-group order target fails closed`、`Observe-only order is diagnostic only`、`Ignored observe control output is observable`
- [x] 7.4 补 design.md D2 中 cross-effect-group 约束的设计决策和被排除的依赖模式；新增 ADR `openspec/designs/adr/lifecycle-hook-effect-isolation.md` 记录"effect 决定策略、observe 与 impact 严格隔离"的长期取舍理由。
  验证：code review 检查点：design D2 包含 cross-effect-group fail-closed 约束和 3 类被排除依赖模式的 workaround；ADR 包含 context、decision、rationale、excluded patterns、trade-offs、consequences。
  来源：design D2；ADR 长期取舍记录
- [x] 7.5 同步长期设计文档：`runtime-boundaries.md` 增加 cross-effect-group order fail-closed 约束；`agent-runtime.md` 增加 cross-effect-group 校验和 observe mutation 不记 mutationSummary；`observability.md` 增加 mutationSummary 只用于 applied mutation 的约束。
  验证：code review 检查点：三份设计文档均包含对应约束描述。
  来源：design 文档承载决策
- [x] 7.6 清理 `packages/` `src/` 目录下 tsc 误输出的 `.js`、`.js.map`、`.d.ts` 编译产物（160 + 82 个文件），恢复手写的 `packages/agent-app/src/types/agent-web-hosting.d.ts`。
  验证：`npm run build`（仅剩既有 `picomatch` TS7016 错误，与 lifecycle hook 无关）；`npx vitest run --config vitest.config.release.ts tests/agent-kernel/lifecycle-hook-execution-*.test.ts`（43 passed, 1 flaky pending test 单独运行通过）
  来源：vitest 加载 `.js` 编译产物覆盖 `.ts` 源文件修改的根因修复

- [x] 7.7 对齐 lifecycle-changing hook evidence 目标为 `HOOK_INVOKED`：刷新 active change proposal/design/spec/tasks 和长期 `core-contracts` / run status / trace-log linking 文档，删除“必须写 `HOOK_OUTCOME_APPLIED`”正向目标，只保留“不得 emit separate `HOOK_OUTCOME_APPLIED`”负向约束；长期设计不再暴露旧 `HookInvocationEvent` / `LifecycleHookPort` public surface。
  验证：`rg -g "!tasks.md" "MUST (publish|写入|形成).*HOOK_OUTCOME_APPLIED|保留为 lifecycle-changing|Hook invocation 不默认进入 timeline|interface HookInvocationEvent|interface LifecycleHookPort|HookInvocationEvent\` 是|HOOK_OUTCOME_APPLIED evidence MUST" openspec/changes/complete-ts-lifecycle-hook-capabilities openspec/specs openspec/designs`（0 matches）；`openspec validate complete-ts-lifecycle-hook-capabilities --strict`
  来源：code review 发现；目标语义为 `HOOK_INVOKED`

## 归档前更新基线检查（非实施任务）

实现完成并验证通过后，在归档前根据 proposal/design 的“归档前更新基线”处理：

- 同步 `openspec/specs/lifecycle-hook-execution/spec.md`。
- 同步 `openspec/specs/ts-core-contracts/spec.md`。
- 同步 `openspec/specs/ts-run-status-visibility/spec.md`、`openspec/specs/trace-log-linking/spec.md` 和 `openspec/specs/human-pending-input-core/spec.md` 中的旧 `HOOK_OUTCOME_APPLIED` / `HookResult.decision` / `decision=PEND` 引用到 `HOOK_INVOKED` / `HookOutcome` 目标语义。
- 按需更新 `openspec/overview.md`。
- 更新 `openspec/designs/architecture/core-contracts.md`。
- 更新 `openspec/designs/architecture/configuration-boundary.md`，移除 `hooksRoot` trusted product hook source root 描述。
- 更新 `openspec/designs/architecture/runtime-boundaries.md`。
- 更新 `openspec/designs/architecture/observability.md`。
- 更新 `openspec/designs/modules/agent-runtime.md`。
- 更新 `openspec/designs/modules/agent-core.md`。
- 更新 `openspec/designs/modules/agent-model.md`。
- 更新 `openspec/designs/modules/agent-context-engine.md`。
- 更新 `openspec/designs/modules/agent-app.md`。
- 按需新增或更新 `openspec/designs/adr/<id>.md`。
- 更新 `openspec/designs/spec-to-design-map.md`。
- 检查长期文档没有重复定义同一状态机、API schema、数据 owner 或接口语义。
