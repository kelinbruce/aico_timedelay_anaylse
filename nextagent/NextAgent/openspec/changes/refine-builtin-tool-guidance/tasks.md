## 1. FN-10.4 自定义工具和提示词

- [x] 1.1 先增加统一 Tool guidance contract tests，覆盖已知路径不预搜索、名称与内容搜索分流、已有脚本与 Python snippet 分流、Rag 索引前提、Skill/Agent visibility 和 ToolSearch deferred boundary；验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/builtin-tool-guidance.test.ts packages/agent-context-engine/tests/prompt-shaping.test.ts` 在实现前 5 个目标断言失败。来源：Function `FN-10.4`，Requirement `系统提示词与 Tool descriptor 提供一致的工具调用指导` 全部 Scenarios。
- [x] 1.2 更新 builtin `SYSTEM_PROMPT` 的 `tooling.md`、`workspace.md` 和 `agent-delegation.md`，使共享选择、路径复用、visibility 和 outcome 恢复规则各有唯一承载位置；验证：focused prompt template tests 通过且 section order 不变。来源：design `FN-10.4 工具指导装配/修改方案` 1-3。
- [x] 1.3 更新 `Read`、`Write`、`Edit`、`Glob` 和 `Grep` 的英文 descriptor，使已知路径、名称 pattern、内容 pattern、full-read snapshot、paging、truncation 和 degraded empty result 与当前实现一致；验证：五组 capability tests 和集中式 guidance test 通过。来源：Requirement Scenarios `已知文件路径不触发预搜索`、`未知目标按名称和内容分流`、`Tool 特有恢复语义与实现一致`。
- [x] 1.4 更新 `Bash` 和 `Python` 的英文 descriptor 及必要的 Bash schema field description，使 sandbox authority、命令行适用域、已有脚本、source snippet、non-zero exit、timeout、truncation 和 background notification 与实现一致；验证：`python-capability.test.ts`、集中式 guidance test 及 Bash descriptor/schema 断言通过；既有 Bash background runtime case 与当前 immediate-check 实现不一致，记录在共享门禁结果中。来源：Requirement Scenarios `命令和 Python 执行按输入形态分流`、`outcome 恢复依据结构化事实`。
- [x] 1.5 更新 `Rag`、`Skill`、`Agent` 和 `ToolSearch` 的英文 descriptor，使 governed index、capability visibility、isolated context、deferred discovery 和 actual outcome mapping 与实现一致；验证：四组 focused capability tests 和集中式 guidance test 通过。来源：Requirement Scenarios `知识和能力发现保持治理边界`、`Tool 特有恢复语义与实现一致`。
- [ ] 1.6 运行 Function focused validation：`npx.cmd vitest run --config vitest.config.release.ts <focused files> --testTimeout=30000 --maxWorkers=4`；实际 197 passed、7 skipped、1 个既有 Bash background case failed，未达到全部通过。来源：design `验证策略`。

- [x] 1.7 根据 MiniMax-M2.5 的 316 例 A/B 评测恢复原有 `Rag` descriptor，并移除 `SYSTEM_PROMPT` 中重复的 Rag 专属选择行；修改前正向 Rag 用例一次空结果后停止，修改后重复 Rag，Read/Rag 边界前后均为 12/12。验证：集中式 guidance test 和 Rag capability tests。
- [x] 1.8 增加 #677 capability visibility 证据边界和零工具优先的 contract tests，再更新 `tooling.md` 与 `agent-delegation.md`；不修改 Agent disclosure 渲染代码。验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/builtin-tool-guidance.test.ts packages/agent-context-engine/tests/prompt-shaping.test.ts packages/agent-capability/tests/glob-capability.test.ts --testTimeout=30000 --maxWorkers=4` 通过（43 passed、2 skipped），已断言已披露能力不得由文件工具或 ToolSearch 重新发现，空结果不得跨 authorized execution roots 推断。来源：Scenario `已披露能力使用受治理可见性事实`、`单一覆盖工具优先于重叠调用`。
- [x] 1.9 增加 #680 多扩展名最小调用 contract tests，再更新 Glob descriptor 和 `pattern` 字段说明，披露 `{yaml,yml}` 与 character class 示例。验证：同一 focused 命令通过（43 passed、2 skipped），既有 Glob brace/class 行为测试通过，且未修改 schema shape、授权范围、500 条上限或路径校验。来源：Scenario `Glob 合并同一文件类别的扩展名`。
- [ ] 1.10 对 MiniMax-M2.7 重复执行 #677 与 #680 真实模型回归，记录每次工具调用数和答案正确性；#677 的文件工具与 ToolSearch 调用数为 0，#680 的 Glob 调用数为 1。来源：Scenario `已披露能力使用受治理可见性事实`、`Glob 合并同一文件类别的扩展名`。
- [x] 1.11 增加 #561 后续描述级优化：首个动作按当前目标/最早阻塞点选择，区分调用意图与提及/否定，定义具体已知路径，按可信数据源路由，直接回答已披露能力的可用性问题，约束 Glob 首次调用，禁止把 Python args 文件名当作发现依据，并仅保留实现支持的文件恢复指导；验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/builtin-tool-guidance.test.ts packages/agent-context-engine/tests/prompt-shaping.test.ts --testTimeout=30000 --maxWorkers=4` 通过（29 passed）；扩展运行得到 57 passed、2 skipped、1 个既有 Python context fixture 断言失败，单独复跑稳定复现，且 diff 确认本 task 未修改 executor。未修改 Agent disclosure、Tool schema、executor 或 workspace path resolution。来源：Scenario `具体目标与调用意图共同决定首个动作`、`按可信数据源选择检索边界`、`直接 Python source snippet 使用 Python`、`Tool 特有恢复语义与实现一致`。

## 2. 共享门禁

- [ ] 2.1 运行 `npm run build`、`npm test`、`npm run test:contract` 和 `npm run lint:architecture`，记录实际结果且不得以 focused tests 替代失败门禁。实际 `lint:architecture` 通过（44 files / 263 tests）；`npm test` 1396 passed、1 个既有 per-call Skill trust case failed；`test:contract` 339 passed、6 个既有 workflow/timeline case failed；`typecheck` 被只读 dist EPERM 和既有跨包类型错误阻断，故 build 未达到通过条件。来源：AGENTS 验证门禁。
- [ ] 2.2 运行 `openspec validate --all --strict` 并使用 `nextagent-skill-review` 检视本 change；本 change strict validation 通过，仓库级结果为 254 passed / 5 个无关 active changes failed；语义检视结论 `PASS`，已按检视修正 execution-view-relative path 表述并拆分复合 Scenario。来源：AGENTS 规格优先和 OpenSpec 验证门禁。
