## 前置契约与确认

- [x] 0.1 `refine-ts-session-activity-stream-boundary`：在实现任何Activity port、route或frontend controller前，确认并严格验证Request Execution Stream与Session Activity Projection Stream的冻结边界；完成后本change只能实现该refinement授权的唯一Activity例外。
  来源：proposal「规范上下文」；design「两类流的唯一边界」「契约归属」
  验证：运行`openspec validate refine-ts-session-activity-stream-boundary --strict`，预期change valid；人工检查其`需群内确认`已解决且不存在待确认问题。
  实际（2026-07-28）：refinement已确认且strict valid，`需群内确认`已记录为已确认，Open Questions为无；后续实现只能使用该具名Activity例外。

## FN-1.21 感知跨会话活动

### 1. 公共契约与边界

- [x] 1.1 `agent-contracts/session` 与 `agent-contracts/runtime`：先新增 session activity 判别联合、conditional field、activityId 上限、observedRunId consume guard和 runtime-facing port 的失败 contract tests，再 additive 实现 `SessionActivityStatus`、`SessionActivityEntry`、`SessionActivityMessage`、`SessionActivityPort` 与 `RuntimeSessionActivityPort`；完成后公共类型只有一个 owning surface，既有 `RunStatus`、`PendingInputKind`、`StreamEnvelope`、`StreamEventType`、`agent-contracts/channel` 和 session list entry shape 不变。
  来源：Requirement「会话活动公共数据契约是严格判别联合」「终态活动仅在真实前台查看后匹配消费」；Scenario「Waiting entry 只携带 pending kind」「Terminal entry 携带 opaque activity id」「非法条件字段被拒绝」「旧 terminal presentation不能消费新活动」；design「契约归属」
  验证：先创建 `tests/contract/session-activity-contracts.test.ts` 并运行 `npm test -- tests/contract/session-activity-contracts.test.ts`，预期因新增 exports/schema 尚不存在而失败；实现后运行同一命令与 `npm run test:contract`，预期新增契约和全量 contract tests 通过。
  实际（2026-07-28）：根`npm test`按仓库配置排除`tests/contract/**`，故使用实际门禁`npx vitest run --config vitest.config.contract.ts tests/contract/session-activity-contracts.test.ts`完成红绿验证（实现前1项失败，实现后3项通过）；`npm run test:contract`完成40个文件、324项测试全部通过；`npm run typecheck -- --pretty false`退出码为0。

- [x] 1.2 Architecture boundary：新增negative architecture tests，禁止`agent-channel-web`导入`agent-session`、禁止创建`agent-contracts/web`、禁止Activity进入`agent-contracts/channel`、`StreamEnvelope`或`RuntimeSessionPort.streamEvents(...)`、禁止activity contract/实现依赖gateway-local或frontend source；完成后跨package只能沿session domain port、runtime-facing port和app composition协作。
  来源：Requirement「会话活动公共数据契约是严格判别联合」「会话活动保持 Owner Scope 与 Agent Scope 隔离」；design「契约归属」「可维护性」
  验证：先创建 `tests/architecture/session-activity-boundary.test.ts` 并以受控非法 fixture 或 source assertion 运行 `npm test -- tests/architecture/session-activity-boundary.test.ts`，确认禁止路径会失败；实现后运行同一命令与 `npm run lint:architecture`，预期合法依赖通过且每个非法依赖 case 被实际拒绝。
  实际（2026-07-28）：使用实际architecture配置执行`npx vitest run --config vitest.config.architecture.ts tests/architecture/session-activity-boundary.test.ts`，4项source-boundary断言通过；`npm run lint:architecture`完成dependency-cruiser 1132 modules/5141 dependencies零违规、package manifest policy通过、40个architecture文件243项测试全部通过。

### 2. agent-session 会话活动 owner

- [x] 2.1 `SessionActivityService` 派生器：先用gateway fixtures覆盖五态优先级、四种pending kind、新run覆盖旧未读与旧消费抑制、terminal-pending、旧run迟到、重复terminal activityId、取消和supersede，以及`R1 EXECUTING + R2 ACCEPTED/QUEUED`时以R2为当前有效run并忽略R1旧pending；再实现基于`latestRun + activePendingInput + terminalCommitState`的唯一派生与`SessionActivityService`私有 sourceRunId去重。完成后同一session只产生一个当前状态，重复未消费terminal通知复用activityId，已消费的同一terminal通知保持对外NONE，新run仍可产生新的未读。
  来源：Requirement「会话活动状态具有唯一语义和固定优先级」「会话活动变化只由已提交 canonical facts 触发」；Scenario「Active pending input 覆盖运行态」「Pending input 解决后恢复运行态」「新 run 覆盖旧终态未读」「迟到的旧 run 事件不能覆盖较新 run」「取消和 supersede 不产生失败提醒」「重复 terminal 通知保持同一活动」「已消费的同一 terminal run 不复活」「已消费旧 run 后的新 run仍产生未读」；design「触发与重新派生」
  验证：先创建 `packages/agent-session/tests/session-activity-derivation.test.ts` 并运行 `npm test -- packages/agent-session/tests/session-activity-derivation.test.ts`，预期因 service 未实现而失败；实现后重跑，预期全部状态、优先级、重复和迟到场景通过。
  实际（2026-07-28）：使用仓库实际release测试配置运行`npx vitest run --config vitest.config.release.ts packages/agent-session/tests/session-activity-derivation.test.ts`，16项派生用例通过；activityId改为可注入工厂且同一source run复用既有opaque id，根`npm run typecheck -- --pretty false`退出码为0。

- [x] 2.2 `SessionActivityService` scope bootstrap 与 subscription：先覆盖全 scope分页 bootstrap、terminal不 seed、in-flight/pending恢复、首帧单 snapshot、snapshot/live race、相等状态无 delta、session-keyed coalescing、NONE清除、无 subscriber期间保留与 AbortSignal cleanup，再实现每个 session恰好一个 `PUBLISHED | CONSUMED_TERMINAL` 内部状态的 scope map、bootstrap merge和 AsyncIterable subscriber；完成后 activity范围不依赖 UI分页，消费抑制不进入snapshot/delta，内部状态与subscriber buffer都按实际 session数有界且没有更小的activity quota。
  来源：FN-1.21 + Requirements「首帧稀疏快照与后续 delta 不丢失状态」「每个 app instance 使用一条全 scope 会话活动连接」「会话删除和离线期间遵守显式活动生命周期」「会话活动在重启和依赖失败后安全恢复」「会话活动状态和待发送变化保持有界」；Scenarios「首次连接只发送非 NONE」「状态未变化时不发送 delta」「NONE 只清除已有状态」「Snapshot 与 live 交接期间发生变化」「加载下一页不改变活动订阅范围」「无浏览器期间的终态在同进程内保留」「重启不复活历史终态未读」「重启恢复仍在等待的 session」；design「进程内 owner 与数据结构」「Bootstrap 与 snapshot-to-live 交接」「Subscriber 合并与 NONE 规则」
  验证：先创建 `packages/agent-session/tests/session-activity-stream.test.ts` 并运行 `npm test -- packages/agent-session/tests/session-activity-stream.test.ts`，预期因 bootstrap/stream 未实现而失败；实现后重跑，预期 snapshot、delta、race、coalescing、restart和 cleanup fixtures全部通过。
  实际（2026-07-28）：红测阶段3项基础stream用例均因未实现而失败；实现后补充的“offset分页期间删除导致页边界位移”和“旧subscriber断开与新subscriber注册重叠”两项并发回归也先在旧实现上稳定失败。release配置下9项stream用例全部通过，覆盖101个session的两页bootstrap、目录变化后从offset 0重扫、待注册connection lease、terminal不seed、首帧snapshot、同进程无subscriber保留、delta/NONE、相等状态无delta、快速失效合并、bootstrap失败重连和rederive失败关闭。

- [x] 2.3 `SessionActivityService` consume、delete 与失败隔离：先覆盖 activityId + observedRunId精确消费后转为private `CONSUMED_TERMINAL(sourceRunId)`、同一 terminal重新派生不复活、新 run覆盖消费抑制、stale/repeated/run-mismatch no-op、跨 scope拒绝、durable delete清理公开或消费抑制状态、delete conflict不清除、pending read/rederive/bootstrap失败关闭连接且不发伪空 snapshot，再实现双坐标匹配消费、session scope校验、delete invalidation和 scope resync失败路径；完成后 activity sidecar失败不能改变主 lifecycle/delete结果。
  来源：FN-1.21 + Requirements「会话活动状态具有唯一语义和固定优先级」「终态活动仅在真实前台查看后匹配消费」「会话删除和离线期间遵守显式活动生命周期」「会话活动在重启和依赖失败后安全恢复」「会话活动保持 Owner Scope 与 Agent Scope 隔离」；Scenarios「已消费的同一 terminal run 不复活」「已消费旧 run 后的新 run仍产生未读」「迟到消费不能清除新活动」「旧 terminal presentation不能消费新活动」「删除会话清除活动但不承担目录同步」「Bootstrap 失败不能伪装为空」「跨 scope consume 被拒绝」；design「进程内 owner 与数据结构」「删除与终态消费」「可靠性与恢复」
  验证：先创建 `packages/agent-session/tests/session-activity-consume.test.ts`，并扩展 `packages/agent-session/tests/session-delete-service.test.ts` 后运行 `npm test -- packages/agent-session/tests/session-activity-consume.test.ts packages/agent-session/tests/session-delete-service.test.ts`，预期因 consume/invalidation 未实现而失败；实现后重跑，预期匹配、幂等、scope、delete和失败隔离全部通过。
  实际（2026-07-28）：consume红测3项均失败、delete callback红测1项失败；实现后`npx vitest run --config vitest.config.release.ts`聚焦运行derivation/stream/consume/delete四文件共30项全部通过，覆盖双坐标匹配、stale/run-mismatch no-op、跨scope先拒绝、消费抑制、durable delete成功后清理及callback失败不回滚delete。

### 3. App composition 与 committed fact 接入

- [x] 3.1 `composeSessionServicesLayer` 与 `composeRequestRuntimeLayer`：先补 composition characterization，证明 activity service 单例在 runtime recovery前建立、现有 listener异常不改变 request lifecycle、仅完整 `PERSISTED` 相关 event入队、`LIVE_ONLY`/model delta被忽略、delete callback只在 durable success后执行；再完成 service、runtime-facing adapter、timeline listener与 app close wiring。
  来源：FN-1.21 + Requirements「会话活动变化只由已提交 canonical facts 触发」「会话删除和离线期间遵守显式活动生命周期」；Scenarios「Committed fact 触发单 session 重算」「列表活动不触发状态计算」「删除会话清除活动但不承担目录同步」；design「触发与重新派生」「删除与终态消费」
  验证：先新增 `packages/agent-app/tests/session-activity-composition.test.ts` 并运行 `npm test -- packages/agent-app/tests/session-activity-composition.test.ts packages/agent-app/tests/request-runtime-composition.test.ts`，预期新增行为测试失败且既有 characterization通过；实现后重跑，预期新旧 tests均通过。
  实际（2026-07-28）：composition创建单一service并在runtime/recovery前接入，同一实例用于timeline invalidation、delete callback、runtime-facing adapter和close；真实`dev:watch`浏览器验收发现timeline listener曾提取未绑定的`invalidateSessionActivity` class method，runtime隔离了该listener异常但Activity sidecar未收到任何committed fact。新增service receiver回归后，listener改为接收owner对象并通过对象调用method；旧实现相关11项失败，修复后request-runtime-composition与session-activity-composition共17项通过。9类PERSISTED事件入队、LIVE_ONLY/无关delta/缺scope坐标忽略，可信AgentId由adapter闭包注入。

- [x] 3.2 Request lifecycle non-regression：补 characterization证明 activity observer读取失败、同步 invalidation异常和无 subscriber均不改变 request acceptance、pending input、terminal commit或 session detail stream；完成后观察链只能作为失败隔离的 sidecar。
  来源：FN-1.21 + Requirements「会话活动变化只由已提交 canonical facts 触发」「会话活动在重启和依赖失败后安全恢复」；design「可靠性与恢复」
  验证：先扩展 `packages/agent-runtime/tests/submit-acceptance-order.test.ts`、`packages/agent-runtime/tests/workflow-pending-resume-durable.test.ts` 与 `tests/agent-kernel/terminal-consistency.test.ts`，运行 `npm test -- packages/agent-runtime/tests/submit-acceptance-order.test.ts packages/agent-runtime/tests/workflow-pending-resume-durable.test.ts tests/agent-kernel/terminal-consistency.test.ts`；修改前现有 cases应通过、新 activity failure case应失败；wiring完成后全部通过。
  实际（2026-07-28）：在既有runtime foundation真实submit/detail-stream路径注入首个同步抛错listener并保留后续listener，request仍accepted并terminal、后续listener与detail stream继续收到事件；与四个composition文件合并的release聚焦运行共5文件60项全部通过，activity gateway读取失败另由service stream测试证明只关闭sidecar subscriber。

### 4. ER Web API 与双 transport

- [x] 4.1 `agent-channel-web` activity schema、SSE 与 consume route：先新增严格 schema/route tests，覆盖无 query SSE、第一帧 snapshot、delta/NONE、unknown field拒绝、`activityId + observedRunId` consume body allowlist、204匹配/过期/run-mismatch结果、safe scope错误和现有 session detail SSE并存；再实现 `/api/v1/session-activities/stream` 与 `/api/v1/sessions/:sessionId/activity/consume`。
  来源：Requirement「会话活动公共数据契约是严格判别联合」「每个 app instance 使用一条全 scope 会话活动连接」「首帧稀疏快照与后续 delta 不丢失状态」「终态活动仅在真实前台查看后匹配消费」；Scenario「Activity connection 与 detail stream 并存」「首次连接只发送非 NONE」「迟到消费不能清除新活动」「跨 scope consume 被拒绝」；design「ER Web transport」
  验证：先创建 `packages/agent-channel-web/tests/session-activity-routes.test.ts` 并运行 `npm test -- packages/agent-channel-web/tests/session-activity-routes.test.ts`，预期因 route/schema不存在而失败；实现后与 `npm test -- packages/agent-channel-web/tests/session-activity-routes.test.ts packages/agent-channel-web/tests/web-api-schema-coverage.test.ts` 一起运行，预期 endpoint registry、schema、SSE和consume全部通过。
  实际（2026-07-28）：实现严格 Activity DTO schema、无 query SSE、双坐标 consume 与独立 session detail stream；与 WebSocket、现有 terminal transport、schema inventory、文档、IR negative、app composition 一起使用 release 配置聚焦运行 8 个文件、44 项测试全部通过。

- [x] 4.2 WebSocket upgrade dispatcher：先覆盖 activity WS无 query、snapshot/delta framing、client close/ping、15秒 backpressure safe close、session detail WS不回归和单一 upgrade listener，再把现有 handler重构为 session/activity两个 path分支共享 framing helper；完成后 SSE与WS message语义一致。
  来源：FN-1.21 + Requirements「每个 app instance 使用一条全 scope 会话活动连接」「首帧稀疏快照与后续 delta 不丢失状态」「会话活动在重启和依赖失败后安全恢复」；Scenarios「WebSocket 配置不再建立 activity SSE」「Bootstrap 失败不能伪装为空」；design「ER Web transport」
  验证：先创建 `packages/agent-channel-web/tests/session-activity-websocket.test.ts` 并运行 `npm test -- packages/agent-channel-web/tests/session-activity-websocket.test.ts packages/agent-channel-web/tests/terminal-projection.test.ts`，预期activity path cases失败、既有 session stream cases通过；实现后重跑，预期双 path、framing、close和backpressure cases全部通过。
  实际（2026-07-28）：统一 upgrade dispatcher 只注册一次 listener，并按 detail/activity path 分派共享 framing、ping/pong、client close和backpressure处理；上述 8 文件聚焦门禁共 44 项通过，覆盖 Activity WS 与既有 terminal projection 非回归。

- [x] 4.3 ER/IR registration 与 public API inventory：先扩展IR negative tests、source-compatibility tests和API alignment tests，证明`WebChannelDependencies.sessionActivities`在通用package接口中optional、旧`registerWebChannel(...)`调用方无需Activity mock、`WebChannelRegistrationContext`在进入Web registration precedence前携带必需Activity port、两个builtin Agent Web ER registration函数都注入且缺失时启动失败、`DEFAULT_WEB + custom webChannelRegistration`只调用custom且能取得Activity port、托管Agent Web的custom fixture显式注册三个Activity endpoints、非Agent-Web custom surface无需注册、`registerIrWebChannel(...)`不注入、Activity endpoints只在适用ER注册、IR返回not-found且现有IR六端点不变；再把activity port注入全部builtin ER composition、补齐custom context contract，并更新public endpoint inventory与当前API文档。
  来源：Requirement「每个 app instance 使用一条全 scope 会话活动连接」「会话活动保持 Owner Scope 与 Agent Scope 隔离」；Scenario「Activity connection 与 detail stream 并存」「Snapshot 不泄漏其他 scope」；design「ER Web transport」「迁移与回滚」
  验证：先运行扩展后的 `npm test -- packages/agent-channel-web/tests/ir-surface-routes.test.ts packages/agent-channel-web/tests/web-api-documentation-alignment.test.ts packages/agent-app/tests/session-activity-composition.test.ts`，预期新增ER/IR/API cases失败；实现并更新 `docs/apis/agent-web-api-list.md`、`docs/developer/10-api-reference.md` 后重跑，预期ER端点可见、IR禁止且文档对齐。
  实际（2026-07-28）：Activity port 已进入 builtin/custom ER composition context，IR 仍精确保持 6 个端点且三个 Activity surface 均为 404；API inventory与两份文档已对齐。release 配置聚焦运行 8 个文件、44 项测试全部通过；schema coverage 精确冻结 5 个既有 Cron inventory-only 端点与 2 个既有 staged-upload manual-schema 例外，未宽泛过滤。

### 5. Frontend activity projection 与连接

- [x] 5.1 `sessionActivityStore`、strict parser 与 client：先覆盖 snapshot全量替换、delta按session merge、NONE删除、malformed JSON、snapshot前delta、重复snapshot与未知字段在store mutation前fail closed、stale generation callback忽略、consume去重和不写 local/session storage，再实现独立store、parser、activity service与HTTP consume client。
  来源：Requirement「会话活动公共数据契约是严格判别联合」「首帧稀疏快照与后续 delta 不丢失状态」「终态活动仅在真实前台查看后匹配消费」；Scenario「非法条件字段被拒绝」「非法 activity frame 触发重连」「重连快照替换旧内存状态」「迟到消费不能清除新活动」；design「Frontend 独立 projection」
  验证：先创建 `frontend/agent-web/tests/sessionActivityStore.test.ts` 与 `frontend/agent-web/tests/sessionActivityService.test.ts`，在 `frontend/agent-web` 运行 `npm test -- tests/sessionActivityStore.test.ts tests/sessionActivityService.test.ts`，预期因store/parser/client不存在而失败；实现后重跑，预期替换、merge、validation、generation和storage negative cases全部通过。
  实际（2026-07-28）：新增纯内存 Zustand projection、严格判别联合 parser 与双坐标 consume client；store/service/controller/transport 四个聚焦文件共 38 项测试全部通过，覆盖 generation、snapshot/delta/NONE、未知字段、consume并发去重、已abort同坐标请求不阻塞新attempt及storage negative。

- [x] 5.2 `SessionActivityConnectionController`：先覆盖每个主app instance恰好一条Activity connection、SSE/WS互斥、Request Execution Stream并存、route/list pagination/search/Popover不重连、malformed或协议错误关闭当前generation并无cursor重连、旧generation回调失效、辅助React root不连接，以及旧backend返回404/503/连接失败时非零延迟有界重试且不清空session/detail状态；再复用底层transport primitive，为Activity controller增加可选strict decoder/protocol-error close入口，并从primitive参数移除未使用的必填sessionId约束，不改变既有detail stream行为。
  来源：Requirement「每个 app instance 使用一条全 scope 会话活动连接」「首帧稀疏快照与后续 delta 不丢失状态」；Scenario「Activity connection 与 detail stream 并存」「WebSocket 配置不再建立 activity SSE」「加载下一页不改变活动订阅范围」「两台电脑各自建立连接」「Activity endpoint不可用时有界降级」「非法 activity frame 触发重连」；design「Frontend 独立 projection」
  验证：先创建 `frontend/agent-web/tests/sessionActivityConnection.test.tsx` 并扩展 `frontend/agent-web/tests/stream-transport.test.ts`，运行 `npm test -- tests/sessionActivityConnection.test.tsx tests/stream-transport.test.ts`，预期activity controller与strict close/reconnect cases失败、detail transport baseline通过；实现后重跑，预期connection cardinality、transport selection、protocol failure reconnect、stale generation和detail non-regression全部通过。
  实际（2026-07-28）：Activity controller 使用固定无 cursor 的 SSE/WS 互斥路径、snapshot-first协议、generation失效保护及 500ms 至 5s 有界重试；底层 transport 仅增加可选 strict decoder/protocol close 并保留 detail malformed-frame 语义。四个聚焦文件 37 项测试与前端 TypeScript build 全部通过。

### 6. 会话行呈现与终态查看

- [x] 6.1 `SessionActivityTrailingSlot`与实际列表入口：先新增component tests覆盖WAITING四种locale tag、RUNNING loading、UNREAD_FAILURE红色圆圈感叹号、UNREAD_RESULT蓝点、NONE时间、唯一优先状态、active且surface-visible时本地抑制、active但surface-hidden时恢复marker、固定槽位、hover/focus-within/menu-open稳定More、More button/menu的Enter/Space不触发行级navigation，以及SearchDialog复用相同状态但hover/focus不隐藏状态且不显示More；再实现shared selector/slot、i18n与accessibility，并接入`SessionHistoryEntryRow`、`SessionHistorySearchDialog`、immersive右布局History CardList和collaborative History Popover。
  来源：Requirement「会话列表按用户注意力优先级呈现活动」；Scenario「非当前会话显示最高优先级状态」「当前正在查看的会话只在本地抑制 marker」「Active route被宿主视图覆盖时不抑制marker」「Hover 和键盘 focus 显示同一操作入口」「More键盘操作不打开会话」「四个用户可见列表入口使用同一呈现语义」「SearchDialog hover 不隐藏活动状态」；design「Frontend 独立 projection」
  验证：先创建 `frontend/agent-web/tests/session-activity-row.test.tsx` 并扩展 `frontend/agent-web/tests/sidebar.component.test.tsx` 中的SearchDialog cases，运行 `npm test -- tests/session-activity-row.test.tsx tests/sidebar.component.test.tsx`，预期新增状态与SearchDialog cases失败、既有sidebar cases通过；实现后重跑并追加 `npm test -- tests/i18n.test.ts`，预期视觉语义、keyboard、menu、layout、SearchDialog无More例外与locale key全部通过。
  实际（2026-07-28）：shared trailing slot 与 Sidebar/SearchDialog 已接入五态、四种 pending locale、固定槽位、active-visible本地抑制、hover/focus/menu稳定 More 和键盘隔离；`npm test -- tests/session-activity-row.test.tsx tests/sidebar.component.test.tsx tests/i18n.test.ts` 共 3 文件、51 项全部通过，前端 TypeScript build通过。

- [x] 6.2 shared terminal view observer：先覆盖active route但document hidden、immersive右布局History/Favorites、Memory、custom panel、PIU minimized/hidden/custom panel、conversation loading/failed、旧terminal run不匹配、仅transport terminal frame均不消费，以及conversation加载失败但surface-visible时只本地抑制marker/History聚合且保留activity、切换离开后恢复提醒、cold load成功、live latest terminal成功、返回conversation surface后消费、active前台会话在非底部/anchored/terminal block未进入viewport时仍消费；再在shared`ChatPageCore`接入`isConversationSurfaceVisible`的本地注意力抑制与terminal consume必要条件、terminal presentation runId与activityId in-flight guard，不新增第三个presentation selector、viewport intersection或滚动水位。
  来源：Requirement「终态活动仅在真实前台查看后匹配消费」；Scenario「前台已呈现完成结果后消费」「当前前台会话不因滚动位置保留未读」「Active session 处于后台时不消费」「恢复前台后消费」「旧 terminal presentation不能消费新活动」「会话内容加载失败不消费」「从 A 切到 B 后 A 的新 run完成仍显示未读」；design「Conversation surface 注意力与终态查看判断」
  验证：先创建 `frontend/agent-web/tests/session-activity-consumption.test.tsx` 并运行 `npm test -- tests/session-activity-consumption.test.tsx tests/chat-page.route-state.test.tsx`，预期新增consume evidence cases失败、既有route cases通过；实现后重跑，预期active/visible/latest-terminal双坐标guard成立，scroll/anchored不构成额外gate且route non-regression全部通过。
  实际（2026-07-28）：唯一 terminal observer 位于 shared `ChatPageCore`，只在 active、conversation ready、宿主会话面可见、document visible 且最新 presented block 为 COMPLETED/FAILED 时提交 activityId + observedRunId，不做本地清除或 viewport/scroll gate；最终review补充“in-flight consume期间快速离面再返回”红测，旧实现只调用一次且不再重试，改为attempt identity guard并让已abort请求不参与同坐标去重后通过。核心消费与client聚焦2文件23项通过，hidden、loading/failed、surface hidden、非terminal、双坐标和快速离面恢复路径均有覆盖。

- [x] 6.3 三宿主shell与collaborative History聚合入口：先扩展local、immersive、PIU contract tests，证明每个主shell只挂载一个shared activity controller、辅助knowledge root不挂载、四个用户可见列表入口只复用同一trailing slot，而terminal consume observer仅由shared `ChatPageCore`承载；再覆盖History trigger在任一不处于当前查看surface的session为非NONE时显示locale-backed蓝点、打开Popover不清除、active-visible session被本地排除、active-surface-hidden session仍参与聚合，以及conversation加载失败时本地排除不消费、离开surface后重新参与聚合，最后完成shell wiring。
  来源：Requirement「每个 app instance 使用一条全 scope 会话活动连接」「会话列表按用户注意力优先级呈现活动」「Collaborative History入口聚合不在当前查看表面的会话活动」；Scenario「两台电脑各自建立连接」「四个用户可见列表入口使用同一呈现语义」「收起的History入口提示后台会话活动」「打开History不清除聚合dot」「当前正在查看的session不产生入口dot」「Active session表面不可见时仍产生入口dot」；design「Frontend 独立 projection」「Conversation surface 注意力与终态查看判断」
  验证：先扩展 `frontend/agent-web/tests/app-entry-shell.test.ts`、`frontend/agent-web/tests/immersive-entry.test.tsx` 与 `frontend/agent-web/tests/piu-runtime-contract.test.tsx`，运行 `npm test -- tests/app-entry-shell.test.ts tests/immersive-entry.test.tsx tests/piu-runtime-contract.test.tsx`，预期新增controller/visibility cases失败；实现后重跑，预期三宿主和辅助root边界全部通过。
  实际（2026-07-28）：local、immersive、PIU 主 shell 各挂载一个 controller，shared/knowledge 辅助 root 不挂载；local cron、immersive左布局cron/memory/custom、immersive右布局history/favorites/memory/custom、PIU hidden/minimized/custom 均不算可见会话面。沉浸式左布局使用route + content view + custom panel共同计算`isConversationSurfaceVisible`，避免active route在cron或custom panel下错误抑制marker。PIU History 以 locale-backed 蓝点聚合后台 activity，打开 Popover 不清除。

- [x] 6.4 Sidebar intrinsic trailing：先新增布局回归测试，证明local/immersive左侧Sidebar不再固定预留140px，也不使用绝对覆盖或渐变；时间、四态活动与hover/focus/menu More按当前内容实际宽度参与布局并保持右对齐，标题使用剩余宽度单行硬裁切且不显示省略号；再为`SessionHistoryEntryRow`使用显式Sidebar intrinsic mode，并保留SearchDialog、immersive右布局CardList和collaborative History Popover的既有壳层。
  来源：Requirement「会话列表按用户注意力优先级呈现活动」；Scenario「Sidebar右侧提示按实际宽度占位并硬裁切标题」「Hover 和键盘 focus 显示同一操作入口」「SearchDialog hover 不隐藏活动状态」；design「Frontend 独立 projection」
  验证：先扩展 `frontend/agent-web/tests/session-activity-row.test.tsx` 与 `frontend/agent-web/tests/e2e/session-activity-awareness.spec.cjs`，运行聚焦component test，预期intrinsic layout、无overlay/gradient和无ellipsis cases在实现前失败；实现后重跑component、Sidebar/SearchDialog与Activity Playwright旅程，预期trailing按实际宽度右对齐、标题硬裁切无省略号、状态/操作切换不改变行宽且SearchDialog固定槽位不回归。
  实际（2026-07-29）：聚焦component test先以`expected '' to be 'hidden'`失败，证明旧overlay实现未满足硬裁切契约；实现后`npm test -- tests/session-activity-row.test.tsx`为12/12、`npm test -- tests/sidebar.component.test.tsx`为37/37、`npm run test:e2e:smoke`为Chromium 19/19。Playwright直接断言Sidebar无overlay、标题与实际slot宽度加8px gap恰好占满行内宽度、slot右边距为7-9px、标题computed style为`overflow:hidden`、`white-space:nowrap`、`text-overflow:clip`且无`ant-typography-ellipsis`。`npm test`全量、`npm run build`与`npm run build:vite:modes`均以exit 0完成；SearchDialog继续使用默认`RESERVED`模式，immersive右布局CardList与collaborative History Popover未改变。

## 整体验证

- [x] 7.1 Activity用户旅程：建立可重复验收矩阵。Playwright覆盖local Sidebar/SearchDialog四态、active-visible抑制与离开surface后恢复、双坐标终态消费、两个独立browser page/app instance经后续scope snapshot同步清除、immersive右布局History CardList，以及immersive左布局cron覆盖时marker恢复。组件与协议测试覆盖conversation加载失败、document background、非底部终态、collaborative History聚合与打开不清除、PIU hidden/minimized/custom、A旧run已查看后新run在后台完成、列表分页不重连，以及SSE/WS互斥和每个app instance单connection。
  来源：FN-1.21 + Requirements「每个 app instance 使用一条全 scope 会话活动连接」「终态活动仅在真实前台查看后匹配消费」「会话列表按用户注意力优先级呈现活动」「Collaborative History入口聚合不在当前查看表面的会话活动」「会话删除和离线期间遵守显式活动生命周期」「会话活动在重启和依赖失败后安全恢复」「会话活动状态和待发送变化保持有界」；Scenario「Activity connection 与 detail stream 并存」「加载下一页不改变活动订阅范围」「两台电脑各自建立连接」「前台已呈现完成结果后消费」「当前前台会话不因滚动位置保留未读」「Active session 处于后台时不消费」「会话内容加载失败不消费」「从 A 切到 B 后 A 的新 run完成仍显示未读」「当前正在查看的会话只在本地抑制 marker」「打开History不清除聚合dot」；design「验证策略」
  验证：先创建 `frontend/agent-web/tests/e2e/session-activity-awareness.spec.cjs`，在 `frontend/agent-web` 运行 `npx playwright test tests/e2e/session-activity-awareness.spec.cjs --config=playwright.config.cjs`，预期目标旅程在实现前失败；实现与fixture完成后重跑，预期所有用户旅程通过且连接数量断言成立。
  实际（2026-07-28）：4条fixture驱动真实 Chromium 旅程覆盖local Sidebar/SearchDialog 四态、current-visible抑制、cron离面恢复与双坐标终态消费；两个独立浏览器页面/app instance在下一scope snapshot同步清除；immersive右布局History CardList复用状态且打开不消费、返回conversation后消费；immersive左布局在active session路由切到cron后恢复marker。修复composition receiver后，复用真实`npm run dev:watch`服务执行无API fixture浏览器验收：提交请求形成active run，后台session显示`WAITING_FOR_INPUT/等待回答`，重新打开active conversation时本地抑制marker，切到`#/cron-tasks`后同一active route恢复marker，回答pending input后主request继续执行。PIU History聚合、document/host visibility、loading/failed、non-bottom、SSE/WS互斥与单controller等宿主/协议组合由同一change的组件/集成测试补足。

- [x] 7.2 全量质量门禁：完成backend/frontend build、focused/full tests、contract、architecture、三宿主artifact和OpenSpec strict验证，并确认没有新增表、migration、gateway Record、feedSequence、revision、独立terminal waterline、activity-specific quota、frontend持久化、IR Activity route、第三类私有stream family或Activity对`StreamEnvelope`/`RuntimeSessionPort.streamEvents(...)`的依赖。
  来源：design「质量属性影响」「验证策略」「迁移与回滚」
  验证：在仓库根目录运行 `npm run build`、`npm test`、`npm run test:contract`、`npm run lint:architecture`、`openspec validate --all --strict`；在 `frontend/agent-web` 运行 `npm run build`、`npm test`、`npm run build:vite:modes` 与 `npx playwright test tests/e2e/session-activity-awareness.spec.cjs --config=playwright.config.cjs`。全部命令必须退出码为0；code review必须确认禁止项不存在并把实际命令、结果和任何保留风险记录到task验收证据。
  实际（2026-07-28）：根目录 `npm run build` 退出码0；`npm test` 113文件/1024项通过；`npm run test:contract` 40文件/324项通过；`npm run lint:architecture` 对1141 modules/5184 dependencies零违规，architecture 40文件/243项通过；`openspec validate --all --strict` 251项通过、0失败。`frontend/agent-web` 的 `npm run build` 与 `npm test` 均退出码0（138文件/1585项），`npm run build:vite:modes` 完成local/immersive与PIU产物，Activity Playwright复用真实dev-watch 5173服务后4项Chromium旅程全部通过。最终新增聚焦回归覆盖bootstrap分页目录变化重扫、连接注册期间scope租约、真实composition receiver到public Activity delta、immersive左布局离开conversation surface后marker恢复，以及terminal consume在in-flight期间快速离面/恢复后的新attempt；后端Activity聚焦33项、app composition聚焦17项、前端消费/client聚焦23项全部通过。独立review发现并修复timeline listener把class method作为裸函数传递导致Activity receiver静默失效的问题，随后真实浏览器验证从`REQUEST_ACCEPTED`到`WAITING_FOR_INPUT`、跨session marker、本地抑制、离面恢复和pending input继续执行均通过。禁止范围审计确认Activity未进入`agent-contracts/channel`、`agent-runtime` detail stream、gateway/local persistence，Activity实现未出现storage、table/migration、feedSequence、revision或quota关键词，IR negative tests保持通过。保留项只有既有Fastify/React test warning、Ant Design `use client`与bundle-size warning，不影响退出码或本change语义；验证时另发现当前dev profile访问`/api/v1/cron-tasks`返回404，该问题不影响本change的activity marker验证，按独立cron管理能力的composition/route启用问题跟进。
