## REMOVED Requirements

### Requirement: Cron task dashboard lists manageable tasks

**Reason**：本 change 修改该 Requirement 的页面标题；该 Requirement 位于 `FN-10.9 Cron 工具` 的 legacy spec，按照触及即迁移规则迁往该 Function 的 canonical spec。

**Migration**：完整目标态由同一 change 的 `cron-task-management-api` delta 中同名 ADDED Requirement 承载；`agent-web-cron-task-dashboard` 中其他未触及 Requirements 原位保留。
