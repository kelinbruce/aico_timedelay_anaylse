/**
 * Design-only test-first skeleton. This directory is not discovered by TestClaw.
 * Rationale: a real candidate process must prove HTTP/SSE acceptance, one terminal
 * result and history consistency; Fastify injection is insufficient.
 */
import { describe, it } from 'vitest';

describe('TC-SI-115 remote deployment mainline', () => {
  it('runs session create, submit, SSE terminal and history through the candidate process', async () => {
    throw new Error('Design skeleton: move into tests/suites and implement TC-SI-115');
  });
});
