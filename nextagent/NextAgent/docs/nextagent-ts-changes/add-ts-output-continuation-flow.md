# add-ts-output-continuation-flow

规划入口：[roadmap-v2](../nextagent-ts-change-roadmap-v2.md)
所属分组：明确不规划 / Output continuation

状态：not-planned
类型：明确不规划 change
主要 owner：无
依赖：无

目标：
- 不纳入首版本，后续版本也不规划自动继续生成能力；不创建实施型 OpenSpec change，本文件仅作为 `not-planned` 记录。首版只要求输出长度受限或可能截断时记录并提示用户，不得静默截断。

后续维护：
- 本文件承载该 change 的详细规格输入、契约输入、实现约束、非目标、验收要点和并行边界。
- 如果本 change 需要修改已冻结核心契约，必须先提出 contract refinement change。
