## 0. 前置确认

- [x] 0.1 确认依赖 change 的最小 contract 已进入 baseline 或 archive 且顺序满足：`agent-package-assembly`、`lifecycle-hook-execution`、`risk-policy-enforcement`、`capability-catalog` / `ts-core-contracts`、`app-config-schema`。`complete-ts-lifecycle-hook-capabilities` 必须已归档，本 change 消费当前 `LifecycleHook` / `AgentAssembly.hooks` 主路径。
  验证：`openspec validate agent-package-assembly --strict`; `openspec validate lifecycle-hook-execution --strict`; `openspec validate risk-policy-enforcement --strict`; `openspec validate capability-catalog --strict`; code review 确认现有 risk policy governance 不被插件 Tool 绕过，`agent-core` routing policy boundary、`AgentRoutingDecision`、`RoutingConstraints`、`LifecycleHook` / `defineLifecycleHook(...)`、`AgentAssembly.hooks`、Tool SPI 可被本 change 消费，且 active changes 中不存在旧 hook directory / binding 入口。
  来源：proposal 依赖；design Context；Requirement "Policy plugins use an explicit open policy inventory"

## 1. Plugin SDK 和配置契约

- [x] 1.1 新增 `@nextagent/agent-plugin-sdk` workspace package，导出 `definePlugin`、`definePluginFactory`、`defineCapabilityProvider`、`defineTool`、`defineToolProvider`、`defineAgentRoutingPolicy`、`defineLifecycleHook`、`getPluginMetadata`、插件 provider/policy/hook 类型、开放 policy inventory metadata、host external id/type/inventory 和必要 public contract type re-export；SDK scope 聚焦 authoring helper、test helper 和 public contract type surface；`defineCapabilityProvider` 面向 `agent-contracts/capability` public provider SPI，允许插件贡献自定义 discovery/executor；`defineTool` 接收 `DefineToolInput` 并返回 `ToolDefinition`；`defineToolProvider` 接收 `DefineToolProviderInput`，把 `ToolDefinition[]` 包装成标准 Tool provider 的糖；`defineAgentRoutingPolicy` 接收并返回本 change 冻结的 `AgentRoutingPolicy` shape；`getPluginMetadata(...)` 只读取已物化插件对象 safe metadata；`definePlugin(...)` 是默认推荐 authoring 路径，`definePluginFactory(...)` 只用于声明 host externals 的优化路径且 factory host object 首版只包含 `{ externals }`。
  验证：`npm run build`; 新增 `packages/agent-plugin-sdk/tests/plugin-sdk.test.ts` 覆盖 `definePlugin` 保持输入 shape、`definePluginFactory` 保持 factory shape 且 host object type 只包含 `{ externals }`、`defineCapabilityProvider` 保持 runtime `CapabilityProvider` shape 且允许 custom `EAGER` / `SEARCH` discovery 和 executor、`defineTool` 保持当前 `agent-capability` public `defineTool(...)` authoring shape（metadata 字段、可选 `configure`、必需 `execute`、输出 `ToolDefinition { metadata, tool }`）但不从 `agent-capability` re-export 或依赖其实现、`defineTool` 不注册 Tool、不创建 provider、不决定 discovery mode、不读取 config、不注入 dependency、不生成 binding、`defineToolProvider` 可消费 `defineTool(...)` 返回的 `ToolDefinition`，且只接受 `providerId`、`providerType?`、`description?`、`tools: ToolDefinition[]` 并将其规范化为支持 `TOOL` 的 plugin provider、插件加载时 `providerId` 必须由插件作者显式声明且不得从 `pluginId` 推导、`providerType` 省略或等于 `nextagent-plugin-tool`、`defineToolProvider` 拒绝或无法表达 `discoveryMode`、executor、binding policy、default exposure、Agent activation、`pluginId`、`configRoot` 或 path、`defineAgentRoutingPolicy` 只支持 `agentRoutingPolicy` 并复用既有 core routing policy 的 `decide(run, context, signal)` / result contract、SDK inventory metadata 将 `agentRoutingPolicy` 标记为 `OPEN` 且将 `restrictedOperationPolicy` / `modelSelectionPolicy` / `modelFallbackPolicy` / `contextWindowPolicy` 标记为 `RESERVED` 且不提供 RESERVED implementation helper、`defineLifecycleHook` 与 runtime lifecycle hook validation 语义一致、`getPluginMetadata(...)` 返回 plugin id/version/provider id/policy id/hook id 等 safe metadata 且不读取 `plugin.json`、不执行 dynamic import、不校验 host external 版本、不冻结 registry、不编译 Agent activation、`HOST_EXTERNAL_INVENTORY` 只包含 `typebox` 和 `ajv`；新增/更新 SDK package README、package manifest exports、root package inventory、dependency-cruiser package lists、implementation package firewall 和 `tests/architecture/workspace.test.ts`，确认 SDK root 只依赖允许的 `agent-common`、public `agent-contracts` subpaths（包括 `agent-contracts/capability` provider SPI / `DefineToolInput` / `DefineToolProviderInput` shape，以及 `agent-contracts/core` / `agent-contracts/runtime` 中 routing policy 所需 public type）和 runtime-safe utility surface，不依赖 `agent-app`、`agent-runtime`、`agent-core`、`agent-capability`、`agent-platform-gateway-*`、Web/channel、provider SDK、filesystem/shell/network implementation helpers。
  来源：design D1；Requirement "Plugins load only during trusted startup composition"

- [x] 1.2 扩展 system config schema，增加有界 `plugins[]` 配置：最多 8 个插件条目，每个条目包含 `pluginId`、`path`、`required=true`，并校验 `path` 只能是 `configRoot` 下本地相对插件目录，目录必须包含 `plugin.json`。
  验证：新增 `packages/agent-app/tests/plugin-system-config.test.ts` 覆盖合法配置、9 个插件条目被拒绝、重复 plugin id 被拒绝、URL/绝对路径/parent traversal/glob/shell expression/zip/archive/单独 module 文件被拒绝；`npm exec -- vitest run --config vitest.config.release.ts packages/agent-app/tests/plugin-system-config.test.ts`。
  来源：design D2/D3；Requirement "Plugins load only during trusted startup composition"

- [x] 1.3 定义插件运行时 artifact 和依赖边界：`plugin.json.main` 只能指向同一插件目录内的单文件 `.js` ESM bundle；除 `plugin.json.hostExternals` 声明且白名单允许的 host-provided 工具库外，artifact 必须自包含；`agent-app` 不执行插件 `npm install`、不解析插件私有 `node_modules`、不托管插件依赖版本。
  验证：新增 `packages/agent-app/tests/plugin-manifest.test.ts` 覆盖合法 `plugin.json`、main 指向目录外/非 `.js`/缺失 `artifactType=esm-bundle`/relative chunk import 被拒绝；architecture/source test 确认 plugin loader 不调用 npm/yarn/pnpm、不递归解析插件 `node_modules`，并在 dynamic import 前执行 main ESM bundle import specifier 静态扫描。
  来源：design D2；Requirement "Plugins load only during trusted startup composition"

- [x] 1.4 定义 framework-owned `HostExternalInventory`，首版清单精确包含 `typebox(OPEN -> @sinclair/typebox)` 和 `ajv(OPEN -> ajv)`，并固定关闭 `fastify`、`pino`、`kysely`、`@opentelemetry/api`、所有 `agent-*` implementation package、gateway/provider SDK、HTTP/DB client、filesystem/shell helper 和 workspace private path。
  验证：新增 `packages/agent-app/tests/plugin-host-externals.test.ts` 断言清单 id 精确匹配；合法 `typebox`/`ajv` 通过，关闭库、未知 npm package、workspace private path、版本不兼容均被拒绝；断言 loader 不从 host `node_modules` 按插件 import 解析 external。
  来源：design D1/D2/D3；Requirement "Plugins load only during trusted startup composition"

- [x] 1.5 定义 framework-owned `OpenPolicyInventory`，首版清单精确包含 `agentRoutingPolicy(OPEN)`、`restrictedOperationPolicy(RESERVED)`、`modelSelectionPolicy(RESERVED)`、`modelFallbackPolicy(RESERVED)` 和 `contextWindowPolicy(RESERVED)`，并固定关闭 `redactionPolicy`、`promptAssemblyPolicy`、`capabilityConflictResolutionPolicy`、`observabilityProjectionPolicy`、`authorizationAnswerPolicy`、`gatewayRetryPolicy`。
  验证：新增 `packages/agent-app/tests/plugin-policy-inventory.test.ts` 断言清单 id 和 status 精确匹配；断言 `RESERVED` 和关闭清单项均不可激活。
  来源：Requirement "Policy plugins use an explicit open policy inventory"; design D7

- [x] 1.6 定义插件 API contract version：`plugin.json.apiVersion` 可选，采用 `"1.0"` 这类 major/minor 字符串；省略时由 `agent-app` 优先使用插件 export 的 `apiVersion`，若 export 也省略则按当前宿主最新支持版本解释。首版 `LATEST_PLUGIN_API_VERSION` 和 `SUPPORTED_PLUGIN_API_VERSIONS` 只支持 `"1.0"`；root `definePlugin(...)` 作为当前 v1 authoring helper 默认写入固定 `"1.0"`，不得随未来 host latest 版本漂移；显式声明暂不支持的 `"2.0"` 必须 fail closed；插件自身发布版本继续由 `plugin.json.version` / plugin export `version` 表达；未来多版本通过新的 OpenSpec change 增加显式版本化 SDK subpath，本 change 不预先暴露 `vXX` subpath。
  验证：新增/更新 SDK、loader、scaffold 和开发者文档测试，覆盖 SDK 暴露 root/latest/supported plugin API version、`definePlugin(...)` 写入固定 `"1.0"`、manifest 省略 `apiVersion` 可按 plugin export / host latest fallback `"1.0"` 加载、显式 `"1.0"` 可加载、manifest 或 plugin export 显式 `"2.0"` 被拒绝、scaffold 生成 `"apiVersion": "1.0"`、产品路径 e2e 的 `{ plugin.json, index.js }` artifact 携带当前 API version；运行相关 vitest、`npm run build` 和 `openspec validate add-ts-agent-scoped-plugin-composition --strict`。
  来源：Requirement "Plugins load only during trusted startup composition"; design D1/D2

## 2. 启动期加载和 registry 冻结

- [x] 2.1 实现 `agent-app` plugin loader：启动期只按 system config 显式 `plugins[].path` 读取本地插件目录，校验 `plugin.json`、main ESM bundle、host externals、plugin id/version/export/provider identity/provider shape/policy id/hook id/schema/required dependencies/safe description，并生成 `PluginRegistrySnapshot`。
  验证：新增 `packages/agent-app/tests/plugin-loader.test.ts` 覆盖合法插件进入 snapshot、plugin id 不匹配、单插件 4 个 provider 合法、单插件 5 个 provider fail closed、provider id 缺失/重复/providerType 不匹配/reserved provider id/重复 policy id/重复 hook id/非法 manifest/非法 export shape/非法 host external fail closed。
  来源：Requirement "Plugins load only during trusted startup composition"; design D2/D3

- [x] 2.2 实现 host external 注入语义：声明 `hostExternals` 的插件 default export 必须是 plugin factory；`agent-app` 只注入 inventory 中已校验的 `typebox`/`ajv` safe surface，且首版 factory host object 精确为 `{ externals }`；未声明 host external 的插件可以导出 plain plugin object。
  验证：`packages/agent-app/tests/plugin-host-externals.test.ts` 覆盖 factory 注入 `typebox`/`ajv` 且 host object 不包含 undocumented field、plain plugin 声明 host external 被拒绝、factory 请求未声明 external 被拒绝、main bundle 残留 `import { Type } from "@sinclair/typebox"`、`export { Type } from "@sinclair/typebox"`、`await import("@sinclair/typebox")` 或 `import "./chunk.js"` 时由 dynamic import 前的静态 import specifier 扫描拒绝并 fail closed。
  来源：Requirement "Plugins load only during trusted startup composition"; design D1/D2/D3

- [x] 2.3 实现 required/optional 加载语义：`required=true` 插件加载或校验失败阻断 readiness；`required=false` 加载失败只产生 safe degradation diagnostic，且不进入 registry。
  验证：`packages/agent-app/tests/plugin-loader.test.ts` 覆盖 required 失败和 optional 失败；断言 optional 失败插件不能被后续 Agent activation 使用。
  来源：design D3；Requirement "Plugins load only during trusted startup composition"

- [x] 2.4 增加插件加载边界 tests，确认 `agent-app` 只按 system config 显式声明的本地插件目录执行启动期加载，且 request path 只消费冻结 registry 和当前 Agent activation。
  验证：新增 `tests/architecture/plugin-loading-boundary.test.ts` 和 `packages/agent-app/tests/plugin-loader.test.ts` negative cases；实际传入这些字段并断言 loader 未被调用、activation 未改变；未在 system config 显式声明的插件目录即使存在也不进入 registry；覆盖 bundle import specifier 静态扫描拒绝 static import、re-export with `from`、bare package、closed host package、workspace private path、Node builtin / `node:` specifier、URL、绝对路径、parent traversal、relative chunk import 和 string-literal dynamic import，确认拒绝发生在 main bundle evaluate 之前。
  来源：Requirement "Plugins load only during trusted startup composition"; design D2/D3/D9

## 3. Agent-scoped activation 编译

- [x] 3.1 扩展 `AgentDefinition` parser/schema，增加 Agent 顶层 `policies` 配置，只支持开放 policy implementation activation，并编译到 `AgentAssembly.policies`；插件 provider 下的 Tool binding 仍只能通过既有 `capabilityBindings` 声明，插件 hook activation 仍只能通过同级 `hooks` / `AgentAssembly.hooks` 声明，并拒绝 `policies` 中定义 provider binding、Tool binding、hook binding、Tool schema、execution mapping、hook implementation、plugin path、module path、script、remote call 或 DSL。
  验证：新增 `packages/agent-app/tests/agent-plugin-definition-parser.test.ts` 覆盖合法 activation 和非法 key negative cases。
  来源：Requirement "Plugin activation is explicit and Agent-scoped"; design D5

- [x] 3.2 扩展 `StartupAgentAssemblyCompiler`，根据 `PluginRegistrySnapshot` 校验 Agent `policies`，并把当前 Agent 的 policy activation 编译为 `AgentAssembly.policies` implementation-free binding facts；插件 provider/Tool binding 继续由 `AgentAssembly.capabilityBindings` 表达，插件 hook object 只进入 startup hook registry，Agent-scoped hook activation 继续由 `AgentAssembly.hooks` 表达；不把 implementation handle 写入 public `AgentAssembly`。
  验证：新增 `packages/agent-app/tests/plugin-activation-compiler.test.ts` 覆盖 `AgentAssembly.policies` keyed by accepted assembly、每个 compiled policy binding 必含 `policyPointId`、`pluginId`、`policyId` 和 `enabled` 且只可选带 `timeoutMs` / validated `config`、`policies` 中 provider/Tool/hook activation 被拒绝、provider 下的 Tool 只能通过 `capabilityBindings` 绑定、hook 只能通过 `hooks` 激活；contract/architecture test 确认 `AgentAssembly.policies` 不包含 evaluator function、closure、module path、plugin directory path、registry handle、raw config 或其它 plugin implementation handle。
  来源：Requirement "Plugin activation is explicit and Agent-scoped"; design D4/D5

- [x] 3.3 实现 Agent policy/hook activation 完整性校验：Agent assembly 编译校验 plugin id、policy id、policy point id 和 hook id 引用，并在引用缺失或非法时产生 safe diagnostic。插件 provider 下的 Tool binding 支持 descriptor 延迟发现，descriptor 缺失由 capability catalog gate 投影为 unavailable。
  验证：`packages/agent-app/tests/plugin-activation-compiler.test.ts` 覆盖缺失 policy plugin/policy id/policy point/hook id；`tests/agent-kernel/plugin-provider-capability.test.ts` 覆盖绑定但 descriptor 未发现时 catalog 不暴露该 Tool；断言 app 不以缺失 policy/hook ready。
  来源：Requirement "Plugin activation is explicit and Agent-scoped"; Requirement "Plugin diagnostics are safe and auditable"

- [x] 3.4 增加多 Agent 作用域测试：同一 plugin provider 下的 Tool 被 Agent A 绑定、policy/hook 被 Agent A 激活时，Agent A/B 各自只消费自己的 accepted `AgentAssembly` facts。
  验证：`packages/agent-app/tests/plugin-agent-scope.test.ts` 覆盖 Agent A/B 独立 activation；断言只按 accepted `agentAssemblyRef` 查找。
  来源：Requirement "Plugin activation is explicit and Agent-scoped"; design D4

## 4. Plugin provider 进入 capability 主路径

- [x] 4.1 在 capability contract 中将当前纯身份对象 `CapabilityProvider` 重命名为 `CapabilityProviderIdentity`，将当前注册单元 `CapabilityProviderContribution` 重命名为 runtime `CapabilityProvider`，并保持 runtime `CapabilityProvider = identity + discovery + optional executor`；新增/冻结 `DefineToolInput`、public `ToolDefinition` authoring shape 和 `DefineToolProviderInput`，用于 plugin SDK 和 first-party module 低成本创建 Tool 与标准 Tool provider。Descriptor、discovery、binding、diagnostic、安全投影只携带 provider identity；provider implementation handle 不进入 descriptor、AgentAssembly、Web response、stream 或 gateway record；所有 provider 注册来源都必须迁移到同一 shape，包括 builtin/config-driven provider assembly、外部 provider 输入、plugin provider 和现有 `memory-tools` provider。
  验证：新增/更新 contract tests 覆盖 `CapabilityDescriptor.provider`、`CapabilityDiscovery.provider`、runtime `CapabilityProvider.identity` 的一致性，以及 `CapabilityProvider` 支持 custom `EAGER` / `SEARCH` discovery 和 optional executor；contract test 覆盖 `DefineToolInput` 对齐当前 public Tool authoring shape 并输出 `ToolDefinition { metadata, tool }`，覆盖 `ToolDefinition`、`ToolMetadata`、`Tool`、`ToolExecuteOptions`、`ToolDependencies`、`ToolObservabilityDefinition` 通过 `agent-contracts/capability` 暴露；contract test 覆盖 `DefineToolProviderInput` 只包含 required `providerId`、optional `providerType`、optional `description`、`tools: ToolDefinition[]`，并覆盖 plugin loader 对该通用 shape 施加 plugin provider identity validation；`npm run build`; `npm run test:contract`; architecture test 确认 provider executor/discovery implementation handle 不进入 Web response、stream、gateway record 或 AgentAssembly；source/contract check 确认产品代码不再导出或消费 `CapabilityProviderContribution`。
  来源：Requirement "Plugin providers enter the capability governance path"; design D6

- [x] 4.1a 在 `agent-capability` 增加 provider validation/normalization/wrapping，消费 runtime `CapabilityProvider`，统一封装 provider identity consistency、descriptor projection/schema validation、config override、required dependency check、duplicate capability id validation、discovery result validation、executor lookup、timeout/cancellation、safe error 和 diagnostic；plugin manifest、plugin activation、Agent binding policy、host external injection、memory opt-in 和 memory domain execution 语义保持在各自 owner。
  验证：新增 `packages/agent-capability/tests/provider-wrapping.test.ts` 覆盖 descriptor identity 一致、duplicate capability id fail closed、unknown Tool config fail closed、required dependency 缺失时 descriptor unavailable、config override 生效、executor lookup 使用 provider identity + capability id、custom `SEARCH` discovery 调用 timeout/cancellation/safe error mapping/safe diagnostic 和 discovery result validation、wrapped provider 可被现有 provider assembly 消费；architecture test 确认 wrapping 不依赖 `agent-app`、`agent-plugin-sdk`、`agent-memory`、gateway/platform、filesystem/shell/network helper。
  来源：Requirement "Plugin providers enter the capability governance path"; design D1, D6

- [x] 4.1b 在 capability subsystem 中实现 framework-owned catalog binding policy：`AgentAssembly.capabilityBindings` 消费 Agent 显式 binding；系统默认暴露作为 catalog 内部有限 allowlist 生效；binding matching 支持精确 capability binding 和既有 provider/condition selector；`enabled=false` 的精确 binding 可覆盖默认暴露或过滤 search 结果；未知 provider kind/providerType 使用 OpenSpec-defined binding policy 或 fail-closed 默认策略。
  验证：新增/更新 `packages/agent-capability/tests/capability-binding-policy.test.ts` 覆盖 builtin Tool/Skill/local Skill/local subagent 的 catalog 内部默认暴露且无 synthetic assembly binding、top-level Agent 精确 binding、SkillHub provider/condition selector、memory-tools opt-in、plugin provider 精确 binding；覆盖未知 CUSTOM providerType、未知 providerKind 或未声明 binding policy 的 provider 不可见/不可 search/不可 invoke；source/architecture test 确认 plugin manifest/Agent config/descriptor schema 不包含 default exposure 或 binding policy 字段。
  来源：Requirement "Plugin activation is explicit and Agent-scoped"; design D6

- [x] 4.2 在 `agent-capability` 增加 plugin provider adapter，消费 `agent-app` 从已加载插件 `providers[]` 校验出的 `CapabilityProvider`，要求首版每个插件最多 4 个 capability provider，且每个 provider identity 使用 `providerKind=CUSTOM`、`providerType=nextagent-plugin-tool` 和插件作者显式声明的 safe `providerId`；插件 provider 返回 `TOOL` descriptors，可使用 `EAGER` 或 `SEARCH` discovery；插件 provider 复用 4.1a 的 provider validation/normalization/wrapping 和统一 discovery/executor path。
  验证：新增 `packages/agent-capability/tests/plugin-provider.test.ts` 覆盖 descriptor projection、插件作者自定义 providerId safe-id provider identity、provider id 缺失 fail closed、provider id 全局重复 fail closed、reserved provider id fail closed、providerType mismatch fail closed、单插件 4 个 provider 合法、单插件 5 个 provider fail closed、plugin provider `EAGER` descriptors、plugin provider `SEARCH` delayed descriptors、plugin provider `SEARCH` discover throw/timeout/cancellation/invalid result 均通过统一 wrapping fail closed 并产生 safe diagnostic、非 `TOOL` descriptor 被拒绝、required dependencies 校验和 unavailable descriptor、自定义 executor 通过统一 invocation path 调用，并断言插件 provider 与 first-party provider 输入同构且没有第二套 plugin Tool discovery/executor registry。
  来源：Requirement "Plugin providers enter the capability governance path"; design D6

- [x] 4.3 在 `agent-app` composition 中把已加载插件的 `providers[]` 作为外部 runtime `CapabilityProvider` 接入 capability subsystem；`defineToolProvider(...)` 生成标准 Tool provider，`defineCapabilityProvider(...)` 可携带自定义 discovery/executor；同步把现有 memory tool 注册从 `createMemoryToolsProviderContribution(...)` / `externalContributions` 迁移到新的 `createMemoryToolsProvider(...)` / external `CapabilityProvider` 输入，且 memory-owned 薄 wrapper 使用 `agent-contracts/capability` provider SPI / `DefineToolInput` / `DefineToolProviderInput` shape；插件 provider、memory provider 和 Tool visibility 继续由 catalog binding filtering / memory opt-in 语义治理。
  验证：新增 `tests/agent-kernel/plugin-provider-capability.test.ts` 覆盖带有既有 `capabilityBindings` 且 `enabled` 省略/为 `true` 的 Agent 可在 capability list 看到插件 provider 下的 `EAGER` Tool，并可通过 `CapabilityInvocationPort` invoke；plugin provider descriptor 已被 `EAGER` discovery 投影但当前 Agent 未绑定时不可见；`SEARCH` plugin provider 未被 provider/condition selector 授权时不可 search，授权后返回 Tool descriptor 且 invocation 仍走统一 capability path；新增/更新 memory-tools regression 覆盖 memory opt-in 满足时 `memory-tools` provider 进入 capability subsystem，discovery/provider/descriptor identity 一致，opt-in 不满足时不注册或不可见，并确认 memory Tool provider 使用同构 provider SPI 但保留 memory opt-in/description diagnostic/domain execution 语义；architecture/source test 确认未新增第二套 plugin Tool registry、plugin Tool executor registry、memory Tool registry 或 app-owned Tool catalog，provider conflict validation 继续由 capability provider assembly 处理，并确认 app/assembly compiler 不生成 synthetic binding，`agent-memory` 不依赖 `agent-plugin-sdk` 或 `agent-capability`。
  来源：Requirement "Plugin providers enter the capability governance path"; design D6

- [x] 4.4 增加 capability discovery 与 `capabilityBindings` filtering 边界/regression tests，确认 descriptor presence 与 Agent visibility/search/resolve/invoke authority 分层治理，并覆盖 catalog 内部默认暴露 allowlist、精确 capability binding、provider/condition selector、memory opt-in、plugin provider、builtin Tool、SkillHub、Skill、Subagent 和 unknown providerType 的统一 filtering matrix。
  验证：`tests/agent-kernel/plugin-provider-capability.test.ts` 覆盖 unbound Agent、disabled plugin binding、enabled plugin binding、EAGER plugin descriptor without binding、SEARCH plugin provider without selector、SEARCH plugin provider with selector、builtin Tool default visibility regression 且不生成 synthetic binding、memory-tools enabled binding opt-in regression、memory-tools missing/disabled binding regression、Skill visibility regression、agent-owned Skill default SEARCH regression、SkillHub provider-gated SEARCH regression（未授权不 search、授权后 search、returned Skill 不要求每个 exact enabled binding、exact disabled binding 仍过滤对应 Skill）、top-level Agent binding regression、parent subagent default SEARCH regression，以及 unknown provider binding policy fail-closed；architecture test 确认 runtime/core 无 plugin Tool implementation import，plugin provider 未写入 builtin/default Skill/subagent provider，memory-tools 未被加入 builtin default-enabled provider 集合。
  来源：Requirement "Plugin providers enter the capability governance path"; Requirement "Plugin activation is explicit and Agent-scoped"

## 5. Policy 和 Hook activation 消费

- [x] 5.1 实现 policy validation：插件 policy 只有引用 `OpenPolicyInventory` 中 `OPEN` 状态的 `agentRoutingPolicy` 才可通过，implementation shape 必须满足固定 `AgentRoutingPolicy` contract。
  验证：`packages/agent-app/tests/plugin-policy-inventory.test.ts` 覆盖合法 `agentRoutingPolicy`；`restrictedOperationPolicy`、`modelSelectionPolicy`、`modelFallbackPolicy`、`contextWindowPolicy` 作为 `RESERVED` 被拒绝；`redactionPolicy`、`promptAssemblyPolicy` 等关闭清单项逐项被拒绝。
  来源：Requirement "Policy plugins use an explicit open policy inventory"; design D7

- [x] 5.2 接入 Agent-scoped `agentRoutingPolicy` adapter：复用既有 core routing policy 形状 `decide(run: RequestRun, context: RequestContext, signal: AbortSignal)` 和 `AgentRoutingPolicyResult`；`AgentRoutingPolicyResult` 必须对齐 `agent-contracts/core.AgentRoutingDecision`，只包含 `kind`、`safeReason`、`evidenceRef?`、`skillName?`，且 `kind` 使用现有 `RoutingDecisionKind`；wrapper 不定义单独的 `AgentRoutingPolicyInput`，也不做 safe summary、redaction、truncation 或字段投影，输入边界如需收紧必须在 routing 业务 contract 中统一调整并同时适用于内置 policy 和插件 policy；SDK 暴露通用 `PluginPolicy` contribution shape，`AgentRoutingPolicy` 是 `PluginPolicy<"agentRoutingPolicy"> & AgentRoutingPolicyExecutable` 的 routing-specific specialization；`agent-contracts/runtime` 定义以 `AgentPolicyExecutableByPoint` 枚举 supported policy point 到各自 executable 类型的 resolver contract，`agent-runtime` materialize policy registry/resolver、startup executable 和 assembly-specific configured executable，`agent-core` 提供按 accepted Agent scope + `policyPointId` 从 `AgentAssembly.policies` binding 解析 executable 的 routing typed adapter，`agent-app` 只负责 composition root wiring 并把 policy resolver 注入 `agent-core`；registry/resolver 只作为不同 policy executable 形状的统一容器和查询机制，不要求所有 policy point 使用 routing input/output 或同一 method shape；当前 Agent 激活 routing policy implementation 时由 routing typed adapter 使用插件 `decide(...)`，`AgentAssembly.policies` 缺失/为空/无 enabled `agentRoutingPolicy` binding 或 Agent 未配置 `policies` 时委托系统内置 routing policy，同一 Agent 对同一 policy point 保持唯一 enabled implementation。
  验证：新增/更新 contract tests 覆盖 `AgentRoutingPolicyExecutable.decide(RequestRun, RequestContext, AbortSignal)` 与内置 routing policy shape 一致、`AgentRoutingPolicyResult` 与 `agent-contracts/core.AgentRoutingDecision` shape 和 `RoutingDecisionKind` vocabulary 对齐并拒绝额外 result 字段；新增 `tests/agent-kernel/plugin-routing-policy.test.ts` 覆盖激活、`AgentAssembly.policies` 缺失、`AgentAssembly.policies=[]`、无 enabled `agentRoutingPolicy` binding，并断言 wrapper 使用 policy resolver 按 accepted `agentId`、`agentVersion`、`agentAssemblyRef` 和 `policyPointId` 选择当前 Agent 的 `AgentAssembly.policies`，而不是默认 Agent、Agent config 或全局 plugin registry membership；覆盖 registry 作为可枚举 policy point 的 typed executable 容器，重复激活、activation config invalid 或 implementation/executable 不可用在 assembly 编译或 registry materialization 阶段失败，activation config 通过 `configSchema` 校验并生成 assembly-specific configured executable；policy resolver request path 在存在 enabled binding 时返回 resolved entry，没有激活项时返回 `undefined`；policy throw/timeout/invalid result fail closed to safe routing rejection；architecture/source test 确认 runtime/capability/model/channel 不读取 plugin registry、Agent config 或 plugin path，`agent-core` 不读取 raw Agent config 或 plugin registry 来选择 evaluator，且插件 evaluator 没有替换成全局 app-wide evaluator。
  来源：Requirement "Policy plugins use an explicit open policy inventory"; design D7

- [x] 5.3 实现 plugin hook validation 和 startup hook registry 接入：插件 `hooks[]` 提供从 `agent-plugin-sdk` 的 `defineLifecycleHook(...)` 生成的 `LifecycleHook` object；校验 hookId、supportedStages、effects、failureMode、order、configSchema、configure、execute 和 safe diagnostics；将合法插件 hook object 作为 already-composed startup hook registry input。Agent 是否启用该 hook 由 `agent.yaml.hooks` / `AgentAssembly.hooks` 决定。
  验证：新增 `tests/agent-kernel/plugin-lifecycle-hook.test.ts` 覆盖插件贡献的 `LifecycleHook` object 可被 `agent.yaml.hooks` 激活并在 `BEFORE_AGENT_TERMINAL` 执行、未在当前 Agent `hooks` 中 enabled 时不执行、unsupported stage/effects/outcome 被拒绝。
  来源：Requirement "Hook plugins reuse lifecycle hook execution semantics"; design D8

- [x] 5.4 增加 hook/policy 边界 tests，确认 risk policy、lifecycle hook、hook executor、plugin registry 和 Agent hook activation 各自使用对应 owner 和 activation facts。
  验证：`tests/agent-kernel/plugin-lifecycle-hook.test.ts` 和 `tests/architecture/plugin-loading-boundary.test.ts` 实际触发非法配置并断言失败。
  来源：Requirement "Hook plugins reuse lifecycle hook execution semantics"; design D8

## 6. Safe diagnostics、架构门禁和回归验证

- [x] 6.1 实现 startup、Agent activation、execution 三类 plugin diagnostic 的 safe mapping，只输出 plugin id、provider id、policy id、policy point id、hook id、capability id、agent id、agent version、agentAssemblyRef、safe reason code、bounded summary 和低基数 outcome。
  验证：新增 `packages/agent-app/tests/plugin-diagnostics.test.ts` 和 `tests/agent-kernel/plugin-observability.test.ts`；断言不包含本地绝对路径、raw config、secret、credential、prompt、model output、tool arguments、tool result、stack trace。
  来源：Requirement "Plugin diagnostics are safe and auditable"; design D9

- [x] 6.2 增加 architecture tests，确认 plugin loading owner、runtime/core/capability consumption boundary、SDK dependency boundary、memory provider boundary 和 public contract surface 均按本 change 的 owner 关系收敛。
  验证：新增/更新 `tests/architecture/plugin-composition-boundary.test.ts`；运行 `npm run lint:architecture`；source/architecture check 确认只有 `agent-app` plugin loader 可执行 plugin dynamic import，且 dynamic import 前必须经过 import specifier 静态扫描，不允许通过 Node resolution 兜底解析 host external package、workspace private path 或 relative chunk import。
  来源：design D1/D2/D3/D7；Requirement "Plugins load only during trusted startup composition"

- [x] 6.3 增加默认空插件配置回归测试，确认未配置 plugins 时现有 builtin/local/SkillHub capability、系统内置 risk policy 和 no-op/system lifecycle hook 行为不变。
  验证：`tests/agent-kernel/plugin-default-regression.test.ts`; 运行 `npm test -- --run tests/agent-kernel/plugin-default-regression.test.ts`。
  来源：design Migration Plan

- [x] 6.4 运行 change 验证套件和 OpenSpec 验证。
  验证：`tests/e2e/plugin-composition-product-path.test.ts` 覆盖插件项目 scaffold/source build -> `{ plugin.json, index.js }` 单文件 ESM bundle artifact -> system config loader -> Agent `policies` activation -> request path routing policy 生效的黑盒闭环；`openspec validate add-ts-agent-scoped-plugin-composition --strict`; `npm run build`; `npm test`; `npm run test:contract`; `npm run lint:architecture`。
  来源：proposal 验证入口；design Verification Map

- [x] 6.5 新增插件开发者指南，面向智能体二次开发者说明 `create-nextagent-plugin` 快速开始、本地插件目录结构、`plugin.json` 字段、ESM bundle 构建方式、默认推荐直接把 `typebox`/`ajv` 和其它依赖打包进 bundle 并使用 `definePlugin(...)`、`definePluginFactory(...)` + `hostExternals` 作为多插件共享且需宿主版本一致时的优化路径、`providers[]` / `policies[]` / `hooks[]` 示例、`defineCapabilityProvider(...)` 高级 provider SPI、`defineTool(...)` 单 Tool authoring 糖、`defineToolProvider(...)` provider 糖、`defineAgentRoutingPolicy(...)` policy authoring 糖、`agent-test-kit` plugin harness 示例、Agent `capabilityBindings` Tool 绑定示例、Agent `policies` / `AgentAssembly.policies` policy activation 示例、safe diagnostic 故障排查和禁止事项。
  验证：新增 `docs/developer/agent-plugins.md` 或等价开发者文档，并更新 `docs/developer/README.md` 导航；文档必须包含 `npx create-nextagent-plugin my-plugin -> npm run build -> copy dist output to configRoot/plugins/<pluginId>/` 快速开始、脚手架生成 `package.json` / `tsconfig.json` / `esbuild.config.ts` / `src/index.ts` / `plugin.json` / `tests/plugin.test.ts` 的说明、`configRoot/plugins/<pluginId>/plugin.json` 示例、`plugins[].path` system config 示例、system config 最多 8 个插件、插件 `providers[]` 首版最多 4 个 provider、默认 `definePlugin(...)` + bundled `typebox`/`ajv` 示例、`definePluginFactory(...)` + `host.externals` 优化示例且说明 factory host object 首版只含 `{ externals }`、`defineCapabilityProvider(...)` custom discovery/executor 示例、`defineTool(...)` + `defineToolProvider(...)` 糖示例、`DefineToolInput` 可表达 Tool metadata/schema/config/dependency/policy/observability/configure/execute 且不得承载 provider/discovery/binding/activation/loading 的说明、`DefineToolProviderInput` 仅包含 required `providerId` / optional `providerType` / optional `description` / `tools` 且不得承载 discovery/executor/binding/activation 的说明、插件侧 provider id 由插件作者显式声明且使用 safe id vocabulary、providerType 为 `nextagent-plugin-tool`、`getPluginMetadata(...)` 只用于 authoring/test shape 自检且不证明生产可加载的说明、`createPluginTestHarness(...)` 直接导入插件对象并 `invokeTool(...)` / `evaluateAgentRoutingPolicy(...)` / `executeHook(...)` 的示例、harness 不验证 bundler/loader/manifest/Agent activation/主路径治理的说明、Agent `capabilityBindings` 绑定插件 provider 下 Tool 示例、`defineAgentRoutingPolicy(...)` 示例、Agent `policies` activation 示例、`AgentAssembly.policies` implementation-free binding 说明、`agentRoutingPolicy` 为唯一 `OPEN` policy point 且其它 policy point 为 `RESERVED` 的说明，以及 no zip/no multi-file bundle/no directory scan/no runtime install/no host `node_modules` import/no private path/no remote code 的禁止清单；code review 确认文档没有定义 spec 未冻结的新行为。
  来源：design Documentation Ownership；Requirement "Plugins load only during trusted startup composition"; Requirement "Plugin activation is explicit and Agent-scoped"; Requirement "Policy plugins use an explicit open policy inventory"; Requirement "Plugin scaffold is available from the SDK dev entry point"; Requirement "Plugin logic is testable without app deployment"

- [x] 6.6 扩展 `@nextagent/agent-test-kit`，新增 public export `createPluginTestHarness(plugin, options?)`，直接消费已导入插件对象，提供 `invokeTool(providerId, capabilityId, input)`、`evaluateAgentRoutingPolicy(policyId, run, context)` 和 `executeHook(hookId, input)` 测试入口；该 harness 只用于测试插件对象逻辑，不启动 `createComposedApp`，不读取 system config / `plugin.json` / plugin directory，不执行 dynamic import，不做 bundle import specifier 静态扫描，不校验 host external 版本，不冻结 plugin registry，也不生成 `AgentAssembly` activation facts。
  验证：新增 `packages/agent-test-kit/tests/plugin-test-harness.test.ts` 覆盖直接导入插件对象后可调用 `defineTool(...)` + `defineToolProvider(...)` 暴露的 Tool 并注入 `toolDependencies`、provider/tool id 不匹配 fail closed、缺失 Tool dependency fail closed、可 evaluate `agentRoutingPolicy` 且传入与 core routing policy 一致的 `RequestRun` / `RequestContext`、可 execute `defineLifecycleHook(...)` 暴露的 hook；negative tests 断言 harness 不读取 `plugin.json`、不调用 `createComposedApp`、不执行 dynamic import、不会做 host external injection/version validation、不会创建 plugin registry snapshot 或 `AgentAssembly.policies`；architecture test 确认 `agent-test-kit` 只依赖允许的 `agent-common`、public `agent-contracts` subpaths、`agent-plugin-sdk` public types 和 test-only utility，不依赖 `agent-app`、gateway/platform、filesystem/shell/network helper、provider SDK 或 implementation private path。
  来源：Requirement "Plugin logic is testable without app deployment"; design D10

- [x] 6.7 在 `@nextagent/agent-plugin-sdk/scaffold` 实现 dev-only scaffold，并通过 package `bin` 暴露 `create-nextagent-plugin` CLI；CLI 支持 `create-nextagent-plugin <plugin-directory>`，生成默认 direct-bundle 插件项目，包含 `package.json`、`tsconfig.json`、`esbuild.config.ts`、`src/index.ts`、`plugin.json` 和 `tests/plugin.test.ts`；模板默认使用 `definePlugin(...)`，`esbuild.config.ts` 预配置 ESM、single-file bundle 和 inline sourcemap，`tests/plugin.test.ts` 使用 `getPluginMetadata(...)` 校验插件对象 safe metadata。
  验证：新增 `packages/agent-plugin-sdk/tests/plugin-scaffold.test.ts` 覆盖 CLI/API 生成目录和文件、`package.json` 包含 `@nextagent/agent-plugin-sdk` / `esbuild` / TypeScript / test tooling、`esbuild.config.ts` 配置 ESM single-file inline sourcemap、`src/index.ts` 使用 `definePlugin(...)` 且不默认使用 `definePluginFactory(...)` / `hostExternals`、`plugin.json` 与 plugin id/main/artifactType 对齐、`tests/plugin.test.ts` 使用 `getPluginMetadata(...)` 校验 shape、生成 README 或 scripts 指导 `npm run build` 后复制 dist 到 `configRoot/plugins/<pluginId>/`；negative tests 覆盖目标目录已存在/非法目录名/parent traversal fail closed；architecture test 确认 `@nextagent/agent-plugin-sdk/scaffold` 可使用 dev-only filesystem/template 依赖但 SDK root exports 和 runtime packages 不依赖 scaffold subpath，且 scaffold 不生成 Agent `capabilityBindings`、Agent `policies`、system config `plugins[]` 或 runtime registry artifact。
  来源：Requirement "Plugin scaffold is available from the SDK dev entry point"; design D11

## 归档前更新基线检查（非实施任务）

实现完成并验证通过后，在归档前根据 proposal/design 的“归档前更新基线”处理：

- 同步 `openspec/specs/agent-scoped-plugin-composition/spec.md`。
- 更新 `openspec/overview.md`，提炼受控 Agent-scoped startup plugin composition 进入 capability/governance 基线，同时保留动态加载、热加载、远端分发、marketplace 非目标。
- 更新 `openspec/designs/architecture/capability-spi.md`，提炼 `CapabilityProviderIdentity` / runtime `CapabilityProvider` public SPI 命名修正、`DefineToolInput`、`DefineToolProviderInput`、provider validation/normalization/wrapping，以及 plugin provider 进入 capability catalog 的主路径。
- 更新 `openspec/designs/architecture/security-and-governance.md`，提炼开放 policy 清单、policy replacement 限制和 Agent Scope 隔离。
- 新增或更新 `openspec/designs/modules/agent-plugin-sdk.md`，提炼 SDK authoring helper、`getPluginMetadata(...)`、provider/policy/hook public surface、开放 policy inventory type exposure、host external type exposure、`@nextagent/agent-plugin-sdk/scaffold` dev-only subpath、`create-nextagent-plugin` CLI、workspace package boundary 和禁止依赖 implementation package 的架构约束。
- 更新 `openspec/designs/modules/agent-app.md`、`openspec/designs/modules/agent-capability.md`、`openspec/designs/modules/agent-core.md`、`openspec/designs/modules/agent-runtime.md`。
- 更新 `openspec/designs/modules/agent-test-kit.md`，提炼 plugin test harness 的 test-only helper、public dependency boundary 和不替代 app loader/activation/main path 验证的限制。
- 新增或更新 `openspec/designs/adr/agent-scoped-startup-plugin-composition.md`。
- 更新 `openspec/designs/spec-to-design-map.md`。
- 保留或更新开发者指南 `docs/developer/agent-plugins.md` 和 `docs/developer/README.md` 导航，确保其只解释归档后基线 contract，不成为第二套行为来源。
- 检查长期文档没有重复定义同一状态机、API schema、数据 owner 或接口语义。
