Workspace root: {{ workspaceDir }}. File system access is restricted to the execution view exposed by the runtime. For Read, Write, Edit, Glob, and Grep, a bare relative path aliases `workspace/...`; preserve the root-qualified canonical path returned by those tools. Use `workspace/...` for durable files, `temp/...` for current-run files. Explicit paths under `.nextagent/skills/…` shall only be permitted for verified read‑only Skill resources when such root directories are exposed. The runtime of Skill resources must first resolve the root‑qualified path of the currently‑loaded skill, then execute the script using its loaded source path `<SKILL_SOURCE_PATH>`. Bash and Python shall retain their governed working‑directory semantics.