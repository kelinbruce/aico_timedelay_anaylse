## 1. FN-5.8 发现技能

- [x] 1.1 修改 `packages/agent-capability/src/skills/skill-manifest.ts`：删除 `supportedTopLevelKeys` 白名单与未知顶层键 `INVALID_OFFICIAL_FIELD` 拒绝逻辑（未知顶层键静默忽略、不进 metadata）；metadata 侧不安全键值/保留句柄/直填对象/不支持数组键/不支持值形态改为静默省略（不产生 `SOURCE_METADATA_OMITTED`/`EXTENSION_OMITTED` 诊断、不拒绝 manifest），`parseMetadataWithExtension` 简化为无诊断返回并删除 dead code `parseSourceMetadata`；`supportedMetadataKeys` 移除 `nextagent.model`、`nextagent.modelOptions`、`denied-tools`、`model` 并新增 `modelOptions`；`metadata.nextagent.model` / `metadata.nextagent.modelOptions` / `metadata.denied-tools` / `metadata.model` 与其他未知 metadata 同策略，按安全 source metadata 保留、无治理语义；顶层 `disallowed-tools` 复用 `parseOptionalToolConstraint`（与 `allowed-tools` 同一解析路径）并删除 `metadata.denied-tools` 形态特判；`parseModelDeclarations` 重写为单值解析——顶层 `model` 仅接受模型名字符串（JSON 对象形态以 `UNSAFE_MODEL_DECLARATION` 拒绝），`metadata.modelOptions` 为唯一推理参数入口且拒绝其中出现 `model` 键；删除 `parseModelDeclarationValue` helper 并清理 dead code `stableStringify`/`sortJson`。验证：task 1.2 的 focused tests 通过（32 passed）。来源：Requirements `Skill Manifest Supports Standard And Supported Extension Frontmatter Fields`（Scenario `未知顶层字段被忽略而不拒绝`）、`Unknown metadata Does Not Carry Governed Meaning`、`Skill Manifest Diagnostic Includes Extension Reason Code`、`Metadata Field Parsing Distinguishes String Source Metadata, Reserved Extension Wrapper, and Invalid Direct-Form Object`（Scenario `不安全或不受支持的 metadata 值被静默忽略`）、`allowed-tools And disallowed-tools Are Tool Constraint Facts`、`Model Declarations Are Governed Model Hints`。
- [x] 1.2 更新 `packages/agent-capability/tests/skill-manifest.test.ts`：新增未知顶层键（`icon`/`last-updated`/`author`）被忽略且 manifest accepted 的正向用例；8 个原 degraded/拒绝用例（不安全 source metadata、保留句柄、不安全数组、不安全 extension 键/值、直填对象拒绝、直填 primitive、非白名单 header 键）改为静默省略断言（outcome accepted、无诊断、安全兄弟条目保留）；原 `nextagent.model` / `nextagent.modelOptions` / `metadata.denied-tools` 正向用例改为顶层 `model` 模型名字符串 + `metadata.modelOptions` + 顶层 `disallowed-tools`；empty-metadata / empty-optionals 用例移除已删键；新增 negative cases——`metadata.nextagent.model` 拒绝、`metadata.nextagent.modelOptions` 拒绝、`metadata.denied-tools` 拒绝、`metadata.modelOptions` 含 `model` 键拒绝、顶层 `model` JSON 对象拒绝、非法 `disallowed-tools` 拒绝；新增 `metadata.model` 回归 `sourceMetadata` 与模型名+推理参数归一为单一 hint 的断言。验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/skill-manifest.test.ts` 全部通过（32 passed）。来源：Requirement 全部新增/修改 Scenarios。
- [x] 1.3 更新开发者与用户文档中的声明写法：`docs/developer/04-skill-tool-development.md`（frontmatter 键表、tool 约束说明、示例、checklist）和 `docs/用户配置和使用指导.md` 中 `metadata.nextagent.*`、`metadata.denied-tools` 表述改为顶层 `model` / `metadata.modelOptions` / `disallowed-tools`。验证：`grep -rn "nextagent.model\|denied-tools" docs/` 仅剩历史归档类文档（`docs/nextagent-ts-changes/` 等）与 OTel span 命名空间。

- [x] 1.4 实现 description 文种相关长度上限：含 Han script 字符（含中英混排）上限 1024 不变，纯英文上限放宽到 4096（`\p{Script=Han}` 正则判定，`String.length` 计数）；更新 `skill-creator` builtin SKILL.md 与 developer 文档。验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/skill-manifest.test.ts` 通过（33 passed，新增 4096 接受/4097 拒绝/1024 混排接受/1025 中文拒绝 4 个断言）。来源：Requirement `Skill Manifest Supports Standard And Supported Extension Frontmatter Fields` Scenario `description 长度上限按文种区分`。

- [x] 1.5 移除 `removedGovernedMetadataKeys` 迁移期拒绝守卫：`metadata.nextagent.model`、`metadata.nextagent.modelOptions`、`metadata.denied-tools` 与其他未知 metadata 同策略，安全字符串值保留进 `sourceMetadata`、无治理语义、不拒绝不诊断；顶层 `model` JSON 对象拒绝（`UNSAFE_MODEL_DECLARATION`）保持不变。验证：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability/tests/skill-manifest.test.ts` 通过（34 passed，原两个拒绝 negative cases 改为 sourceMetadata 保留断言）。来源：Scenarios `移除的 nextagent 声明键不再获得治理语义`、`移除的 metadata.denied-tools 键不再获得治理语义`。

## 2. 共享门禁

- [x] 2.1 运行 agent-capability 套件与 `openspec validate --all --strict`，记录实际结果。实际：`npx.cmd vitest run --config vitest.config.release.ts packages/agent-capability` 为 775 passed、17 failed、11 skipped——17 个失败经 stash 基线复跑确认全部为既有失败（glob/grep/write/edit/bash/todo-write/plugin-provider/skillhub，与本 change 无关）；`packages/agent-capability` `tsc --noEmit` 无错误；`openspec validate --all --strict` 为 257 passed、1 failed——唯一失败 `change/fix-agent-web-background-task-panel-clipping` 为会话开始前已存在的无关 untracked change（缺少 delta 文件）；本 change strict validation 通过。来源：AGENTS 验证门禁。
